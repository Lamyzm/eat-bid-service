# CLAUDE.md — eatbid (학교급식 입찰 기록)

**학교급식 전자입찰(eaT) 공개 데이터로 만든 입찰 판단 재료 서비스.**
사장이 오늘 얼마를 넣을지 스스로 정하도록, 우리는 재료만 정확히 놓는다.

진행 기록: `claude-progress.txt` | 규칙: `.claude/rules/` (읽는 순서는 그 README)

---

## 헌법 — 어기면 이 제품은 존재 이유가 없다

1. **예측·추천가 금지.** eaT 예정가는 복수예가 추첨이라 원리적으로 예측 불가다. "이 값을 넣으세요"를 말하지 않는다.
2. **해석 라벨 금지.** 점수·등급·강자 배지 같은 판정을 붙이지 않는다.
3. **사실 + n(표본) 명시.** 숫자 옆에 기준이 없으면 그게 곧 거짓말이다.
4. **모르면 모른다고 한다.** 규칙으로 메우지 않는다. 빈칸이 보이는 게 틀린 값이 보이는 것보다 낫다.
5. **선언적으로 말한다.** 버튼도 "무엇을 해라"가 아니라 "무엇이 되었다"를 말한다. `투찰 저장`(명령)이 아니라 `적어둠`(상태).
6. **우리는 대신 넣지 않는다.** 사장이 NeaT에 직접 넣는다. 이 경계를 흐리는 말은 쓰지 않는다.

## 우리가 하는 일은 문자열 파싱이 아니다

**정부가 이미 잘 정리된 테이블로 내려준다. 우리는 그걸 그대로 받아 보기 좋게 놓을 뿐이다.**

그러니 **원본에서 값을 "만들어내는" 코드는 전부 제거 대상**이다. 주소를 잘라 시군구를 짓고,
기관명에서 괄호를 떼고, 공고명에서 품목을 추측하고, 이름을 이어붙여 키를 만드는 것 —
**그건 원본이 이미 준 것을 다시 만드는 헛수고이고, 그 과정에서 반드시 틀린다.**

| 우리가 만들던 것 | 원본이 이미 주는 것 |
|---|---|
| 주소 첫 토큰 → 시도 (`_SIDO_MAP` 34줄) | `SIDO_CD` 100% |
| 주소 둘째 토큰 → 시군구 | `SIGUNGU_CD` 100% · 시도 교차 0 |
| `"{시군구}\|{학교명}"` 조립 | `PURR_CD` 100% · 이름충돌 0 |
| 허용지역 이름 비교 | `PDLC_CD` — 코드가 표기 변이를 흡수한다 |
| 이름으로 만든 좌표표 | 표준 행정구역 중심점 (코드 기준) |

**예외는 품목 하나다.** 원본에 코드가 없어 이름이 유일한 식별자다(`MAIN_ITEMS` 통제 어휘 8종).
그것도 63.7%는 공고명 추정이라 **`공고명에서 추정`이라고 밝힌다.**

## 이 프로젝트가 반복한 단 하나의 실수

**"무엇인가"를 "무엇이라 불리는가"로 저장했다.**

원본이 코드를 주는데 이름을 받아 적고, 그 이름으로 비교하고, 이름을 이어붙여 키를 만들었다.
이름은 바뀌고(인천 2026-07 자치구 개편), 표기가 갈리고(`서울특별시`/`서울`/`서울시`),
부분일치한다(`서구` ⊂ `강서구`·`달서구`).

**그래서 새 값을 다룰 때 순서가 정해져 있다:**

1. **원본이 뭘 주는지 먼저 본다** — `docs/SOURCE-FIELDS.md`. 없으면 조사부터.
2. **코드가 있으면 코드를 쓴다.** 단, **`코드 → 이름`이 함수인지 실측으로 확인**한다.
   (`MLFD_CLASS_CD`는 코드처럼 생겼지만 60개 순번이었고, 45개가 두 개 이상의 이름을 가졌다)
3. **코드가 없으면 이름이 유일 식별자다**(품목이 그렇다). 그때만 문자열을 쓴다.
4. **우리가 만든 값에는 출처를 붙인다** — 원본인가 우리 계산인가. `category_src`·`qualificationBasis`·`bandBasis`가 그 예다.

## 구조

```
F:/Project/eat-bid/          수집·레이크·로더 (파이썬)
  src/eatbid/                normalize(원본→레이크) · parse · lake · sources
  tools/bidboard/            backfill · fetch_open · update_lake
  tools/serve/               load_postgres.py (레이크→서빙 DB)
  data/raw/                  원본 XML 보존 — 재파싱으로 언제든 복구 가능

F:/Project/eat-bid-service/  서비스 (모노레포)
  packages/shared/           drizzle 스키마 + zod 계약 + 도메인 SSOT
  apps/server/               NestJS. 전 컨트롤러가 app.module.ts 인라인
  apps/web/                  Next.js 16 App Router
  infra/k8s/base/            k3d + ArgoCD GitOps
```

**데이터 흐름**: eaT API → raw XML 보존 → 레이크(parquet) → 로더 → 서빙 Postgres → NestJS → Next.js

## 배포

ArgoCD가 `master`를 본다. **푸시하지 않으면 클러스터에 안 닿는다.**
스키마는 `db-migrate` Job(PreSync 훅)이 매 sync 앞에서 적용한다.

**배포는 `&&`로 잇고, 마지막에 스탬프를 대조한다.** 줄바꿈으로 적으면 앞이 실패해도 뒤가 돈다 —
CronJob의 `;` 사고와 같은 것이고, **이 문서가 그렇게 적혀 있어서 하루에 두 번 옛 이미지로 배포됐다.**

```bash
SHA=$(git rev-parse --short HEAD)
docker build --build-arg GIT_SHA=$SHA -f Dockerfile.web -t eatbid-web:dev . \
  && k3d image import eatbid-web:dev -c eatbid \
  && kubectl -n eatbid rollout restart deploy/web \
  && kubectl -n eatbid rollout status deploy/web --timeout=180s \
  && test "$(kubectl -n eatbid exec deploy/web -- printenv BUILD_SHA)" = "$SHA" \
  && echo "배포 확인 $SHA"
```

**`&&`만으로는 절반만 막힌다.** 빌드가 **성공했는데 내용이 낡은** 경우가 있다(컨텍스트 오염).
그때 이미지 태그엔 옛 이미지가 그대로 남아 import·rollout이 **정상 동작하고 아무도 실패를 보고하지 않는다.**
그래서 스탬프 대조가 진짜 처방이다 — **"도는 코드가 방금 만든 코드인가"를 직접 묻는다.**
도커 다이제스트로는 대조할 수 없다(로컬은 config digest, 파드는 manifest digest라 항상 불일치한다).

## 절대 하지 말 것

- **`schema.sql`을 psql로 직접 실행하지 마라.** 예전에 이 경로로 주요 테이블을 전량 날렸다.
- **`git add -A` 금지.** 여러 좌석이 같은 워킹트리를 쓴다. 파일을 지정해 스테이징한다.
- **Job에 `spec.template`을 고치지 마라.** immutable이라 sync가 영구히 실패하고, 그 실패가 무관한 배포까지 막는다.
- **조용한 실패를 만들지 마라.** `except: []`·`|| true`·`.catch(() => {})`·`?? 기본값`으로 넘어가면
  틀린 값이 정상처럼 보인다. 우리 사고 대부분이 여기서 났다.
- **문서·주석에 값을 복사해 적지 마라.** 소스를 가리켜라. 주석의 `5광주 13전남`(없는 코드)을 믿고
  6만 건을 안 받았고, 지어낸 테스트 픽스처가 설계를 오도했다.
- **픽스처는 실데이터에서 뽑아라.**

## 세션을 마칠 때

`claude-progress.txt` 맨 위에 항목을 추가한다. 무엇을 했는지가 아니라 **왜 그렇게 했는지**와
**실측 수치**를 남긴다. 특히 **"실측으로만 잡힌 것"** — 코드를 읽어서는 못 봤고 실제로 재봐야
드러난 것 — 을 따로 적는다. 그게 다음 사람에게 가장 값지다.
