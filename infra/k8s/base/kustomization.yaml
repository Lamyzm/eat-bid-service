apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - app.yaml
configMapGenerator:
  - name: db-schema
    namespace: eatbid
    files: [schema.sql]
generatorOptions:
  disableNameSuffixHash: true
