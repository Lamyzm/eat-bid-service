# 게이트 A 판정 기준 — **재파싱 전에 못박는다** (2026-08-29 · refactor-critic)

리더 지시: *"나중에 숫자를 보고 기준을 만들면 그건 기준이 아니라 변명이다."*
**그래서 숫자가 나오기 전에 적는다. 이 문서는 재파싱 후에 고치지 않는다.**

읽기 전용 검증자 기준. 레이크는 안 만진다.

---

## 0. 먼저 — **첫 라운드에서 이미 숫자가 안 맞았다**

리더가 준 숫자와 내가 잰 값이 다르다:

| | 리더 | 내 측정 (2026-08-29 00:10) |
|---|---|---|
| 깨진 시군구 | **724행** | `schools` 기준 **68개교** · `school_auctions` 기준 **1,310행** · `firm_bids` 기준 **411,696행** |
| schools | **5,538** | **5,596** (`updated_at` 최신 2026-08-28 14:39) |

**셋 중 어느 축도 724가 아니다.** 원인은 둘 중 하나다 — **축이 다르거나 시점이 다르다.**

> **그래서 기준 0번: 모든 숫자에 `축`과 `측정 시각`을 붙인다.**
> 안 붙이면 두 사람이 다른 걸 재고 같은 이름으로 부른다. **이미 한 번 그랬다.**

**⚠ 그리고 리더 목록에 `firm_bids` 축이 빠져 있다.** 로더가 `a.sigungu`(주소 유래)를 그대로 COPY하므로(`load_postgres.py:435-446`), **코드 전환이 `schools`만 고치고 `firm_bids` 411,696행은 그대로 둘 수 있다.** 그러면 **"고쳤다"고 믿는데 아버지 성적 화면은 안 바뀐다**(A-11). **이 축을 게이트에 넣어야 한다.**

---

## 1. 모집단 — 기준의 분모

```
아카이브 파일  233,338
레이크 회차    233,262
```
**거의 1:1이다. 재파싱이 못 닿는 행이 사실상 없다.**
→ **기준을 "재파싱 가능분 대비"로 깎을 이유가 없다. 100%에 가깝게 잡는다.**

(내가 처음엔 "아카이브가 레이크보다 적어 상한이 있을 것"으로 의심했는데 **아니다.** 그 우려를 여기 적어 폐기한다.)

---

## 2. 항목별 통과/멈춤 — **미리 못박음**

### A-1. `region_src='code'` 비율

| | |
|---|---|
| **통과** | **≥ 99%** |
| **조사 후 진행** | 95~99% — 나머지의 정체를 밝히고, 설명되면 진행 |
| **🛑 멈춤** | **< 95%** |

**근거:** 원본 `SIGUNGU_CD` 채움 **100%**, 아카이브가 레이크를 덮는다(§1). **코드 경로를 못 타는 행이 5%를 넘으면 `normalize`에 조건 분기가 남았다는 뜻**이고, 그건 재파싱을 다시 해야 하는 상태다.

> 리더 질문 *"40%면 통과인가"* → **명백한 실패다.** 100% 충전 필드를 40%만 쓰는 건 파이프라인이 안 바뀐 것이다.

### A-2. 오염 이름 — **A-3와 하나로 합친다**

리더 목록의 2번(7이름)과 3번(깨진 시군구)은 **같은 검사의 다른 표현**이다. 정규 형식 검사가 7이름을 포함한다. 따로 두면 하나만 통과했을 때 판단이 갈린다.

**검사: `^[가-힣]{1,6}(시|군|구)$` 불일치 행 수. 세 축 전부.**

| 축 | 현재 (08-29 00:10) | 통과 | 🛑 멈춤 |
|---|---|---|---|
| `schools.sigungu` | 68 | **0** | > 0 |
| `school_auctions` (school_id 앞부분) | 1,310 | **0** | > 0 |
| **`firm_bids.sigungu`** | **411,696** | **0** | **> 0** ← 리더 목록에 빠져 있던 축 |

**근거: 코드에서 유도하면 오염이 생길 수 없다.** `급식실`·`식생활관`·`참고`는 `SIGUNGU_CD`가 만들 수 있는 값이 아니다. **하나라도 남으면 그 행이 코드 경로를 안 탄 것**이고, 그건 A-1과 같은 문제다.

**`firm_bids`가 0이 안 되면 "부분 성공"이 아니라 멈춤이다** — 사장이 실제로 보는 화면이 그 축이다.

### A-4. `built` vs `loaded` 차 — **숫자가 아니라 등식으로 판정한다**

| | |
|---|---|
| **통과** | **`built − loaded == Σ(skipped 카운터)`** — 오차 0 |
| **🛑 멈춤** | 불일치. 크기와 무관하게 |

**근거: 재파싱하면 숫자가 다 바뀌지만 등식은 안 바뀐다.** "1,170"을 기준으로 잡으면 재파싱 후 그 숫자가 달라졌을 때 통과인지 실패인지 못 정한다. **설명되지 않는 손실이 0인가**가 진짜 질문이다.

**그리고 `skipped` 카운터가 그 차를 전부 설명해야 한다** — 지금 로더가 `시군구없음`·`문턱(낙찰회차5미만)`·`기관명정제` 셋을 센다(`load_postgres.py:224·228·507`). **넷째 원인이 있으면 등식이 안 맞고, 그게 곧 발견이다.**

### A-5. `(SIGUNGU_CD, ym)` 같은 달 이름 둘 — **내가 §7-⑧에서 경고한 것**

**두 층으로 나눈다. 통과 조건은 (b)다.**

| 검사 | 통과 | 🛑 멈춤 |
|---|---|---|
| **(b) 최빈값이 정규 시군구 형식이 아닌 `(코드,ym)` 셀** | **0** | > 0 |
| (a) 같은 ym에 이름이 둘 이상인 셀 | **기록만** (표기 변이는 정상) | — |

**근거:** `SPEC-CODEBOOK` §0이 이름을 `mode(주소시군구)`로 뽑는다. **이름의 출처가 여전히 주소 파싱이다.** 그래서 오염이 코드북 셀의 최빈값이 될 수 있다 — 관측이 적은 셀에서.

**(a)를 통과 조건으로 쓰면 안 된다.** 표기 변이(`창원시`/`창원`)는 정상이고 코드북이 흡수하라고 있는 것이다. **(b)가 진짜 사고다** — 코드가 `급식실`을 자기 이름으로 갖는 것.

**참고 baseline:** `(시도,ym)` 축에서 최빈값이 비정규인 셀은 현재 **2**다. **시군구 축은 지금 못 잰다**(레이크에 `sigungu_code`가 없다). **재파싱이 처음 재는 기회이고, 그게 A-5의 존재 이유다.**

### A-6 (추가) — **셀의 표본 크기**

`SPEC-CODEBOOK ## 비판` §③-7에서 적은 것을 기준에 넣는다.

| | |
|---|---|
| **기록 필수** | `(SIGUNGU_CD, ym)` 셀 중 **n=1인 비율** |
| **🛑 멈춤** | 없음 — **판정이 아니라 계측이다** |

**근거:** `(기관,ym)` 축에서 n=1 셀이 **39.7%**였다. 시군구 축도 비슷하면 **최빈값의 절반 가까이가 관측 하나에서 나온다.** 게이트 (b)는 그걸 못 본다(이름이 하나뿐이라 항상 통과).
**멈추진 않는다** — 표본이 작은 게 오류는 아니다. **다만 숫자를 남겨야 나중에 "왜 이 이름이 틀렸나"를 물을 수 있다.**

---

## 3. 이 기준이 못 보는 것

**넷을 미리 신고한다. 게이트 A를 통과해도 아래는 안 잡힌다.**

1. **`SIGUNGU_CD`가 있는데 값이 틀린 경우.** 형식 검사는 `^[가-힣]{1,6}(시|군|구)$`만 본다 — **`김해시` 자리에 `창원시`가 들어와도 통과한다.** 코드→이름 대조가 필요한데 그 정답지가 다시 주소다(순환).
2. **재파싱이 일부 파티션만 덮은 경우.** 비율 검사는 전체 평균이라, **특정 연·월만 안 됐어도 99%가 나올 수 있다.** → **연·월별로 쪼개 봐야 한다.** 이건 A-1의 보조 검사로 돌린다.
3. **서빙 DB에 반영됐는지.** 레이크가 고쳐져도 **재적재가 안 돌면 화면은 그대로다.** 오늘 "만들었다와 돈다 사이"에서 네 번 났다 — **게이트 A는 레이크 기준이고, 화면 기준 확인이 따로 필요하다.**
4. **품목.** 코드가 없어 이 게이트 밖이다. 재파싱으로 `main_item` 충전율이 오르는데 **그건 A-1~A-6 어디에도 안 걸린다.** 별도 항목이 필요하면 그때 세운다.

---

## 4. 검증 방법 — **명령을 따로 짠다**

리더 지시대로 `data`와 **같은 쿼리를 쓰지 않는다.**
- `data`는 로더/파이프라인 쪽에서 셀 것이다 → **나는 서빙 DB와 파케이를 직접 읽는다**
- 정규식도 다르게 쓴다 — 나는 `^[가-힣]{1,6}(시|군|구)$` **불일치**를 세고, 저쪽이 화이트리스트로 세면 **두 결과가 다를 때 그 차이가 곧 발견**이다
- **레이크는 읽기만.** 파케이 직접 조회, 쓰기 없음

**보고 형식:** 각 항목에 `축 · 측정시각 · 값 · 판정(통과/조사/멈춤)`. **축과 시각이 없으면 그 숫자는 안 쓴다.**

---

## 5. 개정 — 게이트 분리 판정 (2026-08-29 · 재파싱 직후)

### ① 분리는 정당하다. **그런데 goalpost가 아닌 이유가 리더 근거와 다르다 — 내 실수다**

**판정 기준: 근거가 숫자 이전에 참이었고, 그때 알 수 있었나.**

**참이었고, 알 수 있었다.** `normalize.py:93/95`의 `_split_address(DOG_ADDR)`를 **내가 `AUDIT-STRINGS.md` §2-4에 직접 인용해뒀다** — *"sido·sigungu의 뿌리. 토큰 위치가 다르면 `급식실`이 들어간다"*. **증거를 내 손으로 적어놓고 A-2의 전제를 반대로 썼다.**

> **그러니 이건 리더가 기준을 고치는 게 아니라, 내가 기준을 잘못된 단계에 붙인 것이다.** A-2는 처음부터 **전환 게이트**였고 내가 재파싱 게이트에 넣었다.

**분리 승인. 단 조건 둘을 함께 박는다:**

1. **A-전환에 기한 또는 선행조건을 명시하라.** *"단계 2로 미룬다"*가 반복되면 게이트가 영원히 안 닫힌다. **미루는 것과 없애는 것을 구별하는 건 날짜뿐이다.**
2. **🔴 A-재파싱만 통과한 상태를 "재파싱 성공"이라 부르지 마라.** 표현을 이렇게 제약한다:
   > *"코드가 들어왔다. 화면은 아직 안 바뀌었다."*

   **이게 실질 방어다.** 화면이 안 바뀐 걸 성공이라 부르는 순간, 그게 오늘 네 번 난 **"만들었다와 돈다 사이"**다(pre-commit 훅·배포 스탬프·크롤 정지·급감 가드).

### ② `PDLC_NM` 79.6%로 단계 2를 시작해도 된다 — **비율이 아니라 분포가 답을 준다**

**내가 `AUDIT-STRINGS` §10-26에 세운 규칙을 여기 적용한다.**

**①(처방이 달라지나): 안 달라진다.** 79.6%든 100%든 처방은 같다 — *"이름 있으면 이름, 없으면 미상"*. **비율은 판정선으로 쓸 값이 아니다.**

**②(분포 중 어느 부분이 처방을 가르나): 이게 진짜 질문이고, 실측했다.**
```
아버지 지역 PDLC_NM 커버리지
  경남/김해시 352 · 경남/창원시 775 · 경남/거제시 83 · 경남/통영시 64   ← 넷 다 있다
빠지는 쪽 (서울)
  서울/전체 2,954  vs  서울/노원구 5 · 중랑구 5 · 도봉구 4 · 동대문구 4
```
**아버지 지역 네 곳이 전부 커버된다. 빠지는 20.4%는 서울 자치구다.**

→ **시작해도 된다. 아버지 화면 영향 0이다.**

**⚠ 단 기록한다: 이건 "해결"이 아니라 "지금 안 아픔"이다.** 서울 사용자가 생기면 그날 20.4%가 실해가 된다. **없는 문제를 지금 풀지 않되, 언제 생기는지는 적어둔다.**

### ③ 코드보다 미상이 낫다는 판단에 동의한다. **단 표기가 틀렸다**

**동의 근거는 헌법 4번이다** — *"모르면 모른다고 한다. 규칙으로 메우지 않는다. **빈칸이 보이는 게 틀린 값이 보이는 것보다 낫다**."*
`급식실`은 **틀린 값**이고, 이름 없음은 **모른다는 사실**이다. 후자가 낫다. 여기까지 동의.

**🔴 그런데 `서울특별시 (01003)`은 아니다.**

**코드를 화면에 노출하는 건 우리 사정을 사용자에게 떠넘기는 것이다.** 사장은 `01003`을 못 읽는다. 그건 **모른다는 표시가 아니라 우리 내부 값의 유출**이다.

```
❌ 서울특별시 급식실        ← 틀린 값
❌ 서울특별시 (01003)       ← 우리 사정
✅ 서울특별시 · 시군구 미상   ← 모른다는 사실
```

**헌법 5번(선언적으로 말한다)에도 이쪽이 맞다.** 코드는 **디버그 정보**지 표시값이 아니다. 화면에 필요하면 `title` 속성이나 개발자 도구로 남겨라.

**그리고 이건 이미 선례가 있다** — `qualificationBasis: "region-only"`가 *"화면은 '자격 충족'이 아니라 '지역 자격 충족'이라고 말해야 한다"*로 풀렸다. **코드를 노출한 게 아니라 아는 범위를 말했다.** 같은 형태로 가라.

---

## 6. A-전환 — 통과 조건·선행조건·망각 방지 장치 (2026-08-29)

### 6-1. `시군구 미상`은 오염이 아니다 — **검사 자체를 바꿔야 한다**

```
급식실 = 틀린 값 (주소 파싱 산물)
미상   = 모른다는 사실 (코드는 있고 이름표가 없음)
```
**정반대다.** 옛 A-2 검사(`^[가-힣]{1,6}(시|군|구)$` 불일치 = 실패)를 그대로 쓰면 **정직한 표기를 오염으로 센다.**

**🔴 그리고 형식 검사는 애초에 약하다.** 내가 §3-1에 적었다 — *"`김해시` 자리에 `창원시`가 와도 통과한다."* 형식은 **값이 그럴듯한지**만 보지 **어디서 왔는지**를 못 본다.

→ **출처로 판정한다. 형식 검사를 버린다.**

| | 검사 | 통과 | 🛑 멈춤 |
|---|---|---|---|
| ~~T-1~~ | ~~`region_src = 'address'`인 행 수 = 0~~ | **폐기 (§6-1-a)** | |
| **T-1′** | **`시군구없음`으로 버려진 회차 중 `sigungu_code`가 있는 것** | **0** (현재 **9,521**) | > 0 |
| **T-2** | `시군구 미상` 비율 | **계측만** (예상 ~20.4%) | — |
| **T-3** | **아버지 지역(김해·통영·거제·창원)에 `미상`인 행** | **0** | > 0 |

#### 6-1-a. 🔴 T-1 폐기 — **내 설계 결함** (2026-08-29 03:10 측정)

내가 T-1에 적었다: *"출처가 `codebook`이면 `급식실`이 나올 경로가 없다. 값을 안 봐도 된다."* **맞는 말인데 `region_src`가 출처가 아니다.**

`load_postgres.py:375`
```python
_region_src = "code" if _sgg_cd else ("address" if (sgg or "").strip() else "none")
```
이름은 出處(`region_src`)인데 계산하는 건 **존재 여부**(`if _sgg_cd`)다. **나는 컬럼 이름을 읽고 의미를 믿었다** — 이 문서가 내내 금지한 그것이다([[identity-and-keys]] K1).

그리고 더 나쁜 게 있다. 이 줄은 `:315`의 `continue`를 **통과한 그룹에서만** 실행된다:
```python
if not sgg or not sgg.strip():          # :315  주소 시군구가 비면
    skipped["시군구없음"] += len(aus)     # :317  버린다
    continue                            # :319  ← region_src 계산에 도달하지 않는다
```
- 코드가 **없는** 행 → 0건(A-1 커버리지 100%)이라 `'address'`가 안 나온다
- 주소가 **없는** 행 → `region_src`를 계산하기 **전에 삭제된다**

**`region_src='address'`는 구조적으로 0이다.** 반증할 행이 컬럼이 계산되기 전에 지워진다. **T-1은 항상 통과하고, 통과해도 아무 뜻이 없다.**

**T-1′이 강한 이유: 버려진 쪽을 센다.** `region_src`는 살아남은 쪽만 보는데 사고는 버려진 쪽에서 난다. 측정값:

```
주소 시군구 = 빈 문자열이고  sigungu_code 는 있는 회차 = 9,521
  (적재 요약의 `시군구없음 9513` 과 같은 자리. 8 차이는 축 차이 —
   내 축은 raw auctions, 로더 축은 join bids 뒤)
```

**재파싱이 100% 코드를 줬는데 로더가 9,521 회차를 여전히 버린다.** 그게 "재파싱 완료"와 "A-전환 완료" 사이의 거리다.

**측정 장치는 이미 있다.** 이 숫자는 적재 요약에 매번 찍히고 있다. §6-3 장치에 새 개념이 필요 없다 — 그 줄 옆에 `(그중 코드있음 N) <-- A-전환 미완료`를 붙이면 된다.

#### 6-1-b. 폐기한 가설 하나 — 조용한 탈락은 없다

로더 쿼리 `where a.sigungu is not null`(`:228`)이 코드 있는 행을 **세지 않고** 버린다고 의심했다. 쟀다:

| | |
|---|---|
| `sigungu is null` | **0** |
| `sigungu = ''` (빈 문자열) | 9,521 |

**`sigungu`는 NULL이 된 적이 없다.** `is not null` 관문은 전부 통과시키고 탈락은 `:315`에서 일어나며 **거기서는 세고 있다.** 가설 폐기.

(다만 `:228`이 빈 값을 거르려던 의도였다면 **의도한 일을 안 하고 있다.** 지금은 `:315`가 대신 해서 사고가 안 났을 뿐이다.)

**T-3이 있는 이유:** §10-26 ②(분포가 처방을 가른다). 실측으로 아버지 지역 넷이 `PDLC_NM` 커버리지에 있으니(김해 352·창원 775·거제 83·통영 64) **거기 `미상`이 뜨면 배선이 잘못된 것**이다. 전체 비율이 아니라 **이 넷**이 판정한다.

### 6-2. 선행조건 — 순서까지

```
1. PDLC_CD 를 레이크에 싣는다            ⚠ normalize 가 CTPV_CD·SGG_CD·SGG_NM·PDLC_NM 만 뽑는다.
                                            이번 재파싱에 PDLC_CD 가 포함됐는지 먼저 확인
2. cb_qual_area (pdlc_cd → name) 생성
3. [ 로더가 sigungu 를 코드북에서 조회  +  region_src 출처 컬럼 추가 ]  ← 같은 배포. 갈라지면 안 된다
4. 재적재
5. 화면 문구 (`시군구 미상`)
```

**3번을 가르면 안 되는 이유: 출처 컬럼 없이 이름만 바꾸면 T-1을 판정할 수 없다.** 그러면 형식 검사로 되돌아가고, 그건 §6-1에서 버린 것이다. **판정 수단과 판정 대상이 같은 배포에 들어와야 한다.**

**1번이 미확인이다.** `PDLC_CD`가 레이크에 없으면 2~5가 전부 막힌다. **A-전환 착수 전 첫 확인 항목이다.**

### 6-3. 🔴 망각 방지 — **날짜가 아니라 반복 노출이다**

리더 관찰이 정확하다: *"전부 미룬 게 아니라 잊은 것이다."* 여섯(훅·스탬프·정지장치·급감가드·좌표배포·`docs/` 미포함) 중 **의도적 연기는 하나도 없다.**

**그래서 "문서 한 줄이면 또 잊힌다"가 맞다.** 문서는 한 번 읽고 만다.

**이번 세션에서 실제로 안 잊힌 게 뭐였는지 되짚으면 답이 나온다:**

| 잊힌 것 | 안 잊힌 것 |
|---|---|
| 문서에 적은 것 | **적재 요약에 숫자로 찍히는 것**(R21 카운터) |
| "나중에" 태그 | **화면에 뜨는 것**(사장이 본다) |
| 커밋 메시지 | **빌드를 깨는 것** |

> **잊히지 않는다 = 반복적으로 눈에 띈다.** 한 번 보이는 것은 전부 잊혔다.

**장치: `region_src='address'` 행 수를 적재 요약 맨 위에 매 실행 출력한다.**
```
  전환: 이름을 주소에서 만든 행 185,209  <-- A-전환 미완료
```
- 재파싱 직후엔 **100%**, 전환 후엔 **0**
- **매일 크론 로그에 찍힌다.** 안 줄면 계속 보인다
- **날짜보다 강하다** — 날짜는 지나면 그만이고, **이 숫자는 고쳐질 때까지 계속 나온다**
- **패턴이 이미 로더에 있다**: `flag = "" if (n_split==0 and n_merged==0) else "  <-- 확인 필요"`(`load_postgres.py:522`). 새 개념이 아니라 **한 줄 추가다**

**날짜는 보조로 붙인다: 9/12 동결 전 필수.** 동결이 실재하는 마감이라 그 날짜는 살아 있다. **다만 주 장치는 숫자다** — 날짜만 걸면 9/12에 또 잊힌다.

**그리고 이 장치의 한계도 적는다:** 적재 요약은 **크론 로그**에 있고 **사람이 안 본다**(`verify_live.py`가 배선된 적 없는 것과 같은 위험). **더 강하게 하려면 그 숫자를 `/api/healthz`나 화면 어딘가에 실어야 한다.** 지금은 안 한다 — **먼저 로그에 넣고, 9/12까지 0이 안 되면 그때 화면으로 올린다.**

---

## 7. 실측 결과 — 게이트 A-재파싱 판정 (2026-08-29 03:00~03:40)

**축 · 측정 방법:** 레이크 `F:/Project/eat-bid/data/parquet` 전량(재파싱본 233,336회차), duckdb 1.1.3(`eat-bid/.venv` — 로더가 쓰는 바로 그 버전). **서빙 DB를 보지 않고** 레이크에서 로더 로직(`clean_inst`·`canon_sgg`·그룹핑·문턱)을 **정의째 옮겨 재현**했다. 로더를 돌리지 않았다.

| | 기준 (숫자 나오기 전 못박음) | 실측 | 판정 |
|---|---|---|---|
| **A-1** | `sigungu_code` 커버리지 ≥ 99% | **100.00%** | ✅ |
| A-1 하위 | 연·월로 쪼개도 유지 | 2023·2024·2025·2026 **전부 100.00%**, 커버리지 최저 월 10개도 전부 100.00% | ✅ |
| **A-4** | `그룹 = built + skip`, `built = loaded + conflict` — 오차 0 | **두 등식 다 성립** | ✅ |
| A-5 (a) | 기록만 | 셀 3,354 · 이름 둘 이상 638 (19.0%) · 회차 91,038 | 계측 |
| **A-5 (b)** | 최빈이 비정규 형식인 셀 = **0** | **12** (0.36%) | ❌ **→ §7-3** |
| A-6 | 기록만 | n=1 셀 133 / 3,354 (4.0%) | 계측 |

### 7-1. A-4 등식 (내 재현)

```
원장 행 233,336 · 기관명정제 탈락 12
그룹 8,197  = built 5,607 + skip 2,590          → 성립 (오차 0)
built 5,607 = loaded 5,602 + conflict 5         → 성립 (오차 0)
skip(학교):  시군구없음 761 · 문턱(낙찰회차5미만) 1,829
skip(회차):  시군구없음 9,521 · 문턱 4,416
```

**`built 5,607`이 리더가 적재 요약에서 본 값과 일치한다.** 다른 경로로 센 두 숫자가 같다.

**`conflict` 1,112 → 5.** 재파싱 전의 1,112는 시도 표기 변이로 그룹이 갈린 것이고, 남은 5는 **진짜 동명이인**이다:
`사천시|곤명초등학교` · `강서구|송정중학교` · `김해시|한얼중학교` · `고양시|고양일고등학교` · `창원시|구산초등학교`

전부 다른 시도에 같은 이름의 학교가 있는 경우다. **`school_id = "{시군구}|{기관명}"`에 시도가 없다** — `market_regions` PK에 `sido`가 빠졌던 것과 같은 결함이다. 5건이라 작아 보이지만 구조가 같다. A-전환에서 `school_id`를 코드 기반으로 바꾸면 같이 닫힌다.

### 7-2. 🔴 T-1′ 실측 — **761 / 761**

```
'시군구없음'으로 버려진 학교 761개 중 sigungu_code 가 있는 것 = 761   (전부)
그 회차 = 9,521
```

**대부분이 아니라 전부다.** 이름이 없어서 버리는 학교가 하나도 빠짐없이 코드를 갖고 있다. 재파싱은 답을 실어 왔고 로더가 안 쓴다. **현재값 761(학교)/9,521(회차) → 목표 0.**

### 7-3. A-5(b) 실패를 어떻게 다루나 — **기준을 고치지 않는다**

```
최빈이 비정규 형식인 셀 12:
  '급식실' 6 · '동구송림2동' 1 · '지정' 1 · '2월' 1 · '금천구(계약' 1 · '성수고등학교' 1 · '참고' 1
```

**기준은 숫자가 나오기 전에 못박았고, 문자 그대로 실패다. 0을 12로 고치지 않는다.**

**다만 이 실패가 무엇을 막아야 하는지는 별개로 판단한다.** (b)가 재는 값은 **주소에서 유도된 이름**이고, 그건 A-전환이 통째로 교체하는 값이다. **사라질 값의 품질이 게이트를 막으면 순서가 뒤집힌다.**

→ **판정: A-재파싱은 막지 않는다. A-전환 후 코드북 이름으로 재측정하고, 거기서 0이 진짜 바(bar)다.** 그때 0이 아니면 코드북 결함이고 그건 막는다.

**내 설계 미흡을 같이 적는다:** §2 A-5에 기준을 쓸 때 "셀의 최빈 이름"이 **주소 파생값**이라는 걸 명시하지 않았다. 기준이 층을 안 갈랐다. 결과가 나빠서 바꾸는 게 아니라, **기준이 두 층을 하나로 적었다**는 것이 결함이다.

### 7-4. A-5(a) 19%는 코드북 전환을 **지지한다** — 반대가 아니다

`SPEC-CODEBOOK ## 비판` §③-8에 *"재파싱 후 필수 재측정"*으로 걸어 둔 항목. 쟀고, 내용이 방향을 뒤집는다:

```
코드 118 · 202608  ['화성시(180)', '급식실(1)']
코드  46 · 202608  ['북구(187)', '9월(1)']
코드  48 · 202608  ['수성구(169)', '급식실(3)', '2026학년도(1)']
코드  53 · 202608  ['남동구(71)', '남동구만수2동(1)']
코드 406 · 202608  ['제천시(10)', '제천산업고등학교(2)', '급식소/홍광초등학교(1)',
                    '남당초,(2)', '급식소(1)', '제천중학교(2)']
```

**코드가 흔들리는 게 아니다. 이름이 쓰레기다.** 진짜 시군구가 n=180이고 나머지가 n=1~3인 주소 파싱 잔해다. 코드 406(제천시)은 6개 이름 중 5개가 학교명이다.

전 기간 축도 같다 — 이름 1개인 코드 92 · 2개 44 · 3개 23 · 4개 18 · … · 14개 1 · **22개 1** · 이름이 아예 없는 코드 5.

**흔들리는 축이 코드가 아니라 이름이라는 것이 확인됐다.** 그게 전환의 근거다.

### 7-5. 선행조건 1번 — **해소됨. 단 처방이 리더 안과 다르다**

`PDLC_CD`는 **레이크에 이미 있다.** 못 읽었을 뿐이다.

```
lake.connect() 자체            OK
auctions/bids/reserves count(*) OK  (233,336 / 10,581,014 / 3,497,385)
areas 스캔                     FAIL  TypeMismatchException
areas.pdlc_cd 접근             FAIL  Binder Error: Could not find key "pdlc_cd"
```

**깨지는 건 `areas` 컬럼 하나뿐이고, `auctions` 하나뿐이다**(`bids`·`reserves`는 전 컬럼 투영에서도 OK — `areas`가 없다).

`connect()`가 `areas`를 **먼저 본 파일의 스키마로 통합했고 4필드가 이겼다.** 그래서 `pdlc_cd`가 "없는 키"다.

**선택자는 ns 마커가 아니라 스키마다:**

| | 파일 수 | |
|---|---|---|
| 리더 안 (`ns < 마커`, 3테이블) | 4,019 | **2,754개가 문제와 무관** |
| 실제 원인 (`areas`가 4필드인 auctions) | **1,265** | |

마커는 스키마 경계가 아니다 — 56컬럼 쓰기는 **08-28 21:12**에 시작했고 마커는 **08-29 00:48**이다. 마커 미만에 5필드 파일이 **73개** 있고, 21:12~21:57 **45분간은 두 형식이 동시에** 쓰였다. 레이크에 세대가 **넷** 있다(31·32·36·56 컬럼).

**실증:** 4필드 파일 1,265개만 목록에서 빼고 읽으면
```
areas 스캔 OK · 회차 233,336 (그대로) · areas 비어있지 않음 225,136 (96.5%)
areas.pdlc_cd OK · 비어있지 않은 원소 441,590
```

**→ 지우지 말고 옮긴다.** `data/parquet.pre-reparse/auctions/...`로 상대경로 유지 이동. `lake.connect()`는 `parquet_dir/{table}/**/*.parquet`만 글롭하므로(`lake.py:124`) `data/parquet` 밖은 안 보인다. 같은 볼륨이라 rename이고 **롤백은 반대 방향 이동**이다. **"롤백을 버릴 것인가"는 답할 필요가 없는 질문이었다.**

**덮는지 확인은 카운트가 아니라 집합차로 했다:**
```
                        마커미만      마커초과    옛것에만 있음
auctions (bid_id)        233,262     233,336         0
bids  (bid_id,biz_no) 10,568,378  10,581,014         0
reserves (bid_id,seq)  3,496,275   3,497,385         0
```
**세 테이블 전 중복제거키에서 0.** 그리고 재파싱 실패 2건(`5546909`·`5546797`)은 **옛 파일에도 없다** — 원본 수집 때 같이 실패했다. 재파싱이 만든 손실이 아니다.

### 7-6. ⚠ 옮겨도 안 없어지는 것

`connect()`가 `areas`를 **먼저 본 파일의 스키마로 통합**한다. 이번엔 4필드가 이겨서 예외가 났다. **순서가 반대였으면 조용히 통과하고 옛 파일의 `areas`만 NULL로 나왔을 것이다** — 그게 훨씬 나쁘다. 예외는 시끄럽지만 NULL은 조용하다.

**이번에 우리를 구한 건 DuckDB가 죽어 준 것이다. 설계가 아니라 운이다.** [[failing-loudly]] F1.

### 7-7. 원인은 `reparse.py`다

`scripts/reparse.py:12` 독스트링 — *"절대 라이브 레이크(`data/parquet`)를 건드리지 않는다"*
`scripts/reparse.py:48` 도움말 — *"`data/parquet`과 달라야 한다"*

**그걸 강제하는 코드가 없다.** 유일한 방어는 `_out_dir_is_nonempty()`(`:59-60`) — "비어 있지 않으면 거부". `--out-dir data/parquet`은 당연히 비어 있지 않으니 `--force`를 붙이게 되고, 붙이는 순간 라이브 레이크에 쓴다.

**`--force`가 두 뜻이다** — "새 디렉터리에 이어 쓴다"(정당)와 "라이브 레이크에 쓴다"(사고). 거부 메시지는 *"라이브 레이크를 실수로 덮어쓰지 않도록 거부합니다"*(`:132-134`)라고 **이름까지 대면서 정확히 그 사고를 못 막았다.**

`--out-dir`이 `parquet_v2`였다면 스키마 혼재도, 읽기 실패도, 롤백 딜레마도 없었다. **디렉터리 이름을 바꾸기만 하면 되는 일이었다.**

**처방(한 줄):** `main()`에서 `args.out_dir.resolve() == paths.parquet_dir.resolve()`면 **`--force`와 무관하게** 거부. `--force`로 뚫리면 안 되는 것이다. DEBT (e) 파괴적 경로.

**참고:** `data/parquet_v2`는 08-26 10:19~10:23에 쓴 **32컬럼** 파일이다(auctions 1,688개, 전체 5,064개 164MB). **지금 형식과 두 세대 뒤쳐진 잔해.** 급하지 않다.

### 7-8. 폐기한 가설 — 세기 전에 보냈으면 오보였다

1. **"옛 파일과 새 파일은 같이 못 읽는다"** → 처음 세 번의 시험이 전부 성공해서 재현 실패로 보고할 뻔했다. **내 시험이 `count(*)`였다.** `count(*)`는 구조체를 실체화하지 않는다. **투영을 안 하면 타입 통합이 안 일어나므로 그 성공은 아무것도 증명하지 않았다.** 리더 보고가 맞았다.
2. **"`where a.sigungu is not null`(`:228`)이 코드 있는 행을 조용히 버린다"** → `sigungu is null` = **0**, `sigungu = ''` = 9,521. **NULL이 된 적이 없다.** 탈락은 `:315`에서 일어나고 거기서는 센다. 조용한 탈락 없음.

**둘 다 코드를 읽고 세운 위험이었고, 재 보니 하나는 시험이 틀렸고 하나는 위험이 없었다.** 코드를 읽고 위험을 세우는 것과 그 위험이 실현되는지 재는 것은 다른 일이다.

### 7-9. A-전환의 이득 — 실측 (2026-08-29 03:55)

**⚠ `areas.pdlc_nm` 은 학교 위치의 출처가 아니다.** 버려지는 9,521 회차의 `PDLC_NM` 을 열어 봤다:

```
빈 시군구 회차 9,521 · areas 있음 8,392 (88.1%) · areas 아예 없음 1,129

'서울/전체' 3,137 · '경기/전체' 1,538 · '부산/전체' 600 · '경남/전체' 262
'경기/하남시' 617  vs  '경기/하남' 367       ← 코드북 원본에도 표기 변이가 있다
'경기/성남시' 336  vs  '경기/성남' 265
```

상위 15개 중 `/전체` 항목만 **5,537건 = 66%**. `경기/전체` 는 **"경기 업체 누구나 입찰 가능"**이지 학교가 경기 어디에 있는지가 아니다. **이걸로 시군구를 채우면 "어디 학교인가"를 "누가 입찰할 수 있는가"로 대체하는 것**이다 — 이 문서가 내내 잡은 그 오류의 또 다른 판이다.

`areas` 는 참가제한지역 화면(`allowed_pdlc_cd`)의 출처로는 정확하다. **학교 위치의 출처는 `SIGUNGU_CD`다.**

**§6-1 T-3 의 근거를 바꾼다.** 예전 근거(김해 352·창원 775·거제 83·통영 64가 `PDLC_NM` 에 있다)는 **참가제한지역 커버리지**였다. 아래가 맞는 축이다.

#### 코드북: `(시도코드, 시군구코드) → 이름`, **투표에 정규 형식만 넣는다**

```
distinct sigungu_code       198
distinct (sido_cd, sgg_cd)  198        ← 시도 교차 0. SIGUNGU_CD 단독으로도 키가 된다
정규 형식 이름을 가진 코드   192        ← 나머지 6개가 이름을 못 뽑는 코드
정규 이름이 둘 이상인 코드    21
```

둘 이상인 21개를 열면 **불안정이 아니다 — 층 차이다:**
```
('15','714')  창원시 12,879 · 성산구 4 · 마산합포구 2 · 진해구 2
('12','713')  전주시  8,404 · 덕진구 61 · 완산구 21
('18','61')   남구    2,950 · 광산구 6
('1','15')    서초구  2,409 · 강남구 27 · 서울특별시 1
('4','720')   제물포구   62 · 동구 19 · 중구 16 · 서구 4     ← 인천 2026-07 개편
```
**코드는 시(市) 층위, 주소는 구(區) 층위.** 최빈이 압도적이다(12,879 대 4). `('4','720')` 만 진짜 개명이고, **최빈이 현재 이름 `제물포구`를 준다** — §23-1(코드는 안 바뀌고 이름만 바뀌었다)이 여기서 값으로 확인된다.

**정규 형식 필터가 핵심이다.** 코드 713의 이름 22개 중 19개, 714의 14개 중 8개가 이 필터에서 사라진다. **A-5(b)의 12건도 이걸로 0이 된다.**

#### 구제율

```
버려지는 학교 761곳
  코드북으로 이름 붙음 : 학교 716 · 회차 9,412
  이름 못 붙임(=미상)  : 학교  45 · 회차   109
  구제율  94.1% (학교)  ·  98.9% (회차)
못 붙인 코드: (16,700) 67 · (9,369) 13 · (9,361) 12 · (14,620) 9 · (9,366) 8
```

**T-3 확인:** 김해시 `('15','653')` · 창원시 `('15','714')` · 거제시 `('15','650')` · 통영시 `('15','665')` — **넷 다 코드북에 있다.**

#### 🔴 T-2 를 고쳐 적는다 — 내 숫자가 두 자릿수 틀렸다

§6-1 에 *"`시군구 미상` 비율 계측만 (예상 ~20.4%)"* 라고 적었다. **실제는 109 / 233,336 = 0.047%다.** 20.4%는 `PDLC_NM` 커버리지에서 나온 값이고 **그건 참가제한지역이라 애초에 다른 축이었다.**

`시군구 미상` 문구는 여전히 필요하다(109건이 존재한다). **다만 화면에 거의 안 보인다 — 문구 설계에 들일 힘을 줄여도 된다.**

#### 선행조건, 갱신

```
1. PDLC_CD 를 레이크에 싣는다                      ✅ 이미 있다. auctions 1,265개만 옮기면 읽힌다
2. 코드북 (시도코드,시군구코드) → 이름              ✅ 192/192. 정규형식 필터가 핵심
3. [ 로더가 시군구를 코드북 조회 + 출처 컬럼 ]      ← 여전히 한 배포. 갈라지면 T-1′ 판정 불가
4. 재적재                                          → 학교가 5,607 → 약 6,323 으로 늘어야 한다
5. 화면 문구 (`시군구 미상`) — 109건짜리다
```

**1·2가 닫혔다. 남은 건 3이다.**

⚠ **위 숫자는 내 실험이지 로더가 하는 일이 아니다.** "로더가 코드북을 쓰면 이렇게 된다"는 **예측**이다. 실제 값은 3번 배포 후 재적재에서 다시 잰다. **5,607 → 약 6,323 으로 안 늘면 배선이 틀린 것이다.**

**코드북 키는 쌍으로 잡기를 권한다.** 지금 교차가 0인 것이지 **앞으로도 0이라는 보장은 원본에 없다.** 비용이 컬럼 하나다.
