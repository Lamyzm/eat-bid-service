# SPEC-SSOT — 중복 선언을 구조적으로 불가능하게 (2026-08-28 · architect)

사용자 원문: *"지역이나 축산 등 정규식이 적용된 부분을 정합하게 + 프론트 서버쪽으로 코드를 안 흩고,
SSOT로 중복선언 안 할 수 있나."*

**질문은 "SSOT를 선언할 수 있나"가 아니라 "중복 선언이 불가능하게 만들 수 있나"다.**
문서는 안 지켜진다. 우리는 네 번 안 지켰다. 그래서 이 문서는 **메커니즘**만 다룬다.

**세 문장 답:**
> **① SSOT의 단위는 값이 아니라 (값, 출처)다. 출처 없는 값은 계약 위반이고, 적재는 자기 품질을 숫자로 신고한다.**
> **② 경계를 넘는 것은 "어휘"뿐이고, 어휘는 한 곳에서 선언해 나머지를 생성한다.**
> **③ 알고리즘은 경계를 넘지 않는다 — 층에 못박고, 층을 어기면 빌드가 깨진다.**

②만 있던 게 1차 설계였고, critic 측정치가 그걸 무효화했다(§0-0).
**목록이 같은지만 보는 검사는 "축산"이 원본에서 왔는지 이름 추정인지 구별하지 못한다.** ①이 그래서 1순위다.

---

## 0. 지금 상태 — 왜 규율로는 안 되는지 (전부 실증)

### 0-00. 사고 번호표 — 이 문서의 척추 (critic 지적: 인용할 수 없는 근거였다)

*"여섯 번 실패했다"·"다섯 번째 사고"* 를 근거로 써왔는데 **번호↔사건 대응표가 리포 어디에도 없었다.**
`DEBT.md §1`의 판정표를 정본으로 삼고, 여기 대응만 싣는다.

| # | 사고 | 축 | 이 설계가 막는가 |
|---|---|---|---|
| 1 | 품목 첫 매치 · `MAIN_ITEMS` 뒤늦게 발견 | 값이 틀린다 | ○ (조건부 — §5-0 0-a 성공 시) |
| 2 | 지역 주소 파싱 · `SIGUNGU_CD` 미추출 | 값이 틀린다 | ○ (조건부 — 폐기선 §6 실패1) |
| 3 | 두 화면이 다른 말 (4회) | 값이 틀린다 | **△ 절반** — 알고리즘 복사는 막고, **엔드포인트·limit이 갈리는 종류는 못 막는다**(A27·A26) |
| 4 | DB 전량 삭제 | **값이 사라진다** | ✗ → **§1-4·§9-3으로 편입** |
| 5 | 저장 손실 경로 | **값이 사라진다** | ✗ (역행 — A20을 🟢로 닫았다) → **A23** |
| 6 | 회차당 값 1개 | 값이 틀린다 | ○ (A19 유지) |
| 7 | 스타터 잔재 | **값이 사라진다** | ✗ 전면 미언급 → **A28** |

**critic 판정의 핵심을 그대로 받는다: 이 설계는 "값이 서로 다르다"는 축만 봤고
"값이 없어진다"는 축(4·5·7)을 안 봤다.** §1-0의 *"적재가 자기 품질을 신고한다"* 가 그 방향의
유일한 문장이었는데 **신고 대상이 적재 한 곳뿐**이었다. 아래 전부에 그 축을 넣는다.

> app.yaml의 주석 두 줄이 사고 4의 1차 사료다:
> *"예전에 이 파일로 테이블을 전량 날린 적이 있는데 뚜껑이 덮여 있었다"* ·
> *"예전에는 `;` 로 이어져 있어 레이크 갱신이 실패해도 로더가 낡은 레이크로 7개 테이블을 재구축했다"*

### 0-0. critic 측정치 3건 — 이게 설계를 한 번 무효화했다

**① 품목 SSOT는 안 닫혔다. 서빙 DB의 62.3%가 여전히 이름 규칙산이다.**
라이브 레이크 전량 실측: `name_rule 101,043(62.3%) / main_item 59,437(36.6%) / none 1,842(1.1%)`.
원인은 분류기가 아니라 **레이크**다 — D8 수정 후 `reparse.py`가 `parquet_v2`(51,793건)에만 돌았고
라이브 `parquet`(162,122건)은 재파싱된 적이 없다. 원본 `MAIN_ITEMS` 충전율 68.2% 대 레이크 `main_item` 22.1%.

**② `SIGUNGU_CD`는 아직 추출조차 안 된다.** 원본 100% 충전인데 레이크에 컬럼이 없다.
`sido_code`는 100% 채워 저장해놓고 **아무도 안 읽는다** — `load_postgres.py:145`가 96% 충전인 주소 파싱 `a.sido`를 쓴다.
사고 2는 발견만 됐고 **닫히지 않았다.**

**③ 학교 식별자가 파싱된 문자열 두 개의 조합이고, 실패하면 조용히 사라진다.**
`clean_inst()`가 `None`을 리턴하면 `load_postgres.py:194-195`·`:436-438`·`:457-459`가
`continue` 한다 — **그 학교의 전 회차가 적재에서 빠진다.** 로그도 카운트도 없다.

**이 셋이 내 1차 설계를 무효화한 지점 (critic 지적 수용):**
> *"정의만 옮기는 설계(= `category.ts` 같은 파일 하나 더)는 1번을 못 막는다.
> 1번은 정의가 아니라 **적재분이 규칙산이라는 사실**의 문제다."*

맞다. §1-2의 어휘 생성은 "파이썬과 TS가 같은 목록을 본다"만 보장한다.
`name_rule`로 만든 "축산"도 목록 안의 값이라 §1-4의 어휘 대조가 **전부 통과한다.**
101,043건이 추정인데 검사가 하나도 안 울린다. **값의 출처를 안 보는 SSOT는 SSOT가 아니다.**
→ §1-0을 새로 세운다.

### 0-0b. 그리고 더 나쁜 것 — 지금은 고칠 경로 자체가 막혀 있다

①이 말하는 건 "레이크를 다시 파싱해야 한다"이고, ②는 `normalize.py`에 컬럼을 **추가**해야 한다는 뜻이다.
그런데 `crawlstate.py:32-35`의 `mark_done`이 bid_id를 영구 완료 처리해서
**`update_lake.py`로는 영원히 안 고쳐진다.**

**즉 `normalize.py`를 어떻게 고치든 라이브 레이크에 도달하지 못한다.**
내 1차 설계의 3단계(코드북 전환)는 **실행 불가능한 계획이었다.**
재파싱 경로 복구가 측정보다도 앞선 0단계다(§5-0).

### 0-1. 같은 알고리즘이 두 층에 복사돼 있고, 결과가 다르다 (여섯 번째 사고 · 라이브)

**발주 예보**. 서버와 웹이 같은 알고리즘을 각각 구현했다.

| | `app.module.ts:56-70` | `analysis-board.tsx:504-515` |
|---|---|---|
| 최소 회차 | `dates.length < 4` | `dates.length < 4` |
| 창 | `slice(-7)` | `slice(-7)` |
| 간격 필터 | `g > 5 && g < 90` | `g > 5 && g < 90` |
| 중앙값 | `gaps[Math.floor(len/2)]` | `gaps[Math.floor(len/2)]` |
| **기준 시각** | `new Date(); setHours(0,0,0,0)` = **로컬 자정** | `Date.now()` = **현재 시각** |

매직 넘버 네 개까지 똑같이 베껴졌다. 그리고 **기준 시각 하나가 다르다.**
오늘 화면의 `D-3`과 분석판의 `D-3`이 같은 학교에서 하루 어긋난다.
누가 봐도 "복사해온 코드"인데, **복사를 막는 장치가 없어서** 복사됐다.

### 0-2. 같은 개념의 정규식·매핑표가 세 계층에 흩어져 있다

| 규칙 | 파이프라인 | 서버 | 웹 |
|---|---|---|---|
| 시군구 형식 | `_SGG_RE` (load_postgres.py:116) | `SGG_RE` (app.module.ts:246) | — |
| 시군구 정제 | `clean_regions` · `canon_sgg` | `cleanSggs` | — |
| 시도 표기 | `_SIDO_MAP` 34줄 (normalize.py:17) | — | — |
| 주소 파싱 | `_split_address` | — | — |
| 기관명 절단 | `clean_inst` (괄호 절단) | **없음** — `${sigungu}\|${schoolName}` 로 조립 | **없음** — 4곳에서 조립 |
| 품목 어휘 | `_ATOM`·`_ADD_KEYS`·`_COMPOUND`·`CAT_KEYS` | — | `CATS`(→ shared 이관 완료) |

`_SGG_RE`와 `SGG_RE`는 **문자 하나까지 같은 정규식이 두 언어로 두 벌** 있는 것이다.
지금 우연히 같다. 한쪽만 고치면 아무도 모른다.

### 0-3. "무효"의 정의가 두 가지로 살아 있다 (다섯 번째 사고 · 라이브)

`내 성적` 화면 한 장 안에서 KPI 카드(`record/page.tsx:167` → `/firms/record` → `bidRate < floorRate`)와
아래 표의 무효 뱃지(`record/page.tsx:255` → `bidRate < winRate`)가 **자릿수가 다른 숫자**를 낸다.
실효 하한은 `하한율 × 예정가/기초가`라 명목 90 미만 투찰은 거의 없고, 낙찰가 미만 투찰은 흔하다.

판정 규칙 총 7곳: `app.module.ts` 223·443·531·693·896 + `record/page.tsx` + `analysis-board.tsx:50`.
**원본 `bids.valid`(normalize.py:84)가 답을 갖고 있는데 서빙이 그 컬럼을 안 실어서** 일곱 곳이 각자 복원했다.

### 0-4. 원본이 준 값을 서빙이 통째로 버린 목록

`normalize.py`가 레이크에 넣는데 서빙 DB에 한 글자도 안 들어간 것:
`bids.valid` · `is_qualification_review`(적격심사 — 최저가 모델이 안 맞는 회차가 분포에 섞인다) ·
`institution_code`(PURR_CD, 100%) · `areas[].sgg_cd` · `areas[].pdlc_nm` · `bid_at`(D6이 동가 판정에 필요하다 명시) ·
`rank` · `qualification_limit_item_cd`.

**`pdlc_nm`이 세 번째 실패의 유력 후보다.** `tests/test_normalize.py:73`:
```
{"CTPV_CD":"15","SGG_CD":"15020","SGG_NM":"김해시","PDLC_NM":"한우"}
```
~~원본의 자격 제한이 **지역 목록이 아니라 (지역 × 품목) 쌍**이면, 품목 실패와 지역 실패는
같은 필드를 두 번 버린 결과다.~~ **← 반증됨. 아래.**

#### 0-4b. 이 가설은 **반증됐다.** 그리고 반증 과정이 강제 수단 하나를 낳았다

data가 전수로 쟀다(162,922회차 · `areas` 288,559행):
- `PDLC_NM` 채움 100%, 고유값 252종. **값은 `시도/시군구` 형식**이다 — `서울/전체` 46,528 · `경남/창원시` 12,015
- **`한우` 같은 품목 값은 전수 0건.** `행수 = 지역수 = PDLC수`로 1:1 — 품목 축이 아니라 **지역 한 축**
- 결정적 대조: 같은 공고에서 `PDLC_NM=경기/전체` × `MAIN_ITEMS=가공식품` — **두 필드는 다른 축**

**픽스처가 가짜였다.** `tests/test_normalize.py:73`의 `"PDLC_NM":"한우"`는 **실데이터에 한 건도 없는
손으로 지어낸 값**이다. 그 거짓이 통과하는 테스트로 굳었고, 아키텍트(나)가 그걸 원본 증거로 인용했다.

> **강제 수단으로 승격한다 (§1-5).**
> **테스트 픽스처가 실데이터에서 오지 않으면 테스트가 거짓을 가르친다.**
> 팀이 지금 테스트 방침을 정하는 중인데, **"어디에 테스트를 붙일까"보다 "픽스처를 어디서 뽑을까"가 먼저다.**
> 규칙: **원본 형식을 주장하는 픽스처는 raw에서 뽑고 출처(파일·bid_id)를 주석으로 단다.** 손으로 짓지 않는다.

내가 *"측정 전에 인용하지 마라"* 고 못박은 덕에 피해가 0이었다.
**⚠ 표기 관행을 유지한다 — 경고를 다는 것이 가설을 안 세우는 것보다 낫다.**

**품목 자격 판정은 그대로다.** 업체 등록 품목은 원본에 **없다**(`ds_bidList` 31키·`QLFC_LMT_ITM_CD`·`PDLC_NM` 전수 확인).
`qualificationBasis: "region-only"` 표기가 정직한 대응이고, `user_biz.categories`가 유일한 경로다.
**대신 지역 코드북 후보가 바뀐다 — 그게 더 큰 소득이다(§2-b).**

### 0-5. 그 밖

- 로더가 두 벌: `tools/serve/` 474줄(08-28) vs `data-plane/` 105줄(08-27, S-1 전부 빠짐)
- DDL 두 벌: drizzle 스키마(자칭 "계약의 단일 진실") vs 실제 적용되는 손유지 `schema.sql`
- `@eatbid/shared` import 웹 전체 **4개**, 손으로 다시 선언한 응답 타입 **23개**
- "오늘"이 UTC 2종 + 로컬 1종 + 웹 1종(`today/page.tsx:78`) → 매일 09:00 이전 하루 어긋남

---

## 1. (c) 메커니즘 — 중복 선언이 불가능하게

### 1-0. SSOT의 단위는 "값"이 아니라 **(값, 출처)** 다

critic 지적의 구조적 답. 어휘를 아무리 잘 봉인해도 **그 값이 어디서 왔는지**를 안 들고 다니면
101,043건의 추정이 59,437건의 사실과 구별되지 않는다. 그래서 규칙을 바꾼다:

> **출처 없는 값은 계약 위반이다. 값만 담은 컬럼·필드·타입은 만들지 않는다.**

세 사고가 이 하나로 덮인다. critic이 요구한 형식 그대로:

| 사고 | SSOT는 무엇인가 | 지금 값이 그것과 다르다는 걸 **화면이 어떻게 아는가** |
|---|---|---|
| ① 품목 | 원본 `MAIN_ITEMS` / `ds_mainItemlist` | 응답 타입이 `Sourced<Category>`다. **출처를 안 만지면 렌더가 컴파일 안 된다.** 문구가 갈린다: `축산 (발주처 지정)` vs `축산 (공고명 추정)` vs `품목 미상` |
| ② 지역 | `SIDO_CD` · `SIGUNGU_CD` | 코드가 없는 행은 `sigungu: null`. **규칙으로 메우지 않는다** — 화면은 "지역 미상"으로 말한다 |
| ③ 학교 | `PURR_CD` | 키 생성 실패를 **카운트해서 적재 요약에 싣는다.** 조용한 `continue` 금지 |

공통 모양 두 가지다.
**(가) "모른다"를 표현할 수 있는 타입.** 지금은 모른다를 표현할 자리가 없어서 규칙으로 메웠다.
**(나) 적재가 자기 품질을 숫자로 신고한다.** 지금은 신고하지 않아서 62%를 아무도 몰랐다.

**타입으로 강제하는 법 (tsc, 비용 0):**
```ts
type Provenance = 'original' | 'rule' | 'none';
type Sourced<T> = { readonly unsafeValue: T; readonly src: Provenance };
```
**필드명은 `unsafeValue`다. `.v`가 아니다 (critic 지적).** `.v`는 두 글자라 가드 grep에서
`e.v`·`event.v`·`rev.v` 같은 오탐이 폭발한다. **한 글자 결정이 가드의 생사를 가른다** —
`unsafeValue`는 오탐이 0에 수렴하고, 이름 자체가 "그냥 쓰면 안 되는 값"이라고 말한다.
API 응답이 `category: Sourced<Category>` 면 JSX의 `{o.category}` 는 **객체라서 React가 못 렌더하고
컴파일이 깨진다.** 값을 쓰려면 `stated(o.category)` 를 거쳐야 하고, 그 헬퍼가 출처 문구를 함께 돌려준다.
새 인프라 0, 런타임 비용 0, 이미 도는 tsc가 잡는다.
(`.v` 직접 접근이라는 탈출구는 남는다 — `stated()` 밖의 `.v` 를 가드가 grep 한다. §1-5)

**커버리지 — 게이트가 아니라 적재 요약의 한 줄로 (critic 수정 수용).**
1차안은 *"직전보다 `original` 비율이 떨어지면 적재 실패"* 였다. **틀렸다.**
로더는 `app.yaml:187` 크론으로 **매일 무인 실행**(KST 07:00)되고, 일간 증분이 비율을 매일 흔든다.
노이즈로 실패 → `restartPolicy: OnFailure` 재시도 → 결국 사람이 끈다. **§6-2가 예언한 죽음이 그대로 온다.**

- `scripts/coverage-baseline.json`에 **지금 숫자(36.6%)를 커밋하는 것은 유지**한다. 나쁜 상태를 숫자로 박아두면
  화면이 "발주처 지정"이라고 거짓말할 수 없다.
- **빌드/적재를 깨는 건 이진 조건 하나**: `category_src is null`인 행이 존재하면 실패. 노이즈가 없다.
- 비율은 **적재 요약(load report)에 한 줄**로 남긴다. 추세는 사람이 본다.

> **어휘 대조(§1-4)는 값이 목록 안인지만 본다. 출처 예산은 값이 어디서 왔는지를 본다.
> 사고 ①을 잡는 건 후자뿐이다.**

### 1-0b. 표본이 적다는 사실은 **화면이 말한다. 로더는 지우지 않는다**

§1-0의 "(가) 모른다를 표현할 수 있는 타입"의 쌍둥이 원칙이다. 그리고 이건 이미 결재된 적이 있다 —
`by_floor`의 `len(wrs)<3` 폐기 로직을 걷어내며 로더 주석에 이렇게 썼다:

> *"표본이 적은 구간도 버리지 않는다 — n을 그대로 실어 보내고, 적다는 사실은 화면이 말한다.
> 작은 n을 숨기는 것과 다른 품목을 섞는 것은 같은 죄다"* (`load_postgres.py:230-232`)

**그 원칙을 쓴 파일이 그 바로 위에서 학교를 통째로 지운다**(`:217`).
구간을 지우는 건 죄라고 결재해놓고 **학교를 지우는 건 통과했다.** 더 큰 판본이다.

> **원칙(못박는다): "안정성을 위해 버리기"와 "n을 표기하기"는 같은 목적의 두 수단이고,
> 후자가 우리 헌법이다.** 전자는 사실을 감추고 후자는 사실을 말한다.
> **문턱으로 거른 것은 "없는 것"이 아니라 "우리가 안 보여주기로 한 것"이다.**

예외는 하나뿐이다: **파생값을 계산할 수 없어서 `null`을 내는 것**은 버리는 게 아니라 모른다고 말하는 것이다
(`:228`의 `len(pprs) >= 5 else None`이 그 예 — 행을 지우지 않고 그 값만 비운다). **행을 지우는 것과 값을 비우는 것은 다르다.**

**분모까지 오염됐다는 사실도 실어야 한다.** 62.3%가 추정이면 `by_cat_floor` 의 품목별 분모도
그만큼 추정 위에 서 있다. "축산 34회 기준"이 사실이 아니게 된다.
처방은 최소로 한다 — `catCounts` 옆에 **출처 혼합비 하나**(`srcMix: {original: 9, rule: 25}`)만 싣는다.
회차별 전개는 과설계다. 화면은 `축산 34회 (발주처 지정 9 · 추정 25)`까지 말할 수 있으면 된다.

### 1-1. 먼저 증명: 경계를 넘어야 하는 것은 **셋뿐**이다

"파이썬과 TS가 같은 지식을 갖는다"가 문제로 보이지만, 실제로 **공유해야 하는 지식은 거의 없다.**
값이 태어나는 층을 성격으로 못박으면 대부분의 알고리즘은 **한쪽에만 있어도 된다.**

| 지식 | 성격 | 사는 층 | 경계를 넘나? |
|---|---|---|---|
| 품목 분류 절차 | 회차 고정값 | 로더(py) | ❌ 서버·웹은 DB 컬럼을 읽는다 |
| 지역 파싱/정제 | 회차 고정값 | 로더(py) | ❌ 코드북 적용 후엔 절차 자체가 사라진다 |
| 유효/무효 | 회차 고정값 | **원본 → 로더가 싣기만** | ❌ |
| 적격심사 플래그 | 회차 고정값 | 로더(py) | ❌ |
| 학교/시장 집계 | 회차 고정값 | 로더(py) | ❌ |
| 자격 판정 | 사용자 의존 | 서버(ts) | ❌ 로더는 사용자를 모른다 |
| 판정 3분류·리허설 | 사용자 의존 | 서버(ts) | ❌ |
| 금액 파생(하한금액·실효하한) | 사용자 의존 | 서버(ts) | ❌ |
| 발주 예보 | 파생 | 서버(ts) | ❌ **웹이 베낀 것이 0-1** |
| 포맷·색·정렬·접기 | 표시 | 웹(ts) | ❌ |
| **① 품목 어휘 + 표기 변이표** | **데이터** | ? | ✅ **넘는다** |
| **② 시군구·시도 코드북** | **데이터** | ? | ✅ **넘는다** |
| **③ 학교키 형식** | **형식** | ? | ✅ **넘는다**(로더가 만들고 서버가 읽는다) |

**결론: 언어 경계를 넘는 건 알고리즘이 아니라 어휘 두 개와 형식 하나다.**
셋은 전부 **데이터**다. 데이터는 생성할 수 있다. 알고리즘 공유는 애초에 필요 없다.

이 표가 이 설계의 전부다. 나머지는 이걸 지키게 만드는 장치다.

### 1-2. 어휘 — **생성기를 만들지 않는다** (critic 결정 수용, 내 1차안 폐기)

내 1차안은 `vocab/*.ts` → `_contract.py` **생성 + 해시 봉인**이었다.
§6 실패3에서 내가 "못 골랐다"고 남긴 트레이드오프에 critic이 답했고, **그 답이 맞다.**

**폐기 근거 (critic 논증을 그대로 받는다):**
- 이 파이프가 실제로 나르는 건 `_ATOM` 25줄 + 표시 순서 6개다. **측정 후엔 더 줄어든다**(§0-0 재파싱 뒤 원본 통제 어휘가 답하는 비율이 오른다). 그걸 위해 생성기 40줄 + 빌드 훅 + 해시검증 5줄 + **새 실패 모드** + **커밋된 생성물이라는 지금 없는 이중성**을 산다.
- **§1-4의 postflight 어휘 대조 10줄이 같은 사고를 이미 막는다.** 어휘가 갈리면 → 로더가 목록 밖 값을 씀 → 적재 실패.
- 결정적으로: **갈렸는데 DB에 안 들어가는 불일치는 사용자에게 도달할 수 없다.** 생성기는 도달 불가능한 불일치를 막으려고 110줄을 낸다.
- 완화안(DB 어휘 테이블)은 §1-6이 이미 기각한 안("오타가 진실이 된다")으로 되돌아간다 — **설계가 원을 그렸다.**

**대신 이렇게 한다:**

| 무엇 | 어디 | 왜 |
|---|---|---|
| 품목 **목록**(6종·순서·타입) | `domain/category.ts` (TS) | 소비자가 웹·서버 둘. tsc가 지킨다 |
| 표기 **변이표**(`_ATOM`) | `load_postgres.py` (파이썬) | **분류 절차의 일부**다. 절차가 사는 층에 산다 |
| 정합 | **§1-4 postflight** | 갈리면 적재가 죽는다 |

> **목록과 변이표는 성격이 다르고, 성격이 다른 두 파일은 중복 선언이 아니다.**
> "농공산품 → 농산+공산"은 어휘가 아니라 **파서의 사전**이다. `CATEGORIES`는 계약이고 `_ATOM`은 구현이다.

**이 폐기가 §1-1의 결론을 바꾸지 않는다.** 경계를 넘는 셋 중 ①(어휘)은 **넘지 않아도 됐던 것**으로 판명났다.
남는 건 ②(코드북 — 데이터라 DB 테이블로 간다)와 ③(학교키 형식 — §1-3 브랜디드 타입).
**즉 언어 경계를 넘는 코드는 0이다.** 1차안보다 단순해졌다.

### 1-3. 알고리즘 — 층에 못박고, 어기면 컴파일이 안 된다

**브랜디드 타입** — 0-2의 학교키 조립을 문법 수준에서 불가능하게.

```ts
declare const brand: unique symbol;
export type SchoolId = string & { readonly [brand]: 'SchoolId' };
export function schoolId(sigungu: string, institution: string): SchoolId
export function parseSchoolId(id: SchoolId): { sigungu: string; institution: string }
```

`` `${sigungu}|${name}` `` 을 `SchoolId` 자리에 넣으면 **타입 에러**다.
웹 4곳(`delivery:185,269` · `record:211,261`)과 서버 1곳(`app.module.ts:144`)이 컴파일에서 죽는다.
리뷰도 문서도 필요 없다. `BizNo`·`BidId`·`BidNo`도 같은 처리(A4의 bidId/bidNo 혼용이 이걸로 잡힌다).

**이게 라이브 결함 하나를 바로 고친다**: `fetch_open.py:45`는 `institution`을 원본 그대로 저장하고
로더는 `clean_inst()`로 괄호를 자른다. 그래서 괄호 붙은 기관명 공고는 `OpenController.enrich`의
학교 조회가 조용히 빗나가 `band`·`recent3`·`usualN`이 전부 null이 된다. 화면엔 "자료 없음"으로 보인다.
`schoolId()` 하나를 거치게 하면 두 경로가 같은 절차를 탄다.

**응답 타입 강제** — 컨트롤러에 shared 반환 타입을 명시하고, 웹이 로컬 타입 23개를 버린다.
런타임 zod 검증은 **넣지 않는다**(630만 행 위 응답에 파싱 비용을 얹을 이유가 없다). tsc만으로 충분하다.

### 1-4. DB가 어휘를 배신하면 적재가 실패한다 (원본 변경도 여기서 잡힌다)

생성은 "파이썬과 TS가 같은 목록을 본다"를 보장한다. **DB에 그 목록 밖 값이 들어가는 건 못 막는다.**
그리고 **원본이 바뀌는 것**(eaT가 품목을 하나 추가하는 것)도 못 막는다. 이 둘을 같은 장치로 잡는다.

> ⚠ **한계 명시(critic 지적 수용)**: 이 검사는 **값이 목록 안인지만 본다.**
> `name_rule`로 만든 "축산"도 목록 안이라 101,043건이 전부 통과한다.
> **사고 ①은 여기서 안 잡힌다. §1-0의 출처 예산이 잡는다.** 두 장치는 대체재가 아니라 짝이다.

로더 적재 직후 자기검사. 어긋나면 비영 종료:
```sql
select distinct category from school_auctions            -- ⊆ 생성된 어휘
select count(*) from firm_bids where valid is null        -- 0
select count(*) from school_auctions where category_src is null  -- 0  (출처 없는 값 금지)
select category_src, count(*) from school_auctions group by 1     -- baseline 대비 퇴행 시 실패
```
**그리고 "0행 적재는 자기를 성공으로 신고한다" — 이게 §1-0의 거울이다 (critic A25).**
자기검사에 **행 수 하한**이 없었다. 지금 상태를 실측으로 정리한다:

- ✅ **로더 쪽은 봉인됐다**(커밋 `6d91978`). 목록을 못 읽으면 건너뛰고, 0건이면 기존을 유지한다.
- ❌ **수집기 쪽은 그대로다.** `fetch_open.py:61`이 여전히 `open(OUT,"w")` — **바이트를 쓰기 전에 기존 파일을 0바이트로 자른다.**
  임시파일도 `os.replace`도 없다. **같은 리포 `src/eatbid/archive.py:40-43`은 이미 temp+rename을 쓴다** — 올바른 패턴이 옆에 있는데 여기만 안 쓴다.
- ❌ 그리고 봉인이 **부분 실패를 안 막는다.** `fetch_open.py:29-34`는 지역별 예외를, `:55-56`은 공고별 예외를
  삼키고 **종료 코드 0**으로 끝난다. 17개 지역 중 16개가 실패해도 "3건짜리 유효한 JSON"이 나온다.
  → `oj`가 비어 있지 않으므로 로더의 새 방어를 **통과**하고 `delete` 후 3건을 넣는다.
  **크론의 `set -e`·`&&`도 못 막는다 — 실패가 아니라 성공으로 끝나기 때문이다.**

처방 둘, 둘 다 이미 있는 패턴이다:
1. **원자적 쓰기**: temp + `os.replace` (`archive.py`와 같게). 반쪽 파일이 존재할 수 없게 만든다.
2. **급감 방어**: 로더가 직전 건수 대비 **급감(예: 50% 미만)이면 `delete`를 건너뛰고 사실을 남긴다.**
   0건만 막는 건 절반이다 — 위험한 건 0이 아니라 **3**이다.

그리고 **적재 요약(load report)** 을 매 실행 남긴다. 지금 아무 데도 안 남는 것들:
출처 분포 · 키 생성 실패로 **건너뛴 회차 수**(0-0 ③) · 시군구 코드 결측 수 · 신규 어휘 값 · **직전 대비 건수 변화**.
**"안 되는 건 안 도는 거다"의 올바른 구현은 조용한 `continue`가 아니라 카운트된 실패다.**

그리고 **관측 어휘 보고서**: 저카디널리티 컬럼마다 `distinct` 집합을 뽑아 지난 실행과 diff.
새 값이 나타나면 경고하고 기록한다.

> 우리가 두 번 실패한 이유는 "원본을 안 봐서"였다. 세 번째는 **"원본이 바뀐 걸 몰라서"** 날 수 있다.
> 이 장치가 그 방향을 막는 유일한 것이다.

### 1-5. 게이트 — 실제로 도는 것에만 얹는다

**현실 확인: 이 리포에 테스트 0개, CI 0개, 루트 lint 설정 없음.** 실제로 도는 건 둘뿐이다.
- `apps/web/.husky/pre-commit` → `lint-staged`
- `apps/web/.husky/pre-push` → `bun run build`

**그래서 계약 테스트를 CI에 넣자는 안은 이 프로젝트에서 작동하지 않는다.** 없는 인프라 위엔 강제가 안 선다.

| 층 | 장치 | 붙는 곳 | 비용 |
|---|---|---|---|
| 어휘 | `gen:contract` 재생성 diff | `turbo build` | 40줄 |
| 어휘 | `_contract.py` 해시 검증 | 로더 시작 | 5줄 |
| 알고리즘(TS) | 브랜디드 타입 | tsc (이미 돎) | **0** |
| 알고리즘(TS) | shared 응답 타입 | tsc (이미 돎) | **0** |
| 층 위반 | `ssot-guard.mjs` grep | `turbo build` | 60줄 |
| DB | postflight 어휘 대조 | 로더 종료 | 10줄 |

`ssot-guard.mjs`가 잡는 것 — **10개에서 4개로 줄인다 (critic 수정 수용).**

1차안의 `864e5`·`Math.floor(*.length / 2)`는 **알고리즘이 아니라 대리 지표**다.
서버가 `app.module.ts:63·159·397·461`에서 **정당하게** 쓴다. 웹에만 금지해도 정당한 용례가 바로 걸리고,
**첫날 allowlist가 열린다.** §6-2가 예언한 죽음이 그날 온다. 대리 지표로 가드를 만들면 가드가 먼저 죽는다.

**남기는 것 — 오탐이 0에 가깝고, 각각이 실제 사고에 대응하는 것만:**

| 검사 대상 | 잡는 사고 | 오탐 |
|---|---|---|
| `apps/web/**` 의 `.catch(() => {})` / `.catch(()=>{})` | **사고 5 축.** 지금 제품 최대의 조용한 실패원 **24곳** | 거의 없음 |
| `apps/web/**` 의 `` `${…}|${…}` `` 템플릿 | 사고 3 — 학교키 조립 (A3) | 없음 |
| `stated()` 밖의 `.unsafeValue` 접근 | 사고 1 축 — 출처를 건너뛴 렌더 (§1-0) | 없음(이름이 고유) |
| `data-plane/load_postgres.py` 존재 | 로더 두 벌 | 없음 |

**뺀 것과 그 대안:**
`864e5`·`Math.floor(len/2)` → 대안 없음. **§8-5에서 자백한 대로 0-1은 구조로 못 막는다.**
`toISOString().slice(0,10)` → `todayKST()` 도입 후 **남은 호출부가 0이 되면** 그때 넣는다(그때는 오탐이 0이다).
hex 색 → oxlint 규칙 쪽이 맞다. `LEDGER-CHECKED` → 사람 리뷰로 남긴다(§10).
`schema.sql` ≠ snapshot → §9로 이동(그 파일 자체가 사라진다).
`continue` 앞 카운터 → 정적 검사로 못 본다. **적재 요약의 숫자로 본다**(§1-4).

**allowlist는 `scripts/ssot-allow.json` 하나. 항목마다 `until` 날짜 필수. 연장은 1회, `# why:` 필수.**
근거: 1인 개발에서 allowlist 추가는 언제나 수정보다 싸다. 만료가 없으면 가드는 3개월 안에 죽는다(§6-2).

### 1-6. 기각한 대안 (왜 안 하는지)

| 대안 | 기각 이유 |
|---|---|
| 중립 JSON/YAML → TS·py 양쪽 생성 | 층이 하나 는다. JSON엔 타입·파생·주석이 없어 `category.ts`가 지금 하는 일을 못 한다. 이득 없음 |
| 파이썬이 원본, TS 생성 | 게이트가 파이썬 쪽엔 없다. "빌드가 깨진다"가 성립 안 함 |
| zod에서 DDL 생성 | drizzle이 이미 그 자리에 있다. 두 스키마 소스를 세 개로 늘리는 짓 |
| 응답 런타임 zod 검증 | 630만 행 위 비용. tsc로 충분 |
| 계약 테스트 + CI 구축 | CI가 없다. 인프라부터 만들면 이번 주에 아무것도 안 끝난다 |
| 파이프라인을 TS로 재작성 | 과설계. duckdb·pyarrow 자산을 버리는 값이 이득보다 크다 |
| 어휘를 DB에서 읽어 타입 생성 | 오타가 진실이 된다. 어휘는 선언이어야 한다 |

---

## 2. (a) 정규식·매핑표 처분표

**원칙: 원본에 권위 있는 값이 있으면 규칙은 죽는다. 없으면 한 곳에 모은다.**
사용자 원칙 "정규화는 최대한 걷어내라. 안 되는 건 안 도는 거다"의 직역이다.

| 규칙 | 지금 위치 | 원본 대체재 | 처분 | 조건 |
|---|---|---|---|---|
| `_SIDO_MAP` (34줄) | normalize.py:17 | `SIDO_CD` | **삭제** | 코드북 일관성 측정 |
| `_split_address` | normalize.py:91 | `SIDO_CD`·`SIGUNGU_CD` | **삭제** | 〃 |
| `_SGG_CANON` (마산→창원) | load_postgres.py:115 | 코드북 | **삭제** | 〃 — 단 실패1 참조 |
| `_SGG_RE` | load_postgres.py:116 | 코드북(형식검사 불필요) | **삭제** | 〃 |
| `SGG_RE` (서버) | app.module.ts:246 | 〃 | **삭제** | 〃 |
| `cleanSggs` | app.module.ts:248 | 〃 | **삭제** | 〃 |
| `clean_regions` | load_postgres.py:381 | 〃 | **삭제** | 〃 |
| `clean_inst` (괄호 절단) | load_postgres.py:120 | `PURR_CD` | **삭제**(코드가 키가 되면 이름 정제 불필요) | PURR_CD 유일성 측정 |
| `CAT_KEYS` layer2 이름 규칙 | load_postgres.py:79 | `MAIN_ITEMS` 통제 어휘 | **축소** — 1층이 답하는 회차에선 안 돈다. 남는 건 `categorySrc='name_rule'`로 표시 | 이미 3층 구조로 반쯤 됨 |
| `_ATOM` 표기 변이표 | load_postgres.py:32 | 없음(표기 변이는 원본이 안 풀어준다) | **생존 → TS로 이관·생성** | — |
| `UNRESTRICTED_TOKENS` | app.module.ts:124 | `QLFC_LMT_YN`? | **측정 후 판단** | §5-0 |
| `eligibleFor` 부분문자열 매칭 | app.module.ts:136 | `sgg_cd` 코드 대조 | **삭제** — 코드 대조로 교체 | areas 코드 적재 |
| `region-coords.ts` 137좌표 | 웹 | 없음 | **생존**(시각화 전용, 값 아님) | — |

### 2-b. 코드북 — **후보 교체는 철회한다. 미해결로 남는다**

앞 개정에서 나는 *"코드북 후보가 `sgg_nm`에서 `PDLC_NM`으로 바뀐다"* 고 썼다. **취소한다.**
data가 자기 제안을 스스로 반증했다.

**`PDLC_NM`은 참가 자격 지역 목록이지 공고 소재지가 아니다.**
결정적 증거: `전남광주/전체` 표기가 붙은 공고 **8건**의 실제 소재지가 **전북 군산시 4건 · 경기 4건**이었다.
다른 지역 공고가 광주·전남 업체에게 자격을 열어준 표기다. **"자격 지역 = 소재지"라는 가정이 틀렸다.**
(그리고 `27곳`이라던 앞선 수치도 재측정에서 **8건**이었다 — §6 실패4의 사례다.)

| | 축 | 쓸 수 있는 곳 |
|---|---|---|
| `PDLC_NM` | **자격 지역**(누가 응찰할 수 있나) | A11 자격 판정. **소재지 축에 쓰면 안 된다** |
| `SIDO_CD` + `SIGUNGU_CD` | **소재지**(어디 공고인가) | A2 지역 축. 둘 다 100% 채움 |

**따라서 A2는 원래 자리로 돌아간다: `SIDO_CD` + `SIGUNGU_CD`.**
그리고 **`SIGUNGU_CD` → 이름 코드북은 여전히 미해결 과제다** — 코드는 100% 있는데 이름표가 없다.
`_cb2.py`가 재던 것이 바로 그 이름표의 일관성이고, **§6 실패1의 폐기선(일관성 99%)은 그 측정에 그대로 걸려 있다.**

> **두 번 연속으로 "원본에 답이 있다"는 가설이 틀렸다**(PDLC_NM 품목 → PDLC_NM 소재지).
> 두 번 다 ⚠를 달아둔 덕에 피해가 0이었다. **가설을 세우는 것은 값싸고, 표시 없이 인용하는 것이 비싸다.**

**살아남는 규칙은 둘뿐이다: `_ATOM`(표기 변이)과 `region-coords`(좌표).**
전자는 TS에서 선언하고 파이썬으로 생성한다(§1-2). 후자는 값이 아니라 그림이라 웹에 남는다.
나머지 열한 개는 **원본 코드로 대체돼 사라진다.** 지우면 잃는 게 0이어야 한다 — 아니면 §5-3에서 멈춘다.

---

## 3. (b) 프론트·서버로 안 흩기

### 3-1. 지금 웹이 스스로 만들고 있는 것 (직접 조사분 — front 좌석 결과 오면 합친다)

| 웹이 만드는 것 | 위치 | 서버에도 있나 | 처분 |
|---|---|---|---|
| **발주 예보 전체** | `analysis-board.tsx:504-515` | ✅ `app.module.ts:56-70` | **삭제**, 서버 응답 사용 (0-1) |
| 판정 3분류 | `record/page.tsx:69,131,205,255` · `analysis-board.tsx:50` | ✅ 5곳 | **삭제**, `verdict` 1함수 |
| 하한금액 범위 | `analysis-board.tsx:829,947` | ✅ `anchorAmount`·`effFloor` | **삭제** |
| 낙찰금액 합계 | `record/page.tsx:74` · `wins/page.tsx:67` | ✅ `winSum`(SQL) | **삭제** |
| 중앙값 | `record:73` · `analysis:510,870` · `today:344` · `delivery:93` | ✅ 서버 다수 | **서버로** |
| "어제" 계산 | `today/page.tsx:78` | ✅ 서버 cutoff | **삭제**, `todayKST()` |
| 상위 지역 집계 | `today/page.tsx:85-88` | ✅ `/wins/recent` 재료 | 서버 집계로 |
| 하한 탭 정렬(빈도순) | `analysis-board.tsx:263` · `auction-detail.tsx:50` | ❌ | 서버가 순서를 실어 보낸다 |
| `LENSES`·`PERIODS`·`TABS`·`STATUS` | 각 화면 | ❌ | **웹에 남긴다** — 화면 구조지 값이 아니다 |
| 색·포맷·접기 | `chart-colors.ts`·`format.ts` | ❌ | **웹에 남긴다** |

**판정선**: 서버가 이미 답을 갖고 있거나 가질 수 있으면 웹은 만들지 않는다.
웹이 만들어도 되는 건 **화면 구조와 보임**뿐이다.

### 3-2. 의제 2(토스식 선언적 컴포넌트)와의 관계 — 충돌 아니라 전제

선언적 컴포넌트의 전제가 **"컴포넌트가 값을 만들지 않는 것"**이다.
지금 `record/page.tsx`가 선언적으로 안 써지는 이유는 스타일이 아니라 **69~75행에서 값을 만들기 때문**이다.
§3-1을 끝내면 화면은 서버가 준 사실을 배치만 하게 되고, 그때 선언적 문법이 **자연스럽게** 나온다.

**순서: SSOT 정리 → 선언적 재작성.** 반대로 하면 로직을 품은 채로 예쁘게 감싸는 것이 되고,
중복은 컴포넌트 안으로 숨어 더 안 보이게 된다.

### 3-3. 의제 3(NestJS 철학 분리)과의 관계 — 중복 7곳은 서비스 계층 부재의 증상이다

`app.module.ts` 954줄에 전 컨트롤러가 인라인이다. 서비스가 없다.
그래서 `/results`·`/firms/record`·`/firms/bids`·`/rounds`·`/share`가 판정을 **각자 인라인**했다.
**중복은 게으름이 아니라 구조의 결과다.** 부를 곳이 없으면 복사한다.

**처방은 서비스 4개가 아니라 순수 함수 3개다 (critic 수정 수용).**
1차안에 `VerdictService`·`EligibilityService`·`MoneyService`·`CalendarService`를 적었는데 **과했다.**
`EligibilityService`는 **호출 지점이 `app.module.ts:189` 하나뿐**이다. 한 곳에서 부르는 걸 서비스로 만드는 건
의제 3을 명분으로 층을 하나 더 쌓는 것이다.

```
domain/verdict.ts   ← 판정 3분류 (0-3의 7곳이 여기 하나로)   ★ 순수
domain/money.ts     ← 하한금액·실효하한 (4곳)                ★ 순수
domain/time.ts      ← todayKST (4곳)                        ★ 순수
```
셋 다 **순수 함수라 테스트가 붙고, 서버·웹이 같은 것을 쓴다.** 자격 판정은 지금 자리에 둔다.
발주 예보는 서버 1곳으로 모으되(0-1) 서비스 클래스가 필요 없다.

**부채의 이유도 파일 크기가 아니다.** 954줄이라서가 아니라 —
**판정 3분류가 이 파일 안에서만 5번(`:223·443·531·693·896`) 다시 쓰였고
`:531`과 `:443`이 같은 화면 KPI에 다른 기준을 쓴다**는 것이 부채다.
**모듈 분할은 그 자체로 사고를 못 막는다.** 순수 함수 3개가 막는다.
의제 3의 값은 §8-5의 Swagger 생성이 성립하게 만드는 것 쪽에 더 크다.

---

## 4. 자산 대장 (압축)

**자산 = 두 곳 이상에서 쓰이는데 어긋나면 화면이 거짓말하는 것.** 표시만 바뀌는 건 자산이 아니다.

| # | 자산 | 원천이어야 할 곳 | 계약 | 지금 |
|---|---|---|---|---|
| A1 | 품목 | 로더(원본 1층) | `vocab/category.ts` → 생성 | 🟡 어휘가 파이썬에만 |
| A2 | 행정구역 | 코드북 | `vocab/region.ts` → 생성 | 🔴 규칙 6곳 |
| A3 | 기관 정체성 | `PURR_CD` | `SchoolId` 브랜디드 | 🔴 3층 조립 + 라이브 결함 |
| A4 | 회차 정체성 | `bid_id`/`bid_no` | 분리 타입 | 🟡 혼용 |
| A5 | 사업자 정체성 | `BIZ_NO` | `normalizeBizNo()` | 🟡 정규화 3곳 |
| A6 | 투찰 유효성 | **원본 `bids.valid`** | `verdict.ts` | 🔴 **버려짐** |
| A7 | 판정 3분류 | 서버 `VerdictService` | 〃 | 🔴 **7곳·2종·라이브 불일치** |
| A8 | 낙찰 방식(적격심사) | 로더 | `SchoolAuctionRow` | 🔴 **아무 데도 없음** |
| A9 | 금액 파생 | 서버 `MoneyService` | `money.ts` | 🟡 4곳 |
| A10 | 표본 n·분모 | 서버, 응답 동봉 | `Basis` 유니온 | 🟡 값은 실려 있고 타입이 없다 |
| A11 | 자격 판정 | 서버 `EligibilityService` | `eligibility.ts` | 🟡 1곳이나 부분문자열 매칭 |
| A12 | 카피 canon | COPY-GUIDE | — | 🟡 강제 불가(§1-5 밖) |
| A13 | 색·토큰 | `eatbid.css`(+chart 예외) | DESIGN | 🟢 정리됨 |
| A14 | "오늘" | 서버 `todayKST()` | `time.ts` | 🔴 4중화 |
| A15 | 응답 타입 | shared | — | 🔴 웹 로컬 23개 |
| A16 | DDL | drizzle → schema.sql 생성 | — | 🔴 두 벌 |
| A17 | 로더 | 한 벌 | — | 🔴 두 벌 |
| A18 | 화면 키 | `screens.ts` | `Screen` 유니온 | 🟡 리터럴 |
| A19 | 내가 쓴 값 vs 실제 투찰 | **둘 다 진실. 다른 사실** | — | 🔴 같은 이름(PLANNING-LOG:348 · 0건 일치) |
| A20 | 사용자 상태 저장 | `session.ts` | — | 🔴 **🟢였던 판정을 철회한다 → A23** |
| **A21** | **출처(provenance)와 커버리지** | 로더가 매 값에 붙인다 | `Sourced<T>` + baseline | 🔴 **컬럼만 있고 계약·게이트 없음. 62.3%가 추정인데 아무도 몰랐다** |
| **A22** | **레이크 재파싱 가능성** | raw XML → 언제든 재구축 | — | 🔴 **`mark_done`이 막는다. 이게 없으면 A1·A2 수정이 도달 못 한다** |

| **A23** | **저장의 성공 여부** | 서버 응답 | — | 🔴 **A20 🟢 철회.** `session.ts:97-105`의 `put()`이 `res.ok`를 안 보고, `app.module.ts:792-794`가 **인증 실패를 HTTP 200**으로 돌린다. **두 겹이라 `res.ok`를 봐도 못 잡는다.** 로컬 쓰기는 이미 끝나 이 브라우저엔 값이 보이고 다른 기기엔 없다. 화면은 "✓ 저장됨" |
| **A24** | **배포 계약**(무엇이 무엇을 붙잡고 있나) | k8s 매니페스트 | — | 🔴 스타터 라우트가 readiness 프로브에 물려 있다(critic §2 A-5). **청소가 배포를 깬다** |
| **A25** | **수집 산출물의 원자성** | `open_auctions.json` | — | 🔴 로더 쪽은 봉인됐고 **수집기 쪽은 그대로**. 부분 실패가 정상 종료로 나온다 (§1-4) |
| **A26** | **`limit`이 곧 분모** | 서버 | — | 🔴 `firms/bids`를 세 화면이 300/2000/5000으로 부른다. **같은 질문에 다른 표본** |
| **A27** | **엔드포인트 선택** | — | — | 🔴 같은 학교 이력을 `/schools/:id/auctions`와 `/rounds/school/:id` **두 곳**에서 가져온다(`page.tsx:19`·`auction-detail.tsx:76`). 웹이 값을 *만드는* 게 아니라 **다른 질문을 하는** 것이라 §3-1 판정선도 가드도 못 본다 |
| **A28** | **스타터 잔재** | — | — | 🔴 사고 7. 설계에 한 줄도 없었다 |
| **A29** | **공고↔학교 관계(1:N)** | 원본 `ds_SelectUnionPurceTgtListR` (보유율 100%, 최대 18교) | `SchoolAuctionRow` | 🔴 **스키마가 "공고당 학교 1개"를 전제한다.** 공동구매 5,966건이 그래서 빠진다 |

리더 후보에 없었으나 자산인 것: **A6·A7·A8·A10·A14·A18·A19·A21·A22·A23~A29.**
**A21·A22는 critic 1차 측정에서, A23~A28은 DEBT.md에서, A29는 data 공동구매 조사에서 나왔다.**
A22는 자산이라기보다 **다른 모든 자산의 전제**다 — 원본을 다시 읽을 수 없으면 이 설계 전체가 종이다.

**A27이 이 설계의 구조적 사각지대다 (critic 지적, 수용).**
§3-1의 판정선은 *"서버가 답을 갖고 있으면 웹은 만들지 않는다"* 인데,
A26·A27은 웹이 **만드는** 게 아니라 **다르게 묻는** 것이다. 판정선도 가드 4개도 이 축을 못 본다.
**막을 방법: 도메인당 쿼리 선언을 한 파일로 모으는 것**(§8-4의 `lib/queries/*`)이 유일하다 —
같은 질문의 두 번째 버전을 만들려면 **이미 있는 선언 옆에 써야 하므로 눈에 띈다.**
타입도 가드도 아니고 **배치가 막는다.** 약한 장치라는 걸 인정한다(§10).

| **A30** | **표본 문턱으로 버리는 지점** | 없어야 한다 | — | 🔴 **7곳. 아래 전수** |

**A30 — 리더가 "세 번째가 있을 수 있다"고 했다. 전수로 찾았더니 일곱이다.**

| 위치 | 문턱 | 버리는 것 | 규모 | 판정 |
|---|---|---|---|---|
| `load_postgres.py:217` | `len(opened) < 5` | **학교 전체** | **1,877곳 · 4,543회차** (확정) | 🔴 **행 삭제** |
| `:341` | `len(opened) < 10` | 시장 지도의 시군구×품목 조합 | 미측정 | 🔴 행 삭제 |
| `:411` | `having count(*)>=10` | **`firms` 테이블의 업체** | 미측정 | 🔴 **행 삭제 · 아래 참조** |
| `:451`·`:472` | `having count(*) >= 3` | 로스터의 참여 업체 | 미측정 | 🔴 행 삭제 |
| `app.module.ts:266` | `n >= 20` | **지역 목록의 시군구** | 미측정 | 🔴 행 삭제. 주석이 *"밀양시 21회·의령군 52회도 골라볼 수 있어야 한다"* 고 써놓고 20 미만을 자른다 |
| `:57` | `dates.length < 4` | 발주 예보 행 | — | 🟡 파생 불가 = 모른다. **정당** |
| `load_postgres.py:228` | `len(pprs) >= 5` | `rsd` 값만 `None` | — | 🟢 **행을 안 지운다. 올바른 형태** |

**`:411`이 온보딩에서 지금 거짓말을 하고 있다.** `firms`는 투찰 10회 이상 업체만 담는데
`firm_bids`는 **전량**을 담는다. 그래서 9회 투찰한 사장이 온보딩에서 사업자번호를 넣으면
`/firms/lookup`이 `firms`를 보고 **`{found: false}`** → 화면은 **"기록 없음"**.
**그 사장의 투찰 9건은 `firm_bids`에 그대로 있다.** 온보딩을 통과하기만 하면 성적표에 뜬다.
**첫 화면이 "당신 기록이 없다"고 말하고, 다음 화면이 9건을 보여준다** — 사고 3의 새 사례이자
"값이 조용히 사라진다"(사고 5) 축이다. 미측정이지만 **경로는 코드로 확정**이다.

**A29 — "공고당 학교 1개" 전제 (data 발견).**
`school_auctions`에서 빠진 10,770건 중 **공동구매가 5,966건(55.4%)**이고,
원본 `ds_SelectUnionPurceTgtListR`가 **대상교를 빠짐없이 준다(최대 18교)**. 우리가 안 읽을 뿐이다.
**나머지는 확정됐다: `len(opened) < 5` 문턱이 4,419건(누락의 41%), 시군구 빈칸이 6,067건(대부분 공동구매).
합쳐서 10,770건의 97.4%가 설명된다.**
탈락 학교의 회차 분포가 이 문턱의 성격을 말한다 — 1회 464 · 2회 1,218 · 3회 1,065 · **4회 1,796**.
**4회짜리가 가장 많다. 한 번만 더 열렸으면 통과했을 학교들이다.**
그리고 이 4,419건은 `firm_bids`에 **있다** → **"내 성적"엔 뜨고 학교 화면엔 없다.**
**맞다면 그건 "데이터를 임의로 버리기"이고 사용자 원칙과 정면으로 부딪힌다** —
표본이 적다는 사실은 화면이 말할 일이지 로더가 지울 일이 아니다(`floor_stats`의 P5 주석이 이미 그렇게 적혀 있다).
**§0-4의 정확한 반복이다: 원본이 주는데 우리가 안 읽고, 대신 규칙(문턱)으로 잘랐다.**
이 중 A6·A8·A14는 **원본이 답을 갖고 있는데 안 실은 것** — 두 번의 실패와 같은 모양이다.

---

## 5. 마이그레이션 순서

원칙 셋. ① 거짓말 제거가 리팩터링보다 앞선다. ② 컬럼 추가만, 삭제·개명은 마지막.
③ **URL 영구 호환** — `school_id="김해시|임호초등학교"`가 분석판 URL에 있다. 절대 안 깬다.

### 5-0. 0단계 — **재파싱 경로 복구** (측정보다 먼저. 1차 설계의 오류 정정)

내 1차 계획은 "측정 → 코드북 전환"이었다. **그건 실행 불가능한 계획이었다.**
`crawlstate.mark_done`(crawlstate.py:32-35)이 bid_id를 영구 완료 처리해서
`normalize.py`를 어떻게 고쳐도 라이브 레이크(162,122건)에 도달하지 못한다(0-0b).
`_ATOM`을 TS로 옮기고 `SIGUNGU_CD`를 추출해도 **적재분은 그대로 62.3% 추정**이다.

| 순서 | 할 일 |
|---|---|
| 0-a | **raw XML → 레이크 전량 재파싱 경로**를 만든다. `crawlstate`를 우회하는 경로(재파싱은 수집이 아니다 — 완료 표시와 무관해야 한다). `parquet_v2`에만 돌았던 `reparse.py`를 라이브 대상으로 |
| 0-b | 재파싱 **비용 측정**: 162,122건 몇 시간인가. 이 숫자가 이후 전 단계의 리듬을 정한다 |
| 0-c | 재파싱 전/후 **출처 분포 대조**. `main_item` 22.1% → 원본 68.2%에 얼마나 근접하는가 |
| 0-d | 근접하지 않으면 **거기서 멈추고 원인을 찾는다.** 원본 68.2%인데 레이크가 못 따라가면 정규화가 아직 뭔가 버리고 있다 |

**0-a가 안 되면 이 문서의 §2(규칙 열한 개 삭제)와 §5-3은 전부 폐기다.**
그때 남는 건 서빙 층에서만 할 수 있는 것들(§5-1·5-2)뿐이고, 품목·지역 SSOT는 못 닫는다.
이건 축소가 아니라 **사실 신고**다 — 못 하는 걸 계획에 적어두면 그게 다음 실패가 된다.

### 5-0b. 선행 측정 (구현 0줄 · data 좌석)

> ⚠ **전제 (실패4): 백필 체인이 도는 동안 이 표의 수치는 전부 잠정이다.**
> 같은 질의가 실행마다 다른 답을 준다(실측: 같은 4개 id가 3건 → 1건).
> **비율이 판정선을 넘나드는 항목은 백필 정지 후 재측정해야 확정된다.**
> 각 항목에 **재실행 대조**(같은 질의 2회, 답이 같은가)를 붙인다 — 다르면 그 수치로 결정하지 않는다.
> 반면 **코드를 읽어 확정되는 것**(A30 문턱 7곳·A23 두 겹·A25 부분 실패)은 지금 확정이다.

| 측정 | 좌우하는 것 |
|---|---|
| `ds_areaList.PDLC_NM` 채움률·고유값 | **A11 전체.** 비면 0-4 후반 폐기 |
| `(SIDO_CD,SIGUNGU_CD)→이름` 다수결 일관성 | **A2·§2 전체.** `_cb2.py`가 이미 측정 중 |
| `PURR_CD` 유일성·기관명 변경 이력 | **A3 전체** |
| `bids.valid` vs `bid_rate < win_rate` 일치율 | **A6 — 어느 정의가 사실인가** |
| `is_qualification_review` 비율 | A8 우선순위 |
| `QLFC_LMT_YN` ↔ `UNRESTRICTED_TOKENS` 대응 | A11 |
| **`clean_inst()`가 `None`을 내 건너뛴 회차 수** | A3·A21. **지금 조용히 사라지는 양** |
| **출처 분포 baseline 확정** (재파싱 후) | A21 커버리지 예산의 기준선 |

> ⚠ 이 여섯 개가 다 "안 된다"로 나오면 §5-3 이후의 절반이 무의미해진다. **측정 전에 코드를 쓰지 않는다.**

### 5-1. 1단계 — 거짓말 멈추기 (사용자 데이터·URL·키 형식 안 건드림)

1. `firm_bids.valid` 추가(ALTER) → 로더가 레이크 `bids.valid`를 그대로 싣는다
2. `VerdictService` + `domain/verdict.ts` → **7곳을 하나로**. 정의는 5-0 결과로 고른다
3. `record/page.tsx` KPI와 표가 같은 함수를 본다 → **0-3 종결**
4. `CalendarService.todayKST()` → "오늘" 4곳 통일
5. `analysis-board.tsx:504-515` 발주 예보 삭제, 서버 응답 사용 → **0-1 종결**
6. `data-plane/load_postgres.py` 삭제, `tools/serve/load_postgres.py`를 서비스 리포로 이관
7. **표본 문턱 제거 (A30)** — 행을 지우는 다섯 곳을 걷어낸다. **이건 측정을 안 기다려도 된다** —
   "버리지 않고 n을 싣는다"는 이미 결재된 원칙(§1-0b)이고, 걷어내면 **늘어날 뿐 줄지 않는다.**
   - `load_postgres.py:217`(학교 1,877곳·4,543회차) · `:341` · `:411` · `:451`·`:472` · `app.module.ts:266`
   - **`:411`이 가장 급하다** — 온보딩이 "기록 없음"이라 말하는데 성적표엔 뜬다(A30)
   - 걷어낸 뒤 화면은 `n`을 표기한다. **표본이 적다는 사실은 화면이 말한다**
8. **`fetch_open.py` 원자적 쓰기 + 급감 방어** (§1-4 · A25). 수집기 쪽은 아직 안 봉인됐다

### 5-2. 2단계 — 메커니즘 세우기 (값 변경 0, 컴파일·빌드만)

7. `vocab/` 신설 + `gen:contract` + `_contract.py` 해시 봉인 (§1-2)
7b. **`Sourced<T>` 도입** — `categorySrc` NOT NULL, 응답 타입 `Sourced<Category>`,
    `stated()` 헬퍼로 문구 분기(`발주처 지정` / `공고명 추정` / `미상`). **화면이 62%를 말하게 된다** (§1-0)
7c. **커버리지 baseline 커밋 + 로더 출처 예산 게이트 + 적재 요약**(건너뛴 회차 수 포함) (§1-4)
8. `domain/ids.ts` 브랜디드 타입. **키 형식은 그대로** `sigungu|institution` — 조립 지점만 함수로
   → `fetch_open`과 `clean_inst` 불일치가 여기서 닫힌다
9. 웹 로컬 타입 23개 → shared 응답 타입, 컨트롤러 반환 타입 명시
9b. **`lib/queries/*` 신설 + `react-query` 전환** — 9와 **같은 작업이다**(타입이 붙는 자리가 쿼리 선언이다).
    fetch 55곳 → queryKey·fetcher·타입 한 곳. `/api/me`만 `safeParse`, 나머지 런타임 파싱 없음 (§8-4)
9c. `mergeAccountData()` 순수 함수 분리 — 병합 사고의 처방 (§8-2). **Zustand는 넣지 않는다** (§8-3)
9d. `API-LEDGER.md` 생성기 — 웹이 "서버가 이미 주고 있나"를 볼 곳 (§8-5). 9b 이후에만 가능
10. `ssot-guard.mjs` + 만료 있는 allowlist → `turbo build`에 물림
11. **drizzle 베이스라인 B1~B6** (§9-2). **B1~B5는 DB에 안 쓴다** — B2 diff가 이 단계의 산출물이다.
    `db:push` 삭제 · 파괴 가드 · 보호 목록 · `pg_dump` 선행 (§9-3). **`schema.sql` 폐기**
11b. `createSelectSchema` 파생 + `.extend()` 조립으로 응답 계약 재작성 (§9-4). 9·9b와 같은 작업이다
12. 로더 preflight(컬럼 존재) + postflight(어휘 대조·관측 diff) (§1-4). **파이썬 DDL 경로 폐쇄** (§9-5)

### 5-3. 3단계 — 원본 코드로 갈아타기 (5-0 통과 시에만)

13. `FIELD-LEDGER.md` 생성기 + 최초 대장 커밋 (원본 키별 채움률·고유값 + **lake/DB/화면 소비 여부**)
14. 코드북 적재. `schools.sido_cd`·`sigungu_cd`·`purr_cd` **컬럼 추가**. `schools.id`는 **그대로** — URL이 산다
15. 서버 조회를 "코드 우선, 없으면 id". `user_region.sigungu`(사용자 데이터)는 **읽을 때 매핑**, 행은 안 고친다
16. §2의 규칙 열한 개 삭제 (복원 6,479건 확인, 손실 0 확인 후)
17. `open_auctions.allowed_areas jsonb` = `[{sgg_cd, pdlc_nm}]` 원본 그대로 → `eligibleFor` 코드 대조로 교체

### 5-4. 4단계 — 남은 자산

18. `is_qualification_review` 추가 → 분포·리허설에서 **분리 표기**(제외가 아니라 표기. 헌법: 사실을 말한다)
19. `firm_bids.bid_at`·`rank` 추가 → `/firms/ties`가 동가 승패를 말할 수 있게 된다
20. A19: `미리 정해둔 값` / `실제 투찰값` — **합치지 않는다. 다른 사실이다**
21. `screens.ts` · `normalizeBizNo()` · `MoneyService`

### 5-4b. 사장 승인 항목 — **6행짜리 부채에 갚을 날짜가 없을 수 없다** (critic 지적 수용)

§5-5는 "사용자 행 재작성 금지"라 쓰고, §5-3-15는 그 때문에 **"코드 우선, 없으면 id" 영구 폴백**을 산다.
§10-5에서 나는 *"의도한 부채이고 갚을 날짜를 못 정했다"* 고 썼다.

**실제 규모: `user_mark` 4행 · `user_biz` 2행.**
§5-5의 금지 규칙은 **수백만 행 소실(사고 4)에서 나온 것**인데 지금 **6행**에 적용되고 있다.
근거와 대상의 규모가 안 맞는다.

**사장 승인을 받을 항목**: *6행을 한 번 마이그레이션해도 되는가.*
승인되면 §5-3-15의 이중 조회 폴백이 **통째로 사라지고**, `schools.id` 별칭도 기한을 가질 수 있다.
거절되면 폴백을 유지하되 **그때는 "영구"라고 명시**한다 — 갚을 날짜 없는 부채를 부채라 부르지 않는다.

> 단 **URL 호환은 별개다.** `school_id`가 분석판 URL에 있으므로 **id 해석은 남는다.**
> 승인이 없애는 건 "두 진실"이지 "URL 호환"이 아니다.

### 5-5. 절대 안 하는 것

`school_id` 형식 변경 · `user_mark`/`user_biz`/`user_region`/`events` 행 재작성 · 기존 URL 깨기 ·
`by_floor`(호환) 제거 · `school_roster`(혼합) 제거 · 한 배포에서 스키마와 값 규칙 동시 변경

---

## 6. 이 설계가 실패하는 시나리오 4개

### 실패 1 — 코드북이 원본에서 흔들린다 (§2의 삭제 열한 개가 전부 무효)

척추가 "원본 코드가 문자열 파싱보다 낫다"인데, 그게 틀릴 수 있다.
`SIGUNGU_CD`가 시점에 따라 다른 이름을 가리키면(2010 마산·진해→창원, 2023 강원·전북 특별자치도 개명),
"코드가 진실"은 파싱보다 나은 게 아니라 **다른 종류의 거짓**이 된다.
그 순간 우리는 `_SGG_CANON` 34줄을 **이미 지운 뒤**다 — 되돌릴 규칙이 없다.
`PURR_CD`도 같다. 통폐합·재발급되면 학교 정체성이 시간축에서 쪼개진다.

- 조기 신호: `_cb2.py`의 코드당 이름 다수결 일관성
- **폐기선: 일관성 99% 미만이면 §5-3 전체 폐기, 1·2단계에서 멈춘다** (그래도 0-1·0-3은 고쳐진다)
- 완화: 코드북을 `코드→이름`이 아니라 **`(코드,연도)→이름`**으로. 비용이 오른다
- 남는 위험: 완화해도 "지금 이름"을 골라야 하는 화면(지역 칩)에선 결국 하나를 고른다

### 실패 2 — 가드가 잔소리가 되어 죽는다 (§1-5 무력화)

1인 개발 + 에이전트 팀에서 **allowlist 추가는 언제나 수정보다 싸다.**
에이전트가 급할 때 가드에 막히면 고치는 대신 allowlist에 넣고 간다. 3개월 뒤 40줄이면 가드는 죽은 것이다.
`until` 만료를 걸었지만 **만료일을 미루는 것도 한 줄**이다.

- 조기 신호: allowlist 항목 수, 그리고 **연장 횟수**
- 완화: 연장 1회 제한 + `# why:` 필수. 2회째부터 빌드 실패
- 남는 위험: 규칙 만드는 사람과 지키는 사람이 같으면 어떤 자동화도 최종적으로 자발성에 기댄다.
  **이 설계는 그 사실을 없애지 못한다. 비용만 올린다.**

### 실패 3 — ~~생성이 오히려 새 이중성을 만든다~~ **(소멸. §1-2에서 생성기를 폐기했다)**

이 시나리오는 critic 결정으로 **원인이 제거됐다.** 생성물을 안 만들면 생성물 이중성도 없다.
아래는 기록으로 남긴다 — **내가 못 고르던 트레이드오프를 다른 좌석이 잘라준 사례**다.

<details><summary>원문</summary>

`_contract.py`는 **커밋된 생성물**이다. 생성물을 리포에 두는 순간 "원본과 생성물" 두 파일이 생긴다.
지금 없던 이중성이다. 봉인(빌드 diff + 로더 해시)이 그걸 막지만, **봉인이 불편해지는 순간이 온다**:
파이썬 쪽에서 급히 값 하나를 고쳐야 하는데 노드를 못 돌리는 환경(수집 서버, 컨테이너 안)이면
사람은 생성물을 직접 고치고 해시 검증을 끄는 쪽을 고른다. 그러면 이 설계는 **거꾸로** 작동한다 —
전에는 파일이 하나였는데 이제 어긋난 파일이 둘이다.

- 조기 신호: 로더 실행 환경에 노드가 없는가. 해시 검증을 우회한 흔적(환경변수·주석)
- 완화: 생성물을 리포에 두지 않고 **로더 시작 시 DB에서 어휘를 읽는다**(서빙 DB에 `vocab` 테이블 1개).
  그러면 파일이 하나도 안 늘고 런타임에 항상 일치한다. 대신 로더가 DB 없이는 못 돈다
- 남는 위험: 완화안은 "어휘의 진실이 DB"가 되어 §1-6에서 기각한 안("오타가 진실이 된다")에 가까워진다.
  **이 트레이드오프를 나는 아직 못 골랐다. critic 좌석의 판단을 받고 싶은 1순위 항목이다**

</details>

**후기**: critic이 잘랐다 — *"postflight 10줄이 같은 사고를 이미 막는다. 갈렸는데 DB에 안 들어가는
불일치는 사용자에게 도달할 수 없다."* 내가 완화안으로 적어둔 DB 어휘 테이블은 §1-6이 이미 기각한
안으로 되돌아가는 것이었다. **설계가 원을 그렸고, 나는 그걸 못 봤다.**

---

### 실패 4 — 측정 대상이 움직인다 (**이 팀의 방법 자체의 실패. 이미 일어나고 있다**)

앞의 셋은 설계가 틀리는 시나리오다. 이건 **설계를 검증하는 방법이 틀리는** 시나리오다.

이 문서는 거의 모든 결정을 측정에 걸어뒀다 — §5-0b의 여섯 항목, 실패1의 폐기선(일관성 99%),
§2-b의 행 커버리지 98%, §1-0의 커버리지 baseline 36.6%. **전부 숫자다.**
그런데 **백필 체인이 13시간째 레이크에 쓰고 있어 같은 질의가 실행할 때마다 다른 답을 준다.**
data가 같은 4개 id를 조회했는데 한 번은 3건, 다시 하니 1건이었다.
`전남광주 27곳`이 재측정에서 **8건**이 된 것도 같은 원인이다.

**"측정으로 설계를 검증한다"는 이 팀의 방식이, 측정 대상이 움직이면 성립하지 않는다.**
흔들리는 수치 위에 선 폐기선은 폐기선이 아니다 — 재실행하면 판정이 뒤집힌다.

- 조기 신호: **같은 질의를 두 번 돌려 답이 다르면 그 수치로 결정하지 않는다.** 재실행 대조를 측정 절차에 넣는다.
- **조건(설계에 명시): 확정 수치가 필요한 결정은 전부 "백필 정지 후 재측정"을 전제로 한다.**
  §5-0b의 측정과 실패1·§2-b의 폐기선은 **정지 상태에서 다시 재기 전까지 잠정**이다.
- **잠정 수치로 해도 되는 것과 안 되는 것을 가른다:**
  ✅ **경로·구조의 확인** — 코드를 읽어 확정되는 것. A30의 문턱 7곳, A23의 두 겹 실패, A25의 부분 실패 경로.
     이것들은 데이터가 흔들려도 **코드가 그대로면 그대로다.**
  ❌ **비율이 판정선을 넘나드는 결정** — 폐기선 98%·99%, 커버리지 baseline 36.6%, 문턱 삭제의 손익.
- 남는 위험: **백필이 언제 끝나는지 모른다.** 기다리면 이번 주가 지나가고, 안 기다리면 판정이 무효다.
  **이 선택은 내 몫이 아니다 — 사장·리더 결정 항목으로 올린다.**

> **이 실패가 앞의 셋보다 나쁜 이유**: 실패 1~3은 틀렸다는 걸 나중에 알 수 있다.
> 실패 4는 **맞았는지 틀렸는지를 알 방법이 없다.** 측정이 재현되지 않으면 반증도 불가능하다.
>
> 그리고 이번 주에 이미 두 번 작동했다 — `PDLC_NM` 품목 가설(픽스처가 가짜)과
> `PDLC_NM` 소재지 가설(자격 지역을 소재지로 오독). **둘 다 ⚠를 달아둔 덕에 피해가 0이었다.**
> **⚠ 표기가 이 문서의 유일한 실패4 방어다.**


## 8. 상태 계층 — react-query / Zustand 판정

사용자 지시: *"우리 mw-auction이랑 같은 구조로 하셈 … 추가적으로 최신 동향까지."*

### 8-0. mw-auction 실측 — 리더 브리핑에서 **빠졌던 것 둘**

`F:/Project/mw-auction`을 직접 읽었다. 리더가 전한 패턴은 맞다. 다만 두 가지가 빠졌고, 둘 다 내 판단을 바꿨다.

| 확인한 것 | 값 | 우리에게 |
|---|---|---|
| `api/<도메인>/{api,queries,schema}.ts` | 도메인 12개 | ✅ 그대로 채택 |
| `queryOptions`가 곧 key factory (`keys.ts` 없음) | — | ✅ 채택 |
| `queryFn: ({ signal }) => fetch(…, signal)` | 전 쿼리 | ✅ **U18 forecast 레이스가 이걸로 사라진다** |
| `staleTime: seconds(CACHE.X)` — `@mw-auction/shared`의 `CACHE` | `config/site.ts:59` | ✅ 채택, **값은 우리 리듬으로** |
| `createPollingQuery` (에러 시 폴링 중단·`retry:false`) | 폴링 전용 | ❌ **우리는 폴링이 없다** |
| **`safeApiJson(promise, Schema)` — 전 응답 런타임 zod 파싱** | `z.safeDecode` + Sentry 보고 후 throw | ⚠️ **내 §8-4가 틀렸다 → 아래에서 뒤집는다** |
| **`zustand ^5.0.14`** — feature-local 스토어 + `.spec.ts` | `identity`/`votes`/`reports`/`settlements`/`jitter` | ⚠️ **§8-3 정밀화** |
| `drizzle-orm 1.0.0-beta.22` + `createSelectSchema` | **우리와 같은 버전** | ✅ §9의 최대 근거 |
| **손유지 `schema.sql` 없음.** `generate`+`migrate` 18개 | — | ✅ §9 |
| `api-types.generated.ts` — **NestJS Swagger `/docs-json`에서 생성** | `pnpm generate:api-types` | ✅ **내가 §8-5에서 제안한 "API 대장"이 이미 있다** |
| `@tanstack/react-query-next-experimental` | 스트리밍 하이드레이션 | ❌ 보류 (§8-7) |

**우리와 안 맞는 것 하나를 먼저 지목한다: 폴링.**
mw-auction은 10초 스냅샷 주기라 `refetchInterval`이 구조의 축이다.
**우리는 하루 1회 갱신**이다(로더가 밤에 돈다). `create-polling-query.ts`는 우리에게 죽은 코드다.
대신 우리에겐 mw-auction에 없는 축이 있다 — **`fetchedAt`**(`open_auctions.fetched_at`).
서버가 이미 "이 데이터가 언제 것인지"를 내려주고 있다. 캐시 정책을 시계가 아니라 **그 값에 건다**(§8-2b).

### 8-1. 리더의 3층 구분 — 칸 수는 맞고 **경계가 틀렸다**

리더 안: 서버 상태→`react-query` / 클라이언트 상태→스토어 / UI 상태→`usePersistedState`.
칸 수는 맞다. 그런데 **가르는 축이 "서버냐 클라냐"인 게 문제다.** 이 문서가 내내 묻는 축은 하나다 —
**누가 진실인가.** 그 축으로 다시 그으면 경계가 달라진다.

상태마다 세 가지를 묻는다: ① 진실이 어디 있나 ② 잃으면 어떻게 되나 ③ 게스트에서 살아야 하나.

| 층 | 진실 | 잃으면 | 게스트 | 해당 | 도구 |
|---|---|---|---|---|---|
| **L1 원격 사실** | 서버 DB | 재조회하면 됨 | 무관 | 공고·회차·낙찰·예보·로스터·업체 | **react-query** |
| **L2 계정 데이터(이중 권위)** | **게스트=브라우저 / 로그인=서버** | **사용자 데이터 소실** | **살아야 함** | `bizNos`·`regions`·`marks` | **전용 스토어(현행 유지)** |
| **L3 화면 상태** | 이 브라우저 | 아무 일 없음 | 무관 | 렌즈·펼침·필터·`viewRegion` | **`usePersistedState`** |

**리더 안과의 차이는 L2다.** 리더는 이걸 "클라이언트 상태 → Zustand"로 뒀다.
나는 **L2는 상태 라이브러리로 안 풀린다**고 판정한다. 진실이 **로그인 여부에 따라 이동하는** 상태이고,
Zustand에도 react-query에도 "두 진실을 합친다"는 개념이 없다. `session.ts` 210줄 중 대부분이 그 일이다.

### 8-2. `marks` 판정 — **L2다. react-query mutation 아니다** (리더 안에 반대)

리더는 *"낙관적 업데이트가 필요한 서버 상태 = mutation 영역"*으로 봤다. 세 가지 근거로 반대한다.

1. **게스트에서 완전히 동작해야 한다.** "로그인 없이 전 기능"은 이 서비스의 제약이다(`auth.ts` 주석·`session.ts:5`).
   mutation은 서버가 있다는 전제 위에 선다. 게스트면 mutation 대상이 없다.
2. **`/api/me/marks`는 낙관적 업데이트의 대상이 아니다.** `app.module.ts:838`이 `delete` 후 `insert` —
   **전체 치환 PUT**이다. mutation+invalidate 패턴이면 값 하나 칠 때마다 전량 PUT + 전량 refetch가 된다.
   지금(로컬 즉시 + 백그라운드 PUT)이 더 낫다.
3. **리더가 든 사고 근거가 오히려 반대를 가리킨다.** 둘째 사업자 값이 지워진 건 mutation이 아니라
   **최초 로그인 1회 병합**에서 났다(`session.ts` boot의 `needMerge` 블록).
   `react-query`에는 그 개념 자체가 없다. 라이브러리를 넣어도 **그 코드는 한 줄도 안 준다.**

**다만 리더 지적의 알맹이는 맞다: 손으로 짠 병합이 사고를 냈다.**
처방은 라이브러리가 아니라 **이 문서의 원칙 그대로** — 규칙을 한 곳에, 그리고 순수하게:

```ts
// packages/shared/src/domain/merge.ts   ← 순수 함수, 서버·웹 공용, 테스트 가능
export function mergeAccountData(local: AccountData, server: AccountData): AccountData
```
지금 이 규칙은 `boot()` 안에 인라인이라 **재사용도 검증도 안 된다.**
이 프로젝트에서 사고를 낸 건 언제나 "한 곳에 없는 규칙"이었지 "라이브러리가 없어서"가 아니었다.

### 8-2b. `CACHE` 상수 — `packages/shared`에 둔다. 값은 **`fetchedAt`에 건다**

mw-auction처럼 shared에 둔다. 이유는 "그쪽이 그래서"가 아니라 **캐시 정책이 데이터의 갱신 리듬이고,
그 리듬을 아는 건 로더(파이프라인)지 화면이 아니기 때문**이다. 화면이 `staleTime: 60_000`을 손으로 쓰면
그건 §3-1의 "웹이 스스로 만드는 것"이 하나 더 느는 것이다.

```ts
// packages/shared/src/config/cache.ts
export const CACHE = {
  DAILY: hours(6),      // 로더가 하루 1회 — 6시간이면 같은 날 재조회 0
  OPEN: minutes(30),    // 열린 공고: 마감이 다가오므로 짧게
  ME: 0,                // 계정 데이터는 L2. 캐시 대상 아님
  LOOKUP: hours(24),    // 상호명 — 안 변한다
} as const;
```

**그리고 시계보다 나은 신호가 이미 있다.** `open_auctions.fetched_at`을 응답에 실어 보내면
화면이 *"몇 시 기준 자료"* 를 말할 수 있다 — **헌법의 "사실 + n 명시"가 캐시에도 적용된다.**
`staleTime`은 재조회 타이밍을 정할 뿐이고, **사용자에게 보이는 건 `fetchedAt`이어야 한다.**
지금은 데이터가 오래됐는지 화면이 말하지 않는다. 이건 캐시 도입의 부수효과가 아니라 **얻어야 할 것**이다.

> 로더가 `fetchedAt`을 이미 쓰고 있는데(`open_auctions` 기본값) **아무 화면도 안 읽는다.**
> §0-4("원본이 준 값을 버린다")와 같은 모양이 우리 API에서도 일어나고 있다.

### 8-3. Zustand — **L2를 대체하지 않는다. 다만 mw-auction 용법은 채택한다**

`session.ts` 210줄을 Zustand가 무엇으로 갚는지 세어봤다.

| Zustand가 **대체하는 것** | 줄 |
|---|---|
| `listeners` Set · `emit()` · `setState()` · `subscribe` · `getSnapshot` · `getServerSnapshot` | **약 15줄** |

| Zustand가 **대체 못 하는 것** | 줄 |
|---|---|
| `boot()` 이중 권위 병합(`needMerge`·회차별 rates 보존) | ~40 |
| `readLocal`/`writeLocal`/`put` 3중 쓰기 | ~35 |
| marks 손상 사본 보존(`marksUnreadable`·`MARKS_BACKUP_KEY`) | ~15 |
| `loadBizNames` 지연 조회 | ~12 |

**순비용: 의존성 +1, 없애는 줄 15, 그리고 잃는 안전장치 1개.**
`persist` 미들웨어를 쓰면 방금 만든 `use-persisted-state.ts`의 세 규칙 중 **3번(손상 사본 보존)을 잃는다** —
Zustand persist는 읽기 실패 시 조용히 초기값으로 간다. 우리는 그 조용함 때문에 고지 UI를 만들었다.
**정당화되지 않는다.** 방금 만든 걸 버리는 비용까지 더하면 더 그렇다.

**재검토 트리거(숫자로 못박는다)**: `session.ts`가 **300줄**을 넘거나 구독 훅이 **6개**를 넘으면 다시 본다.
그때는 L2가 진짜 스토어 규모가 된 것이다. 그 전엔 아니다.

**mw-auction 실측이 이 판정을 바꾸지 않는다 — 오히려 확인해 준다.**
그쪽 zustand는 **전역 앱 스토어가 아니라 feature-local 스토어**다:
`community/model/{identity,votes,reports}.store.ts` · `s/_lib/my-settlements.store.ts` · `server-online/model/jitter.store.ts`.
전부 **서버에 진실이 없는 값**(익명 신원·내 투표 기록·로컬 정산 메모)이고, **전부 `.spec.ts`가 붙어 있다.**
즉 mw-auction도 zustand를 **서버 캐시 대체재로 쓰지 않는다.** 우리 L2(이중 권위 + boot 병합)와는 층이 다르다.

**채택하는 것은 라이브러리가 아니라 그 용법이다**: *작고 · 순수하고 · 테스트가 붙은 모듈*.
그게 §8-2에서 내가 `mergeAccountData()`로 제안한 것과 같다. mw-auction은 그걸 zustand로 담았고,
우리는 이미 `useSyncExternalStore` 스토어가 있으니 **담을 그릇을 바꿀 이유가 없다.** 규칙만 꺼내면 된다.
새로 생기는 순수 클라 상태(서버에 진실이 없는 것)가 나오면 그때 zustand를 쓴다 —
단 **`persist` 미들웨어는 쓰지 않는다**(손상 사본 보존 규칙을 잃는다. §8-3 위).

### 8-4. react-query ↔ 계약 설계의 맞물림 (front가 기다리는 답)

**결정: 쿼리 선언은 계약의 *소비* 지점이다. 계약을 검증하지 않고, 계약에서 타입을 받는다.**

```
packages/shared/src/domain/*.ts     ← 계약 (zod 스키마)
        │  타입만 흐른다 (z.infer) — 런타임 파싱 없음
        ▼
apps/web/src/lib/queries/*.ts       ← queryKey + fetcher + 반환 타입
        ▼
화면                                 ← useQuery(roundsQuery(schoolId))
```

- **타입은 계약에서 파생된다.** 웹이 응답 타입을 선언하지 않는다 → **§0-5의 로컬 타입 23개가 여기서 죽는다.**
  react-query 전환과 계약 도입은 **별개 작업이 아니라 같은 작업**이다.
- **런타임 파싱은 한다. 이건 내 앞선 판단을 뒤집는 것이다.**
  1차안에서 나는 *"서버와 웹이 컴파일 타임에 같은 타입을 공유하므로 어긋날 경로는 배포 스큐뿐"* 이라고 썼다.
  **그 전제가 지금 우리에겐 거짓이다.** §0-6에서 내가 직접 확인했듯 **서버는 zod 계약을 응답에 적용하지 않는다** —
  `OpenAuction`·`SchoolSummary`·`FirmRecord`는 어디서도 응답에 안 걸린다. 즉 **서버가 계약과 다른 걸 보내는 게 지금 정상 상태**다.
  컴파일 타임 공유는 §5-2-9가 끝난 *뒤에* 생기는 성질이지, 지금 있는 게 아니다.
  **런타임 파싱은 그 괴리를 보이게 만드는 유일한 장치**이고, mw-auction의 `safeApiJson`이 정확히 그걸 한다
  (`z.safeDecode` → 실패 시 Sentry 보고 → 사용자에겐 "데이터를 불러올 수 없습니다"). 우리도 Sentry가 이미 붙어 있다.
- **기본은 파싱, 면제는 측정된 숫자로만.** `/firms/bids`(limit 5000)·`/wins/recent`(limit 1000)처럼
  큰 배열은 비용이 실재한다. **면제하려면 `scripts/ssot-allow.json`에 실측 ms를 적어야 한다** — 추측 금지.
  이 규칙이 없으면 "무거울 것 같아서" 전부 면제되고, 그러면 장치가 없는 것과 같다.
- **`/api/me`는 실패 시 게스트 폴백.** 다른 곳은 throw + ErrorBoundary. 계정 데이터는 못 읽는 것보다 게스트가 낫다.
- **덤으로 URL 조립이 한 곳이 된다.** 지금 55곳이 각자 `?bizNos=${bizNos.join(',')}` 를 만든다.
  `queries/`로 모으면 **키 조립을 한 곳으로 모으는 §1-3과 같은 계열의 이득**이다.
- **같은 queryKey를 공유하면 두 화면이 같은 데이터를 본다.** 지금 오늘 화면과 분석판이 각자 fetch한다.
  다만 **같은 데이터를 봐도 계산이 두 곳이면 답은 여전히 갈린다** → 8-5.

### 8-5. 0-1(예보 알고리즘 2벌)은 이 결정으로 **안 풀린다** — §1-3의 한계 자백

리더가 "§1-3이 이 경우에 실제로 작동하는지 구체적으로 보여라"고 했다. **작동하지 않는다.**

상태 라이브러리는 "누가 데이터를 갖고 있나"를 정리하지 **그 데이터로 무엇을 계산하나**를 정리하지 않는다.
react-query를 넣으면 웹이 `rounds`를 캐시하고 **여전히 자기가 예보를 계산한다.** 오히려 편해져서 늘 수도 있다.

**브랜디드 타입도 못 잡는다.** 이런 걸 시도할 수 있다:
```ts
declare const served: unique symbol;
type Served = { readonly [served]: true };
export type Forecast = { medGapDays: number; expected: string; dueInDays: number } & Served;
```
웹이 만든 객체를 `Forecast` 자리에 넣으면 컴파일이 깨진다. **그런데 웹이 자기 타입을 새로 선언하면 우회된다.**
그리고 지금 정확히 그렇게 돼 있다 — `analysis-board.tsx:504`의 `forecast` useMemo는 익명 객체를 만들어
`Forecast` 타입을 아예 안 거친다. **타입은 "안 쓰기로 한 사람"을 못 막는다.**

`Math.floor(gaps.length / 2)`는 그냥 숫자 계산이라 타입 시스템이 볼 게 없다.
**§1-3은 키(A3)에는 작동하고 알고리즘(0-1)에는 작동하지 않는다. 인정한다.**

실제로 잡는 건 둘뿐이고, 둘 다 타입보다 약하다:
1. **가드 grep** — `apps/web/**`의 `864e5` · `Math.floor(*.length / 2)`. 이미 §1-5 표에 있다. **이게 유일한 실효 장치다.**
2. **API 대장** — 내가 §8-5에서 손으로 쓰자고 제안했던 것을 **mw-auction이 이미 생성물로 갖고 있다**:
   `packages/shared/src/api-types.generated.ts`, **NestJS Swagger `/docs-json`에서 생성**(`pnpm generate:api-types`).
   우리도 같은 방식으로 간다 — `@nestjs/swagger`를 붙이고 `openapi-typescript`로 뽑는다.
   손으로 쓰는 `API-LEDGER.md` 안은 **폐기**한다(§4의 FIELD-LEDGER와 달리 이건 자동화가 되는 대상이다).
   웹이 뭘 계산하려 할 때 "서버가 이미 주고 있나"를 볼 곳이 지금 **없다** — 그래서 베꼈다.
   **다만 이건 의제 3(NestJS 모듈 분할)이 먼저 끝나야 값이 산다** — 지금처럼 954줄 인라인이면 Swagger 문서도 954줄이다.

> **0-1의 진짜 원인은 상태 관리도 타입도 아니다. "서버가 이미 준다"를 볼 방법이 없었던 것이다.**
> 원본을 못 봐서 규칙을 쌓은 것(§0-4)과 **똑같은 실패를 우리 API에 대해 한 것**이다.

### 8-6. `marks` 낙관적 업데이트 — `onMutate`/`setQueryData`/롤백으로 **안 풀린다** (리더 Q5)

낙관적 업데이트의 정의는 *"서버 응답을 기다리지 않고 캐시를 먼저 고치고, 실패하면 되돌린다"* 이다.
`marks`는 세 조건이 다 어긋난다.

| 낙관적 업데이트의 전제 | `marks`의 실제 |
|---|---|
| 캐시가 진실의 사본이다 | **게스트는 서버가 아예 없다.** 되돌릴 원본이 없다 |
| 실패하면 되돌리는 게 맞다 | **되돌리면 안 된다.** 사장이 방금 친 값이 사라진다 — 로컬에 남기는 게 맞다 |
| 서버 응답이 다음 진실이다 | `PUT /me/marks`는 **전체 치환**이고 응답은 `{ok, n}`뿐이다. 돌아올 진실이 없다 |

지금 구조(로컬 즉시 쓰기 → 백그라운드 PUT → 실패해도 로컬 생존)는 **낙관적 업데이트가 아니라
local-first**다. 그리고 그게 이 서비스에 맞는 모델이다 — 투찰 마감 직전에 네트워크가 흔들려도 값이 살아야 한다.

**리더가 든 사고(둘째 사업자 값 소실)는 여전히 실재하고, 처방은 §8-2 그대로다**:
`mergeAccountData()` 순수 함수 + `.spec.ts`. mw-auction의 store들이 전부 spec을 갖고 있는 것과 같은 규율이다.
**라이브러리는 이 사고를 못 막는다. 테스트가 막는다.**

### 8-7. 최신 동향 — 채택/보류 판정 (유행이 아니라 막을 사고로)

| v5 기능 | 판정 | 근거 |
|---|---|---|
| `queryOptions` 헬퍼 | ✅ **채택** | key·fn·정책의 단일 선언. `keys.ts` 이중화를 처음부터 안 만든다 |
| `queryFn`의 `signal` | ✅ **채택** | **U18 forecast 레이스가 사라진다.** `today/page.tsx:144-154`의 손으로 짠 `AbortController`+스테일 응답 폐기 15줄이 없어진다 |
| `select`로 파생 | ✅ **채택(제한)** | 정렬·필터만. **판정·집계는 금지**(§3-1) — `select`가 §0-1 재발의 새 은신처가 될 수 있다 |
| `useSuspenseQuery` + Suspense 경계 | 🟡 **부분** | `loading` 분기 제거 이득은 크나 화면 전체가 `'use client'`+`useEffect`라 경계 설계가 선행이다. **도메인 이관 3순위 이후** |
| `throwOnError` + ErrorBoundary | ✅ **채택** | 지금 `.catch(() => {})`가 **에러를 통째로 삼킨다**(today:89·133·151 등). 사고가 조용히 지나간다 — §1-4의 "조용한 continue"와 같은 죄 |
| `onMutate`/`setQueryData` 낙관적 갱신 | ❌ **기각** | §8-6 |
| `experimental_prefetchInRender` | ❌ **보류** | experimental. 우리 병목은 렌더 워터폴이 아니라 중복 계산이다 |
| RSC prefetch + `HydrationBoundary` | ❌ **보류** | 전 화면이 클라이언트 컴포넌트다. 이득은 TTFB이고 **막는 사고는 0** |
| `react-query-next-experimental` 스트리밍 | ❌ **보류** | 위와 같음. mw-auction은 쓰지만 그쪽은 SSR 트래픽 서비스다 |
| `createPollingQuery` | ❌ **불필요** | 우리는 하루 1회 갱신 (§8-0) |
| devtools | ✅ **채택** | 이미 설치돼 있다 |

> 보류 넷의 공통 이유: **막을 사고가 없다.** 이 문서의 판단 기준이 그거다.

### 8-8. 도메인 이관 순서 — fetch 55곳을 한 번에 못 옮긴다

기준: ① 읽기 전용·사용자 데이터 없음 ② **여러 화면이 같은 데이터를 본다**(중복 제거 이득) ③ 이미 사고가 난 곳.

| 순 | 도메인 | 왜 먼저인가 | 이 단계에서 닫히는 것 |
|---|---|---|---|
| 1 | **`rounds`** | `today/page.tsx:130` 주석이 이미 자백한다 — *"② 리허설과 같은 소스 … 두 화면 숫자가 갈리면 안 된다"*. **같은 데이터를 두 화면이 각자 부른다** | 캐시 공유로 두 화면이 **같은 응답**을 본다 |
| 2 | **`wins`** | 읽기 전용, 서버에 이미 10분 캐시, 오늘·낙찰·분석판 3화면이 공유 | 중복 요청 · `.catch(()=>{})` 삼킴 |
| 3 | **`schools`** | `forecast`에 U18 레이스가 있다 | **`signal` 하나로 손짠 AbortController 15줄 제거** |
| 4 | **`open`** | 오늘 화면 중심, 단순 | `fetchedAt` 표기 도입 지점 (§8-2b) |
| 5 | `firms` | 읽기 전용이나 사업자번호가 키 — 캐시 키 설계 주의 | 내 성적/납품 중복 |
| 6 | `market` | 단순, 이득 작음 | — |
| — | **`me`** | **옮기지 않는다.** L2다 (§8-1) | — |
| — | `events` | POST 전용. 옮길 이유 없음 | — |

**1·2번만 끝나도 §0-1(예보 2벌)의 절반이 닫힌다** — 두 화면이 같은 캐시를 보게 되기 때문이다.
**나머지 절반(계산이 두 곳인 것)은 §8-5대로 안 닫힌다.** 캐시 공유는 데이터를 맞추지 계산을 맞추지 않는다.

### 8-9. 결론 6줄

1. 3층은 맞다. **경계만 고친다** — L2는 "클라이언트 상태"가 아니라 **이중 권위 계정 데이터**다.
2. mw-auction의 `api/<도메인>/{api,queries}.ts` 구조를 채택한다. **폴링 래퍼만 뺀다**(우리는 하루 1회).
3. **런타임 zod 파싱을 한다 — 내 1차 판단을 뒤집는다.** 지금 서버가 계약을 응답에 안 걸어서, 그게 유일한 탐지 장치다.
4. `CACHE`는 shared에. **사용자에게 보이는 건 `staleTime`이 아니라 `fetchedAt`이다.**
5. `marks`는 L2. **낙관적 업데이트가 아니라 local-first**다. 사고는 테스트가 막는다.
6. **0-1은 이 결정으로 절반만 풀린다.** 캐시가 데이터를 맞추고, 계산은 grep 가드가 잡는다.

---

## 9. drizzle 툴킷 — DDL 이중성(A16)을 없앤다

사용자 지시: *"그 드리즐 툴킷이나 이런 거 전부 다 써."*
**"전부 다"를 그대로 따르지 않는다.** 하나(`push`)는 우리 환경에서 §0-5 사고를 **되풀이할 도구**다.
안 쓸 것을 안 쓴다고 보고하는 게 이 지시에 맞는 답이다.

### 9-00. 실행 시점 — **이번 주에 하지 않는다** (critic 지적 수용, 사실 하나 정정)

critic이 *"§5-2-11을 이번 주에서 빼라"* 고 했다. **받는다.** 근거가 맞다:
`schema.sql`은 지금 **살아 있는 멱등 마이그레이션**이고, drizzle은 비멱등 CREATE + `__drizzle_migrations`를
전제하는데 **그 테이블이 이 DB에 없다.** 살아있는 DB의 마이그레이션 메커니즘 교체다.

**다만 사실 둘을 정정한다(실물 확인):**
1. `schema.sql`은 **매 배포가 아니라 `initial-load` Job의 initContainer**에서 돈다(`app.yaml:150-163`).
   교체 범위가 "배포 전체"가 아니라 **Job 하나의 명령**이라 critic이 본 것보다 좁다.
2. **`|| true`는 이미 제거됐다.** 커밋 `6d91978`이 걷어냈고 지금은 `psql -v ON_ERROR_STOP=1`이다.
   DEBT.md A-2는 그 커밋 이전 상태를 본 것으로 보인다. (주석이 사고 4의 1차 사료로 남아 있다.)

**새로 드러난 비용 하나**: 데이터플레인 이미지는 **파이썬**이다. `drizzle-kit migrate`는 노드가 필요하다.
마이그레이션 Job을 별도 이미지로 세우거나 노드를 얹어야 한다 — 1차안이 안 센 비용이다.

**결정: §9는 설계로 확정하되 실행은 §5-1(거짓말 멈추기) 완료 후.**
그 전에 할 수 있고 위험이 0인 것 하나는 **지금 한다 — `pull` + diff(B1·B2).**
DB에 한 글자도 안 쓰고, 리더가 막으라 한 사고 ①(*"스키마와 실제 DB가 어긋나도 아무도 모름"*)을
**즉시 드러낸다.** 나머지(B3~B7)는 그 diff를 보고 판단한다.

### 9-0. 위험도가 크게 낮다 — 같은 beta를 쓰는 형제 프로젝트가 있다

`drizzle-orm/drizzle-kit 1.0.0-beta.22`는 beta라 도입 전에 걸렸는데, **mw-auction이 정확히 같은 버전을
프로덕션에서 쓴다.** 그리고 그쪽엔 **손유지 `schema.sql`이 없다** — `generate`+`migrate`로 마이그레이션
18개(파티션·인덱스 교체 포함)를 운용한다. 우리가 하려는 것이 이미 검증돼 있다.
설치된 CLI 실측 명령: `generate · migrate · pull · push · studio · up · check · export`.

### 9-1. 도구별 판정

| 도구 | 판정 | 이유 |
|---|---|---|
| `generate` | ✅ **핵심** | 스키마 → 마이그레이션. A16을 닫는 물건 |
| `migrate` | ✅ **핵심** | **유일한 DDL 적용 경로.** k8s Job으로 |
| `pull` (구 introspect) | ✅ **베이스라인 전용** | 읽기 전용. **드리프트 보고서를 만드는 도구**(9-2) |
| `check` | ✅ **빌드에 물림** | 마이그레이션 충돌·순서 깨짐 탐지. 공짜 |
| `export` | ✅ **한정** | 전체 DDL 출력. **`schema.sql` 폐기 전 대조용으로만** |
| `up` | 🟡 **필요시** | 스냅샷 포맷 업그레이드. beta라 버전 올릴 때 쓸 일이 온다 |
| `studio` | 🟡 **개발 전용** | **프로덕션 URL로 열면 쓰기 가능한 GUI다.** dev DB에만 |
| **`push`** | ❌ **금지. `package.json`에서 삭제한다** | 9-3 |

### 9-2. 베이스라인 — 사용자 데이터가 위험해지는 경로가 없음을 보인다

현 상태: 라이브 DB는 손유지 `schema.sql`로 만들어졌고, `packages/shared/drizzle/`엔 08-27자 마이그레이션이
**하나** 있는데 그 뒤 컬럼 추가는 전부 `schema.sql` ALTER 블록으로만 갔다.
즉 **마이그레이션 원장(`__drizzle_migrations`)이 라이브 DB에 없거나 현실과 안 맞는다.**

| 단계 | 명령 | DB에 쓰나 | 사용자 데이터 위험 |
|---|---|---|---|
| B1 | `pull` (라이브 대상) | **읽기만** | 없음 |
| B2 | 생성된 스키마 ↔ 손으로 쓴 drizzle 스키마 **diff** | 안 씀 | 없음 |
| B3 | diff를 근거로 TS 스키마 수정 (현실이 맞으면 TS를, TS가 맞으면 B5 마이그레이션에) | 안 씀 | 없음 |
| B4 | 낡은 `20260827064745_*` 삭제 후 `generate` — **베이스라인 1개** | 안 씀 | 없음 |
| B5 | 베이스라인 SQL을 **손으로 멱등화**(`CREATE TABLE IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS`) | 안 씀 | 없음 |
| B6 | `migrate` 1회 — 라이브에선 **전부 no-op**, 원장 행만 기록된다 | **원장 행 1개만** | **없음** |
| B7 | 이후 마이그레이션은 정상 생성·적용 | 씀 | 9-3의 방어 적용 |

**B2가 이 도입의 진짜 산출물이다.** *"drizzle 스키마와 실제 DB가 어긋나 있어도 아무도 모르는 것"* —
리더가 막으라고 한 사고 ①이 **B2 한 번으로 즉시 드러난다.** 코드 수정 0줄, 위험 0.
**B1~B5는 DB에 한 글자도 안 쓴다.** 위험은 B6 한 줄뿐이고, 멱등화했으므로 실행 결과는 원장 행 하나다.

> **왜 원장 행을 손으로 넣지 않고 멱등 마이그레이션을 실행하는가**: 해시를 위조해 원장에 넣는 방법도 있지만,
> 그건 "적용했다고 주장하지만 실제로 대조된 적 없는 상태"를 만든다. 멱등 SQL을 진짜로 실행하면
> **DB가 스스로 대조한다** — 컬럼이 없으면 그때 생기고, 있으면 아무 일도 안 난다. 검증 가능한 쪽을 고른다.

### 9-3. `push` 금지와 파괴 방어 (DB 전량 삭제 사고 재발 대비)

**`push`를 금지하는 이유.** `push`는 스키마와 DB를 직접 diff해 적용한다. **스키마에 없는 테이블·컬럼을
DROP하자고 제안**하고, 비대화형에서 `--force`면 그냥 지운다. 리더 지적대로 **우리는 k3d가 곧 프로덕션이라
dev/prod 경계가 없다** — 같은 명령이 어느 DB를 보는지 `DATABASE_URL` 하나에 달렸다.
`db:push`가 `package.json`에 있는 한, 그건 **한 줄 오타로 782만 행을 지우는 버튼**이다. 삭제한다.

**`generate`도 안전하지 않다.** 누가 TS 스키마에서 컬럼을 지우면 `generate`는 군말 없이
`DROP COLUMN`을 뱉고 `migrate`가 적용한다. 그리고 §5-5가 "절대 지우지 말라"고 한 것들이 정확히
지우고 싶어지는 것들이다 — `by_floor`(호환용) · `school_roster`(혼합) · `user_mark`의 구 형식.

방어 셋, 전부 이미 도는 게이트 위에:

1. **파괴 구문 가드** — `ssot-guard.mjs`가 새 마이그레이션 SQL에서 `DROP TABLE|DROP COLUMN|TRUNCATE`를 찾으면
   빌드 실패. 통과하려면 그 파일에 `-- DESTRUCTIVE-OK: <이유>` 줄이 있어야 한다. **조용한 파괴를 의도적 행위로 바꾼다.**
2. **보호 목록** — `user`·`session`·`account`·`verification`·`user_biz`·`user_region`·`user_mark`·`events`와
   호환 컬럼(`by_floor`·`school_roster`)은 **`DESTRUCTIVE-OK`가 있어도 빌드 실패.** 사람이 스키마를 고쳐야 통과한다.
3. **`migrate` 전 사용자 테이블만 덤프** — `db:migrate` 스크립트가 먼저 `pg_dump`로 위 8개 테이블을 뜬다.
   **핵심은 우리가 잃을 수 없는 것이 아주 작다는 사실이다**: 회차 147,633·투찰 782만은 **로더가 다시 만든다.**
   되돌릴 수 없는 건 계정·마크·이벤트뿐이고 그건 수 MB다. 재적재 가능성과 아닌 것을 가르면 **방어 비용이 급감한다.**

### 9-4. `drizzle-zod` — 어디까지 파생하고 어디부터 손으로 쓰나

mw-auction이 답을 갖고 있다: `createSelectSchema(table)`로 **테이블 모양만** 파생하고,
응답 봉투·계산 필드는 손으로 조립한다(`domain/{equip,keyword,listing,price,settlement}.ts`).

**규칙: 테이블 모양은 파생, 응답 모양은 조립.**
```ts
const OpenAuctionRow = createSelectSchema(openAuctions);   // ← 컬럼 추가가 자동 반영. 손 동기화 0
export const OpenAuctionResponse = OpenAuctionRow.extend({ // ← 계산 필드만 손으로
  categories: z.array(Category), anchorAmount: z.number().nullable(),
  band: FloorStat.nullable(), recent3: z.array(z.number()), usualN: z.number().nullable(),
  nSameFloor: z.number(), unrestricted: z.boolean(), allowedLabel: z.string(),
  qualificationBasis: z.literal('region-only'), countBasis: z.literal('inclusive'),
});
```
이게 front가 찾은 *"zod가 응답의 절반만 기술한다"* 를 정확히 고친다 —
**절반(테이블)은 자동이라 어긋날 수 없고, 나머지 절반(계산)은 작아서 손으로 유지된다.**

**품목 SSOT와의 관계 (§1-2와 충돌하지 않는다).** `createSelectSchema`는 `category`를 `varchar` →
`z.string()`으로 준다. 그대로 두면 어휘가 풀린다. **refine으로 좁힌다**:
```ts
createSelectSchema(schoolAuctions, { category: () => Category })   // Category = z.enum(CATEGORIES)
```
**`vocab/category.ts`가 여전히 어휘의 SSOT이고, drizzle 파생은 그 어휘를 주입받는다.**
`Sourced<T>`(§1-0)는 계산 필드이므로 `.extend()` 쪽에 들어간다 — 파생 쪽을 오염시키지 않는다.

**과잉 파생 금지선**: `createInsertSchema`는 **쓰지 않는다.** 우리 쓰기 경로는 로더(파이썬)와
`/me/*` PUT 셋뿐이고, 후자는 이미 손으로 쓴 좁은 zod가 맞다(사업자번호 정규식 등 DB 제약보다 강하다).

### 9-5. 파이프라인 — 파이썬이 DDL을 만드는 경로를 닫는다

**규칙: DDL은 drizzle만. 로더는 스키마를 요구할 수 있을 뿐 만들 수 없다.**

data가 최근 `ALTER ADD COLUMN`을 직접 적용한 경로가 있다. 그걸 닫는다 —
컬럼이 필요하면 **TS 스키마를 고치고 `generate`+`migrate`**다. 파이썬은 §5-2-12의 preflight로
*"내가 쓸 컬럼이 다 있는가"* 를 묻고 없으면 **적재 전에 죽는다.** 만들지 않는다.
이게 언어 경계에서 유일하게 필요한 계약이고, §1-1의 표와 같은 원리다 — **파이썬은 어휘와 스키마를 받기만 한다.**

그리고 이건 **로더 두 벌 문제(§0-5)를 더 급하게 만든다**: 낡은 `data-plane/load_postgres.py`가
마이그레이션된 스키마에 대고 돌면 preflight에서 죽거나, 더 나쁘게는 낡은 컬럼 집합으로 덮어쓴다.
**§5-1-6(사본 삭제)이 이 도입의 선행 조건이다.**

### 9-6. 이 도입이 막는 사고 (리더가 요구한 셋 + 하나)

| # | 사고 | 막는 장치 |
|---|---|---|
| ① | drizzle 스키마와 실제 DB가 어긋나도 아무도 모름 | **B2 diff** — 한 번으로 즉시 드러남. 이후 `check`가 계속 봄 |
| ② | zod가 응답을 절반만 기술해 화면이 자기 타입을 만듦 | `createSelectSchema` 파생 + §5-2-9 |
| ③ | 컬럼 추가 시 두 곳을 손으로 맞춤 | `schema.sql` **폐기**. `migrate`가 유일 경로 |
| ④ | *(추가)* 컬럼·테이블이 조용히 사라짐 | 9-3 파괴 가드 + 보호 목록. **`push` 삭제** |

## 10. 스스로 표시하는 약한 부분

0. **1차 설계가 틀렸던 지점 (critic이 잡음, 수용):**
   (가) 어휘 생성만으로는 사고 ①을 못 막는다 — 값 목록 검사는 `name_rule`산 101,043건을 전부 통과시킨다.
   §1-0(값+출처)을 새로 세워 덮었지만, **이건 내가 놓쳤던 것이고 같은 종류를 또 놓쳤을 수 있다.**
   (나) 3단계(코드북 전환)는 `mark_done` 때문에 **실행 불가능한 계획**이었다. §5-0으로 정정했다.
   **두 오류 다 "적재 경로의 현재 상태를 안 보고 계약만 설계해서" 났다** — 이 프로젝트가 네 번 한 실수와 같은 모양이다
1. **`PDLC_NM` 근거가 픽스처 2행뿐이다.** 0-4 후반·A11·§5-3-17이 전부 여기 걸렸다. 측정 전 인용 금지
2. **A6에서 어느 정의가 옳은지 나는 안 골랐다.** `bids.valid`(원본 유도)와 `bid_rate < win_rate`(관측)가
   불일치할 수 있다. 고르는 건 data 좌석. 설계는 "하나여야 한다"까지만
3. **브랜디드 타입은 서버 SQL 문자열을 못 막는다.** `split_part(school_id,'|',1)`은 그냥 문자열이라 tsc가 못 본다.
   grep으로 잡아야 하는데 SQL 안이라 오탐이 많을 것이다
4. **`_contract.py` 생성 대 DB 어휘 테이블 — 실패3의 트레이드오프를 못 골랐다.** 이게 가장 약하다
5. **`schools.id`를 영구 별칭으로 남기면 A3의 절반만 해결된다.** 코드와 문자열키가 공존하는 동안
   "어느 것이 진실인가"가 다시 둘이 된다. §5-3-15의 폴백이 그 이중성이다.
   **의도한 부채이고, 갚을 날짜를 나는 못 정했다**
6. **A12(카피)·A19(내 값 vs 실제)는 강제 수단이 없다.** 사람이 지킨다고 썼다.
   그건 지금까지의 방식이고, 지금까지 여섯 번 실패했다
7. **비용 추정을 안 붙였다.** 1·2단계는 며칠로 보이지만 3단계(코드북 전환)는 규모를 모른다.
   5-0 측정 전엔 추정 자체가 허구라서 뺐다
8. **front 좌석 결과를 아직 못 받았다.** §3-1은 내 직접 조사분이다. 받으면 합친다
9. **`Sourced<T>`가 응답을 무겁게 만든다.** 필드마다 `{v, src}` 객체면 페이로드가 커지고
   기존 소비자가 전부 깨진다. 나는 **품목 하나에만** 적용하자고 제안한다(사고가 난 축).
   지역·금액까지 확대하면 과설계다. **어디까지 적용할지의 선은 내가 임의로 그었다 — 공격받을 지점이다**
10. **커버리지 예산이 개선을 막을 수 있다.** "직전보다 떨어지면 실패"는 정당한 하락
   (원본이 실제로 나빠진 달)도 실패로 만든다. 예외 경로가 필요한데 그 예외가 §6-2의 allowlist와 같은 운명이 된다
11. **A22(재파싱 가능성)를 자산 표에 넣었지만 설계는 안 했다.** `mark_done` 우회가
   수집 정합성을 깨지 않는지는 내가 판단할 수 없다 — 수집기를 안 읽었다. **data 좌석 몫으로 넘긴다**
12. **§8-5는 내 설계의 한계 자백이다.** 브랜디드 타입은 키에는 작동하고 알고리즘에는 작동하지 않는다.
   0-1을 잡는 건 grep 가드뿐인데 **grep은 §6-2에서 내가 "죽을 수 있다"고 쓴 바로 그 장치**다.
   즉 **0-1의 재발 방지는 이 설계에서 가장 약한 고리다.** 더 나은 안이 있으면 받겠다
13. **API 대장(§8-5)을 새로 제안했지만 생성 방법을 안 정했다.** NestJS 컨트롤러에서 응답 필드를 뽑으려면
   반환 타입이 먼저 붙어야 하는데(§5-2-9), 그건 이 대장이 없어서 미뤄지는 작업이다. **닭·달걀이다**
14. **react-query 전환의 규모를 안 셌다.** fetch 호출 55곳이고 그중 몇 개가 단순 치환인지 모른다.
   §5-2에 넣었지만 **2단계 전체를 몇 배로 늘릴 수 있다.** front 좌석의 규모 추정을 기다린다
15. **§8-4에서 내 1차 판단을 뒤집었다(런타임 파싱).** 뒤집은 이유는 타당하지만, **면제 기준을 "실측 ms"로만
   정하고 그 문턱값을 안 정했다.** 몇 ms부터 면제인가? 내가 못 정했다 — 실측 뒤에 정하는 게 맞다고 봤지만
   그 사이에 사람이 임의로 면제할 여지가 남는다
16. **drizzle 베이스라인 B5(멱등화)를 손으로 한다.** 생성된 SQL을 사람이 편집하는 단계가 하나 있고,
   그 편집이 틀리면 B6이 실패한다(안전하게 실패하지만 멈춘다). **자동화 못 했다**
17. **`push` 금지는 명령을 지우는 것이지 능력을 지우는 게 아니다.** `npx drizzle-kit push`는 여전히 된다.
   가드는 리포 안의 스크립트만 본다. **사람이 직접 치는 걸 막을 방법이 없다** — §6-2와 같은 한계다
18. **의제 3(NestJS 모듈 분할)에 의존이 생겼다.** Swagger 기반 API 타입 생성(§8-5)은 컨트롤러가
   954줄 인라인인 채로는 값이 안 산다
19. **A27(엔드포인트 선택)을 막는 장치가 "배치"뿐이다.** 타입도 가드도 이 축을 못 본다.
   `lib/queries/*`에 모으면 눈에 띈다는 것이 전부다. **가장 약한 장치이고, 대안을 못 찾았다**
20. **사고 4·5·7 축("값이 사라진다")을 이번 개정에서 넣었지만 늦었다.** 577줄을 쓰고 나서
   critic이 지적해야 보였다. **§10-0의 자기진단이 또 맞았다** — 나는 계약을 보고 경로를 안 본다.
   구조적 교정을 못 찾았다: 다음 설계에서도 같은 편향이 나올 것이다
21. **가드를 4개로 줄이면서 `LEDGER-CHECKED`(§4-2)가 자동 검사에서 빠졌다.** §4의 재발 방지가
   **사람 리뷰로 내려갔다.** 이건 축소가 아니라 후퇴다 — 오탐 없는 검사 방법을 못 찾았다
22. **"원본에 답이 있다"는 가설을 두 번 연속 틀렸다** — `PDLC_NM`을 품목으로 봤고, 그다음 소재지로 봤다.
   둘 다 ⚠ 덕에 피해가 0이었지만, **내 가설 생성이 한 방향으로 편향돼 있다는 신호**다:
   나는 "원본에 있을 것"을 과대평가한다. 이 문서의 출발점(*"원본에 이미 있는 걸 몰랐다"*)이
   **반대 방향의 과잉으로 나를 밀고 있다.** 교정 방법을 못 찾았다 — ⚠를 계속 다는 것이 전부다
23. **A30의 문턱 다섯 곳 중 넷이 미측정이다.** 규모를 모른 채 "걷어내라"고 썼다.
   걷어내면 늘어날 뿐이라 위험은 낮지만, **시장 지도의 `< 10`은 걷어내면 표본 10 미만 칸이
   지도에 뜬다** — 화면이 그걸 어떻게 말할지는 front가 정해야 한다. **내가 정하지 않았다**
24. **실패4를 발견했지만 대응이 "기다려라"뿐이다.** 백필 정지를 기다리면 이번 주가 지나가고,
   안 기다리면 판정이 무효다. **내가 고를 수 없는 선택을 사장·리더에게 넘겼다** — 설계가 결정을 미룬 것이다
