# 2026-09-02 현재 상태 — **지금 참인 것 한 장**

> **team-lead 작성. 오늘 수치가 문서 여섯 개에 흩어졌고 정정이 겹쳤다. 이 문서가 목차이자 정본 요약이다.**
> 🔴 **각 줄에 *누가 쟀는가* 를 적는다. `[직접]` = team-lead 가 직접 계산하거나 독립 재현. `[보고]` = 팀 보고, 미재현.**

## 0. 사장 질문 둘 — 둘 다 답이 나왔다

```
1차  "우리 모델이 그 사람보다 잘 낙찰하는가"     ✅ 그렇다.  적합 밖에서 확인
2차  "그 범위를 사용자한테 줄 수 있는가"          ✅ 그렇다.  확률이 맞는다
```

## 1. 🔴 채점 기계 — 모든 수치의 토대 `[직접]`

```
낙찰 ⟺ x >= R  AND  경쟁자 중 [R, x) 에 아무도 없음   (자기 자신만 제외)
전수 10,586,855 투찰 · 233,380 회차에서 **기록 낙찰자 99.92% 재현**
동점 규칙(관대/엄격) 바꿔도 결론 불변
```
**⟹ 아래 모든 대결 수치가 이 기계로 나왔다. 재현 코드는 `tools/m1/run_m5_rerun.py` 안에 있다.**

## 2. 방법 비교 — 적합 밖 20만 투찰 `[직접]`

| | 낙찰률 | 비고 |
|---|---:|---|
| 사람 | 2.423% | |
| 제비뽑기 `1/N` | 2.437% | ⚠ **항등식.** 아래 §5 |
| 상수 (학습기 선택 `x0=0.9960`) | 3.395% | 정보 0 |
| `M4` GBDT | 3.586% | |
| `M5` 신경망 (예산맞춤) | **0.759%** | 🔴 퇴화. §4 |
| ✅ **`M1` 구조식** | **4.088%** | |

### `N` 층 — 🔴 **제품이 서는 자리**

| `N` | n | 사람 | 제비뽑기 | 상수 | `M4` | `M1` | `M1` 이 이긴 폭 |
|---|---:|---:|---:|---:|---:|---:|---|
| 3–20 | 27,162 | 10.647% | 10.657% | **17.020%** | 16.961% | 18.655% | +9.6% (상수 대비) |
| 21–40 | 14,556 | 3.380% | 3.610% | **5.015%** | 5.572% | 6.767% | +34.9% |
| **41+** | 158,282 | **0.924%** | 0.919% | 0.909% | 1.108% | **1.342%** | 🔴 **+45.2%** (사람 대비) |

> 🔴 **`N41+` 에서 상수(0.909)가 사람(0.924)보다도 낮다. 싼 대안이 없는 유일한 구간이고, 아버지 중앙 `N`=51 이 거기다.**
> ⚠ **`N 3-20` 에서 `M4`(16.961)가 상수(17.020)보다 낮다. GBDT 는 저`N` 에서 값을 못 한다.**

⚠ **낙찰 시 평균 `x`: 상수 −1.014% · `M4` −0.896% · `M1` −1.195%. 여전히 싸게 이긴다.**

## 3. ✅ 보정 — 승률을 화면에 낼 수 있다 `[직접]`

**적합 밖 235만 투찰 전수:**
```
전체 예측 2.4881% vs 실제 2.4318%  →  편차 +0.056%p
N>=21 에서 비 0.995 / 1.005 / 1.004   ← 오차 0.5% 이내
N 3-10 만 5.2% 과대 (17.50 vs 16.63)  ← 문구 필요
Murphy: 신뢰도 0.000027 ≈ 0 · 분해능은 불확실성의 7%
```
> 🔴 **"누가 이길지는 못 맞히지만 확률은 정직하다." 예측기가 아니라 계기판이다.**

## 4. `M5` 신경망 — **예산이 아니라 외삽** `[직접]`

```
40만 행 → 1.25%      150만 행 → **0.759%**     ⟹ 자료를 늘리니 더 나빠졌다
D1  고른 x 의 60.38% 가 격자 최저점 0.9600 (실제 투찰 1% 분위는 0.98381)
D2  평평한 게 아니라 **단조 감소**.  봉우리를 못 배웠다
D3  ✅ 보정은 좋다 — 관측 구간에서는 확률을 제대로 낸다
```
> 🔴 **판정 안 함(퇴화). "이 격자에서 이 구성은 못 쓴다"까지만.**
> **⟹ 그리고 이것이 `M1` 의 우위를 설명한다 — 표현력이 아니라 *데이터 없는 곳에서의 행동*이다.**

## 5. ⚠ 기준선 — **`N` 이 정한다** `[직접]`

```
🔴 항등식:  Σ(1/N) = 233,380.0 = 회차 수 = 실제 낙찰
   ⟹ 풀링 제비뽑기는 인구 평균과 **정의상 같다**.  결과로 쓰면 안 된다
✅ 그런데 사업자 개인에는 안 걸린다 — (실제−기대) 표준편차 47.06 낙찰 · |차이|>5 가 1,917명
   ⟹ 사업자 실력 측정(z 분포)은 항등식과 무관하고 그대로 유효하다
```
| `N` | 제비뽑기 | 상수 | 센 쪽 |
|---|---:|---:|---|
| 3–40 | 낮음 | 높음 | 상수 |
| **41+** | **1.626 / 0.672%** | 1.607 / 0.642% | 🔴 **제비뽑기** |

> **규율 38(정정): 이득은 *제비뽑기와 상수 중 높은 쪽* 대비로 적는다. 둘 다 표에 넣는다.**

## 6. 아버지 — 기준 사용자 `[직접 재현]`

```
투찰 3,926 · 참여 2,068 회차 · 낙찰 75 · 중앙 N=51
🔴 사업자 둘로 한 회차에 두 번 넣는다 (둘 다 넣은 회차 1,858 = 89.8%)
   한 사업자는 회차당 정확히 1건 — 10,586,855 쌍 전부, 예외 0

기대 (사업자 2개)               73.26
기대 (사업자 1개, 필드도 N−1)    39.56
⟹ 산술 이득 1.852배 · 산술 이상 1.024 (z=+0.21, 아님) · **낙찰의 46% 가 이 전략**
```
🔴 **대결 채점의 비대칭: 사람은 두 자리, 모형은 한 자리. §2 의 `M1 +45.2%` 는 자원 절반으로 낸 수치다.**
⚠ **규정 미확인 — "사업자를 하나 더 만드세요"는 화면에 안 나간다. 측정했고 권하지 않는다.**

## 7. 실력의 정체 `[보고]` — 🔴 **핵심 주장은 §11 에서 `[직접]` 확인됨**

```
실력은 실재한다        전수 z 분포 sd=2.41 · 전체 상관 rho=+0.241 (n=2,267)
❌ 극단 상위로는 못 집는다  상위 1% 16명 중 6명만 지속 (p=0.45).  한 명이 낙찰의 82%
🔴 무엇이 실력인가       **위치다**
     s1 표본 0.0004 · (a) 선택 0.0075 · **(b) 위치 0.1966** · (c) 시각 0.0128
     ⟹ (b) 가 설명력의 94.8%.  과합병 최악에서도 88.8%
     계수: 회차 내 x 백분위 0.954 · x−R 0.975  ⟹ **"낮게, R 에 바짝"**
⚠ 축 16개가 21% 를 설명.  과거 성적만으로는 35% — 아직 못 보는 게 더 많다
```

## 8. ⚠ 바뀌지 않는 제약

```
표본 외 봉인 없음        HOLD 소진 · 자료 2026-08 종료.  "적합 밖"이지 "새 봉인"이 아니다
균형 아님               현 인구에 대한 최적반응.  모두가 쓰면 이득이 사라진다
다중 사업자 식별 불가     공동출현이 아버지를 z=+96 로 탐지하나 같은 니치 남남과 못 가른다
                       (실패는 민감도가 아니라 **특이도**)
화면 입력 범위 제한 필요   사람이 안 넣는 금액을 물으면 외삽이고, M5 가 거기서 무너졌다
```

## 9. 오늘 철회한 것 — 남겨 둔다

```
❌ "상위 1% 는 제비뽑기의 2.1배"      한 명이 82%.  집단으로 지속 안 함
❌ "M1 이 사람을 65~74% 이긴다"        대부분 상수의 몫.  실제 +13~20%
❌ "사람 = 제비뽑기" (실력 없음 근거)   항등식
❌ team-lead "109승 p=0.00996"       86개 격자 중 최댓값
❌ team-lead "고N 에서만 값을 한다"     실제 최대는 N 21-40
❌ team-lead "표 2배로 R 을 당긴다"     부록 C: 슬롯에 가격 정보 없음.  방향 없다
❌ team-lead "기준선은 상수다"          N41+ 에서는 제비뽑기가 더 세다
❌ ml-expert "M5 는 예산 부족"          예산을 늘리니 더 나빠졌다
```
> **⟹ 여덟 개 중 넷이 내 것이다. 전부 *재보고* 뒤집었지 논쟁으로 뒤집지 않았다.**

## 10. 문서 지도

```
2026-09-02-constant-baseline.md      상수 기준선 · 부록 A(전수 검증) · 부록 B(규율 38 정정)
2026-09-02-m5-rerun-result.md        M5 재실행 · 진단 · 성능
2026-09-02-calibration-outfit.md     보정 — 2차 질문
2026-09-02-baseline-user.md          아버지 기록
2026-09-01-ml-review.md  개정 35     N 층 × 상수 (ml-expert)
2026-09-01-data-inventory.md AJ·AK·AM  z 분포 · 회귀 · 공동출현 (data-inventory)
2026-09-02-PREREG-m5-rerun.md        M5 사전 등록
```

---

## 11. 🔴 "실력은 위치다" — 독립 확인 `[직접]`

> **§7 의 중심 주장을 16축 회귀 없이, 완전히 다른 방법으로 다시 쟀다.**
> **`data-inventory` 는 포아송 회귀 + 축 16개. 나는 두 변수 + 순위상관. 시간 분할은 같다.**

```
위치는 **정의기간**(~202512) · 성적은 **측정기간**(202601~) · 대상 2,250 사업자
배수 = 낙찰 / Σ(1/N)   ⟹ N 의존을 정규화
```

### 결과 — 주장과 일치

```
회차 내 x 백분위  →  나중 배수    rho = **−0.2389**   p=1.5e−30
x − R 평균       →  나중 배수    rho = **−0.1572**   p=6.5e−14
⟹ 둘 다 음수.  **낮게 놓을수록 나중에 더 낙찰한다**
```

| 5분위 (정의기간 위치) | n | 회차 내 백분위 | 측정기간 배수 |
|---|---:|---:|---:|
| Q1 가장 낮게 | 450 | 0.2819 | **1.1372** |
| Q2 | 450 | 0.4299 | 0.9793 |
| Q3 | 450 | 0.5074 | 0.9813 |
| Q4 | 450 | 0.5801 | 0.9106 |
| Q5 가장 높게 | 450 | 0.7416 | **0.8458** |

> 🔴 **5분위 전부 단조. Q1 → Q5 가 1.137 → 0.846 으로 34% 벌어진다.**

### 🔴 대조가 결정적이다

```
평균 log N (어느 회차를 고르나)   rho = −0.0177   **p = 0.40**   ← 아무것도 아니다
정의기간 투찰 수 (활동량)         rho = −0.0052   **p = 0.81**   ← 아무것도 아니다
```

> **⟹ 위치 `p=1e−30` vs 선택 `p=0.40`.**
> **⟹ `data-inventory` 의 `(b) 0.1966` vs `(a) 0.0075` 가 전혀 다른 방법에서 그대로 재현됐다.**
> **⟹ §7 을 `[보고]` 에서 올린다. 중심 주장은 확인됐다.**

⚠ **다만 확인된 건 *방향과 크기 순서*이지 회귀의 계수값이 아니다. 축간 상관·감쇠·영업권 이동은 그대로 한계다.**
⚠ **그리고 상관이지 인과가 아니다 — "낮게 놓으면 이긴다"와 "이길 수 있는 사람이 낮게 놓는다"를 이 자료로 못 가른다.**

**재현: `scratchpad/position_check.py`**
