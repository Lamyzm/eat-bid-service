# `M1` 보정 — **승률을 화면에 보여줄 수 있다**

> **2026-09-02 · team-lead 직접 측정 · `tools/m1/calib_outfit.py`**
> **적합 밖 `TUNE+HOLD` **235만 1,664 투찰** 전수. 표본 추출 없음.**
> 🔴 **`ADR 0027` 조건 2("불확실성을 같이 낸다 · 보정 상태를 수치로 병기")가 요구하는 측정이다.**

## 0. 왜 별도 질문인가

```
오늘 확인한 것   argmax 성능 — "우리가 고르면 더 이긴다"    ✅ M1 +45.2% (N41+)
🔴 이 문서       보정 — "1.3% 라고 말하면 진짜 1.3% 인가"
⟹ 다른 요구다.  잘 고르는 모형이 확률을 틀리게 말할 수 있다
⟹ 그리고 **화면에 나가는 건 후자다**.  ADR 0027 이 추천가를 경계 밖에 둔 이유
```

## 1. 🔴 결과 — 맞는다

```
전체   예측 2.4881%   실제 2.4318%   편차 **+0.0563 %p**
```

### `N` 층별 — **제품이 서는 자리에서 특히 좋다**

| `N` | n | 예측 | 실제 | 편차 | 비 |
|---|---:|---:|---:|---:|---:|
| 3–10 | 123,921 | 17.5019% | 16.6324% | +0.8695 | 1.052 |
| 11–20 | 194,615 | 6.9321% | 6.8284% | +0.1037 | 1.015 |
| 21–40 | 171,372 | 3.5982% | 3.6167% | −0.0185 | **0.995** |
| 41–80 | 374,467 | 1.6555% | 1.6474% | +0.0081 | **1.005** |
| 81+ | 1,487,289 | 0.7373% | 0.7343% | +0.0030 | **1.004** |

> 🔴 **`N>=21` 에서 오차 `0.5%` 이내다. 그리고 `N41+` 이 제품이 서는 자리다.**
> ⚠ **저`N`(3–10)에서 `5.2%` 과대추정. 절대값이 17% 대라 `+0.87 %p` 는 화면에서 보인다. 그 구간엔 문구가 필요하다.**

### 십분위 (예측 확률로 정렬)

```
 1   0.1240% → 0.3861%   −0.26      6   1.1368% →  1.0027%   +0.13
 2   0.3452% → 0.5923%   −0.25      7   1.3940% →  1.1851%   +0.21
 3   0.5488% → 0.6727%   −0.12      8   1.8463% →  1.6478%   +0.20
 4   0.7407% → 0.7897%   −0.05      9   3.6488% →  3.8581%   −0.21
 5   0.9278% → 0.8798%   +0.05     10  14.1685% → 13.3041%   **+0.86**
```
⚠ **최하위 두 구간이 과소(−0.26/−0.25), 최상위가 과대(+0.86). 양 끝이 안쪽으로 눌린다 — 전형적인 회귀 편향이고 방향이 예측 가능하다.**

## 2. Murphy 분해

```
Brier 0.021843 = 신뢰도 0.000027 − 분해능 0.001663 + 불확실성 0.023727
기술점수 (1 − Brier/불확실성) = **0.0794**
```

```
🔴 신뢰도 0.000027   ≈ 0.  **말한 확률이 맞는다**
⚠ 분해능 0.001663   불확실성의 7%.  **누가 이길지는 거의 못 맞힌다**
```
> 🔴 **⟹ 이 둘이 같이 성립하는 게 핵심이다: *"누가 이길지는 못 맞히지만, 확률은 정직하다."***
> **그게 판단 재료의 정의다. 예측기가 아니라 계기판이다.**

## 3. ⟹ 제품 판정

```
✅ 사장 2차 질문("그 범위를 사용자한테 제공할 수 있는지")의 **전제가 통과했다**
✅ ADR 0027 조건 2 를 만족할 수치가 생겼다 — 구간별 보정 상태를 그대로 병기하면 된다
⚠ 저N(3–10) 문구 필요 — "이 구간은 실제보다 약 5% 높게 나옵니다"
⚠ 양 끝 눌림 — 아주 낮은/높은 확률은 안쪽으로 치우친다.  화면에 적는다
```

## 4. 한계

```
⚠ 적합 밖이지만 **새 봉인은 아니다** (HOLD 소진 · 자료 2026-08 종료)
⚠ 실제 투찰 x 에서 잰 것이다 — 사용자가 **사람들이 안 넣는 금액**을 물으면
   그건 외삽이고, M5 가 거기서 무너진 걸 오늘 봤다.  화면 입력 범위를 제한해야 한다
⚠ 균형 아님 — 인구가 바뀌면 F_X 가 바뀌고 보정도 낡는다.  재측정 주기가 필요하다
```
