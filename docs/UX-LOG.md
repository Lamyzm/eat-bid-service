# UX 검증 로그 — 상설 사용자 검증관(ux)

매 배포마다 같은 사람이 같은 일을 하러 온다는 가정으로 화면을 다시 본다.
전문가 눈이 아니라 **쓰는 사람 눈**으로만 판정한다. 코드는 건드리지 않는다.

---

## (a) 페르소나

**기본 페르소나 — "김해 축산 62세 사장"**

| 항목 | 내용 |
|---|---|
| 하는 일 | 김해에서 학교급식 축산 납품. 사업자 2개(신성유통·오뚜기축산) |
| 컴퓨터 | PC로만. 엑셀은 쓴다. 웹 서비스는 낯설다 |
| 모르는 말 | 중앙값·분포·표본·리허설·리플레이·앰버·동가 — 통계/영어 용어 전부 |
| 아는 말 | 투찰·개찰·기초가·하한율·밀림·낙찰·무효 (조달 실무 용어) |
| 급한 것 | **"얼마 쓰노"** 하나. 나머지는 다 곁가지 |
| 읽는 법 | 표를 먼저 본다. 그림은 참고. 긴 설명문은 안 읽는다 |
| 실수 반응 | 눌렀는데 결과가 다르면 다시 안 누른다. 물어보지도 않는다 — 그냥 안 쓴다 |

**보조 페르소나(필요 시)** — "서울 공산 45세 사장": 참여 80곳 판, 여러 학교 동시 투찰, 속도 우선.

---

## (b) 표준 과제 3개 (매 라운드 동일하게 재실행)

| # | 과제 | 성공 판정 |
|---|---|---|
| ① | 내일 마감 공고에 **얼마 쓸지 정하기** | 넣을 금액을 스스로 정하고 저장까지 끝냄 |
| ② | 지난달에 **왜 떨어졌는지** 알아보기 | 진 이유(밀림/무효)와 차이를 말로 설명할 수 있음 |
| ③ | **이번 달 납품** 확인 | 이번 달 납품 건수·금액을 찾아냄 |

각 과제마다 기록: 성공/부분/실패 · 클릭 수 · 스크롤 판수 · 중도 포기 여부 · 막힌 지점.

---

## (b-2) 검증 방식 규약 (2026-08-28 강화)

**손으로 만지는 검증만 인정한다.** 아래는 매 라운드 의무.

| # | 규약 | 이유 |
|---|---|---|
| 1 | `take_snapshot`으로 접근성 트리를 먼저 잡고, **uid 기반 `click`/`fill`/`hover`로 실조작**. `evaluate_script`로 상태를 직접 건드리는 우회 금지 | 실사용자와 다른 경로를 타면 검증이 무의미. 버튼이 실제로 눌리는지까지가 검증 |
| 2 | 과제 시작 `performance_start_trace` → 종료 `performance_stop_trace`. **LCP·인터랙션 지연 기록** | "느려서 못 쓰겠다"도 UX 결함이다 |
| 3 | `list_console_messages` + `list_network_requests` — 실패 요청·**1초 이상 느린 API**를 잡아 사장 체감과 연결 | 화면이 멈춘 것처럼 보이는 이유는 대개 뒤에 있다 |
| 4 | 화면마다 `take_screenshot(fullPage)` 증거 보존 + **"몇 판을 스크롤해야 하는지" 수치 기록** | 길다는 인상을 숫자로 |
| 5 | `emulate`로 **1920 / 1280 두 폭 비교** (PC 우선이되 노트북 1280에서 잘리는 것 확인) | 사장 책상은 1920, 아들 노트북은 1280 |

`evaluate_script`는 **읽기 전용 계측**(스크롤 판수 계산, DOM 존재 확인)에만 쓴다. 조작은 언제나 uid로.

**측정값 기록 형식**: 과제표에 `LCP 1.8s · 최장 인터랙션 320ms · 느린 API 1건(rounds 1.4s)`처럼 병기.

---

## (c) 라운드 0 — 기준선 (2026-08-28, 커밋 df247ed 기준)

### 과제 결과

| 과제 | 결과 | 클릭·스크롤 | 막힌 지점 |
|---|---|---|---|
| ① 얼마 쓸지 | **실패(치명)** | 2클릭 + 스크롤 3판 | 계산기가 화면 두 판 아래. 판단 근거를 못 읽음. 저장 버튼이 기록을 삭제 |
| ② 왜 떨어졌나 | **부분 성공** | 2클릭 + 스크롤 2판 | 3단 스택은 이해. 월 지정 불가, 같은 학교 두 줄의 정체 불명 |
| ③ 이번 달 납품 | **성공** | 1클릭, 3초 | 없음 |

### 결함 (치명 3 · 상 4 · 중 3)

| ID | 심각도 | 결함 | 사장의 말 |
|---|---|---|---|
| U1 | 치명 | 투찰함 버튼이 토글 — 저장하려고 누르면 기록이 삭제됨 | "저장할라꼬 눌렀디만 없어져뿟네? 내 아까 넣은 거 어데 갔노?" |
| U2 | 치명 | 같은 공고에 금액 3개(8,389,260 / 8,394,387 / 8,393,921)가 라벨 없이 병존 | "어느 돈을 넣으라는 소린지…" |
| U3 | 치명 | 분석판 기초금액이 그 공고 금액이 아님(9,321,400 vs 7,889,600) | (모르고 틀린 금액으로 계산할 위험) |
| U4 | 상 | "밀림5·낙찰5·무효8"을 해석 못 함 | "재생이 뭐꼬? 비디오도 아이고. 무효가 8이면 이 값 쓰면 안 된다는 소린가?" |
| U5 | 상 | 같은 학교 회차 수가 화면마다 다름(18회 vs 20건) | "어느 게 맞노" |
| U6 | 상 | 내 성적 첫 화면에 "무효(하한미달) 578회"가 빨간 큰 글씨, 설명은 맨 아래 각주 | "내가 578번을 헛짓했다고? 이거 맞나?" |
| U7 | 상 | 모르는 말 산재 — 리허설·리플레이·앰버·중앙값·동가·재생·분포·5%/95% | "이게 뭔 소린지…" |
| U8 | 중 | 계산기가 화면 두 판 아래 (읽을거리가 먼저, 할 일이 나중) | "돈 얼마 넣는지부터 보여주면 될 낀데" |
| U9 | 중 | 투찰 내역에 사업자 구분 없음 — 같은 날 같은 학교 두 줄 | "내가 두 번 넣었나? 어느 게 우리 회사꺼고?" |
| U10 | 중 | 첫 문장이 남의 동네("최다 전주시"), 지역 기본값 "전체 지역" | "내는 김해 사람인데 왜 맨 위에 전주 얘기가 뜨노" |

**기타(하)**: 납품 브레드크럼 영어("홈 / delivery") · 달력 전부 노란색이라 경고처럼 보임 · 분석판 저장 버튼 회색 이유 불명 · "1건/1건/1건" 세 곳 반복 · "추천가는 없습니다"가 "그라믄 내보고 우짜라고"로 읽힘.

### 딱 하나만 남긴다면
- **오늘**: 마감 임박 카드 1장(학교·마감·넣을 금액 한 줄)
- **공고 상세**: 투찰 계산기를 맨 위로 — 지금은 순서가 정확히 거꾸로
- **내 성적**: 3단 스택 표 (KPI "무효 578회"는 설명 없이는 공포)
- **납품**: 정산 표 — 손댈 것 없음

### 라운드 0 이후 조치 (리더 결재분)
- IA 3안 적용(커밋 60b3b1f): 분석판 공고 컨텍스트 주입 → U3 해소 예상, 몰림 중복 제거
- front 작업 중: U1(저장 토글) · U2(금액 라벨) · U4(리허설 문장화) · U8(계산기 최상단)

---

## (d) 라운드 기록 양식

```markdown
## 라운드 N — YYYY-MM-DD (커밋 XXXXXXX)

### 회귀 확인 (직전 라운드 결함)
| ID | 직전 상태 | 이번 상태 | 판정 |
|---|---|---|---|
| U1 | 치명 | ? | 해소 / 부분 / 미해소 / 악화 |

### 표준 과제
| 과제 | 결과 | 클릭·스크롤 | 체감 속도 | 직전 대비 | 막힌 지점 |
|---|---|---|---|---|---|
| ① 얼마 쓸지 | | ?클릭 · ?판 | LCP ?s · 최장 인터랙션 ?ms | 개선/동일/회귀 | |
| ② 왜 떨어졌나 | | | | | |
| ③ 이번 달 납품 | | | | | |

### 뒷단 관찰
- 실패 요청: (없음 / 목록)
- 느린 API(1초 이상): (없음 / 엔드포인트·소요)
- 콘솔: (에러 N · 경고 N)

### 폭 비교 (1920 / 1280)
| 화면 | 1920 | 1280 | 잘림·깨짐 |
|---|---|---|---|

### 새 결함
| ID | 심각도 | 결함 | 사장의 말 |
|---|---|---|---|

### 딱 하나만 남긴다면
(화면별 한 줄)

### 총평
(2~3줄. 쓰는 사람이 이 화면을 계속 열겠는가에 대한 답)
```

**기록 규칙**
- 결함 ID는 U 접두사 + 통산 일련번호(재사용 금지). 해소돼도 번호는 남긴다.
- 사장의 말은 지어내지 말고, 실제로 막힌 지점에서 나올 법한 한 마디만 짧게.
- "고쳤다"는 front의 보고가 아니라 **내가 그 화면에서 과제를 다시 해봐야** 확정한다.
- 회귀(전에 되던 게 안 됨)는 새 결함보다 항상 심각도 한 단계 위로 본다.

---

## 라운드 1 — 2026-08-28 (커밋 b4fb1ca, 뷰포트 1280 기준)

### 회귀 확인 (라운드 0 결함)

| ID | 직전 | 이번 | 판정 | 증거 |
|---|---|---|---|---|
| U1 저장 버튼이 삭제 | 치명 | — | **해소** | 저장된 공고에서 갱신 클릭 → marks 유지(90.099→90.05로 갱신). `이 값으로 갱신` / `해제` / `✕` 3버튼 분리 |
| U2 금액 3개 라벨 없음 | 치명 | — | **해소** | "공고 기초금액" · "내가 넣을 금액(원)" · "하한 금액" · "내 투찰가"로 전부 라벨 붙음 |
| U3 분석판이 다른 공고 금액 | 치명 | — | **해소(구조)** | 분석판 링크가 `?bidNo=&rate=&base=` 전달 (단 U14 참조) |
| U4 "밀림5·낙찰5·무효8" 해석 불가 | 상 | — | **해소** | "이 값이면 과거 18회 중: 남이 더 낮게 써서 밀린 게 5회 · 내가 먹었을 게 5회 · 하한 아래라 무효였을 게 8회". '재생' 용어 제거 |
| U5 회차 수 화면마다 다름 | 상 | ? | **미확인** | 이번 라운드 분석판 미방문 — 다음 라운드 확인 |
| U6 무효 578회 공포 | 상 | 상 | **미해소** | 내 성적 상단 그대로 빨간 큰 숫자, 설명은 여전히 맨 아래 |
| U7 모르는 말 | 상 | 중 | **부분 해소** | '재생' 사라짐. '전장'·'앰버' 잔존 |
| U8 계산기가 두 판 아래 | 중 | — | **해소** | 헤더 직하로 이동. 공고 화면 스크롤 3판 → **1.5판** |
| U9 사업자 구분 없음 | 중 | 중 | **미해소** | 투찰 내역 표에 사업자 열 없음 |
| U10 첫 문장이 남의 동네 | 중 | 중 | **미해소** | "최다 전주시(57건)", 지역 기본값 여전히 "전체 지역" |

**치명 3건 전부 해소.** 라운드 0의 가장 큰 함정(저장이 삭제되는 것)이 사장 손으로 눌러도 재현되지 않음.

### 표준 과제

| 과제 | 결과 | 클릭·스크롤 | 체감 속도 | 직전 대비 | 막힌 지점 |
|---|---|---|---|---|---|
| ① 얼마 쓸지 | **성공** | 2클릭 · 1.5판 | LCP 398ms · CLS 0.21 | **실패 → 성공** | 저장값이 입력칸에 안 채워져 있음(U13) |
| ② 왜 떨어졌나 | **성공** | 2클릭 · 2.08판 | LCP **1,554ms** · CLS **0.40** | 부분 → 성공 | 화면이 1.5초 늦게 뜸(U11). 지난달만 보는 필터 없음 |
| ③ 이번 달 납품 | **성공** | 1클릭 · 1판 | — | 동일 | 없음 |

과제 ②는 답을 찾을 수 있게 됐다(3단 스택 + 밀림/무효 + 차이). 표시 건수도 600캡이 풀려 1,283회 실측.

### 뒷단 관찰
- 실패 요청: **없음** (fetch/xhr 35건 전부 200/304)
- 느린 API(1초 이상): **없음**
- 콘솔: React #418 1건(W2, 기능 무해) · form id 경고 1건(C3)
- **DOM**: 내 성적이 1,383행 렌더 → 접근성 트리 24,609줄(1MB). LCP·CLS 악화의 직접 원인

### 폭 비교 (1920 / 1280)

| 화면 | 1920 | 1280 | 잘림·깨짐 |
|---|---|---|---|
| 전 화면 사이드바 | 글자 보임 | **아이콘만(32px)** | 라벨 없음 — U12 |
| 공고 상세 | 계산기·기록 여유 | 동일 배치 | 없음 |
| 착지 차트 | **오른쪽 절반 텅 빔** | 눌려 보임 | 이상치 92.25까지 축이 늘어남 — U15 |

### 새 결함

| ID | 심각도 | 결함 | 사장의 말 |
|---|---|---|---|
| U11 | **상** | 내 성적 LCP 1,554ms · CLS 0.40 (DOM 1,383행). R6 캡 600→2,000 확대의 부작용 | "눌렀는데 한참 있다가 뜨네. 그라고 글자가 움직여서 누를라믄 딴 게 눌린다" |
| U12 | **상** | 1280(노트북)에서 사이드바가 아이콘만 — 글자 없음 | "이 그림들이 뭔지 우째 아노. 하나씩 눌러봐야 되나" |
| U13 | 중 | 저장한 값(90.05)이 다시 들어왔을 때 계산기 입력칸에 복원 안 됨. 버튼에만 표시 | "어제 얼마 넣었더라? 칸이 비었네" |
| U14 | 중 | 같은 분석판으로 가는 링크 3개 중 **1개만** 공고 컨텍스트를 들고 감 (`분석판 상세 →`·`참여 업체 전체 →`는 맨몸) | "아까는 금액이 맞더니 이번엔 딴 금액이네" |
| U15 | 중 | 착지 차트 x축이 이상치(92.25)까지 늘어나 정작 볼 구간(90.00~90.15)이 왼쪽 끝에 눌림 | "점이 다 왼쪽에 뭉쳐있고 오른쪽은 왜 이래 넓노" |
| U16 | 하 | 안내문은 "[투찰 저장]을 누르면"인데 실제 버튼은 "이 값으로 갱신" — 이름 불일치 | "저장하라 캐놓고 갱신이라 써놨네" |
| U17 | 하 | '전장 109,306건'·'3건 이상은 앰버' — 여전한 용어 벽 | "전장이 뭐꼬. 앰버는 또 뭐고" |

### 딱 하나만 남긴다면
- **오늘**: 마감 임박 카드 (히어로 "오늘·내일 마감 1건"은 자리 잡음)
- **공고 상세**: 투찰 계산기 — 이제 맨 위에 있다. 손댈 것 없음
- **내 성적**: 3단 스택 표 — 단 KPI의 "무효 578회"를 설명 없이 빨갛게 두는 건 여전히 겁준다
- **납품**: 정산 표 — 3라운드째 무결점

### 총평
치명 3건이 실제로 사라졌다. 사장이 "얼마 쓰노"를 **혼자 끝낼 수 있게 된 첫 라운드**다 — 계산기가 맨 위에 있고, 판단 근거가 한국말로 풀려 있고, 저장이 저장으로 동작한다.
남은 벽은 두 종류다. 하나는 **속도**(내 성적 1.5초·글자 밀림), 하나는 **여전히 남의 말**(전장·앰버·아이콘만 있는 메뉴). 둘 다 "쓸 수 있느냐"가 아니라 "계속 쓰겠느냐"를 가르는 것들이라 다음 라운드 최우선이다.

---

## 라운드 2 — 2026-08-28 (재적재 직후, 뷰포트 1280 기준)

### 회귀 확인 (라운드 1 결함)

| ID | 직전 | 이번 | 판정 | 증거 |
|---|---|---|---|---|
| U6 무효 578회 공포 | 상 | 상 | **미해소** | KPI에 "무효(하한미달) 578회" 그대로, 설명 없음 |
| U9 사업자 구분 없음 | 중 | 중 | **미해소** | 투찰 내역 표에 사업자 열 여전히 없음 |
| U10 첫 문장이 남의 동네 | 중 | 하 | **부분 해소** | 자격 지역 설정 시 발주 예정은 김해로 채워짐. 단 첫 문장은 여전히 "최다 전주시(57건)" |
| U11 내 성적 성능 | 상 | **치명** | **악화** | LCP 1,554ms → **2,009ms**, CLS 0.40 → **0.44** |
| U12 사이드바 아이콘만 | 상 | — | **해소** | 쿠키 삭제 후에도 expanded(239px) — 신규 사용자도 글자 보임 |
| U13 저장값 미복원 | 중 | — | **해소** | 재방문 시 계산기에 90.05 복원(금액 8,393,921 동반) |
| U14 링크 컨텍스트 1/3 | 중 | — | **해소** | 분석판 링크 3개 **전부** `?bidNo&rate&base` 전달 |
| U15 차트 x축 이상치 | 중 | ? | **미확인** | 이번 라운드 미검증 |
| U16 안내문/버튼명 불일치 | 하 | 하 | **부분 해소** | "값을 고치면 [이 값으로 갱신]이 뜹니다" 설명 추가. 단 안내문은 여전히 [투찰 저장] 표기 |
| U17 용어 벽 | 중 | — | **해소** | '전장'→"전국 최근 14일", '동가'→"같은 값", '앰버'→"노란색" |

라운드 1의 상급 2건 중 U12는 해소, **U11은 악화(치명 승격)**. 회귀는 새 결함보다 한 단계 위로 본다는 규칙 적용.

### 표준 과제

| 과제 | 결과 | 클릭·스크롤 | 체감 속도 | 직전 대비 | 막힌 지점 |
|---|---|---|---|---|---|
| ① 얼마 쓸지 | **성공** | 2클릭 · 1.46판 | LCP 729ms · CLS 0.19 | 동일(성공 유지) | 저장값 복원됨 — 막힘 없음 |
| ② 왜 떨어졌나 | **성공** | 1클릭 · 2.08판 | LCP **2,009ms** · CLS **0.44** | 성공이나 **더 느려짐** | 화면이 2초 늦게 뜨고 글자가 밀림 |
| ③ 이번 달 납품 | **성공** | 1클릭 · 1판 | — | 동일 | 없음 |

### 뒷단 관찰
- 실패 요청: **없음**
- 느린 API: 없음. 단 **`/api/schools/forecast`가 2회 호출**(무파라미터 + `?sigungu=김해시`) — U18의 원인
- 콘솔: form id 경고 1건뿐. **React #418 사라짐**(W2 해소)
- 다크 모드로 렌더됨(시스템 테마) — 라운드 1은 라이트. 두 테마 모두 판독 가능

### 폭 비교 (1920 / 1280)
| 화면 | 1920 | 1280 | 잘림·깨짐 |
|---|---|---|---|
| 사이드바 | 글자 보임 | **글자 보임(해소)** | 없음 |
| 납품 | 1판, 표 1,260px | 1판 | 브레드크럼 "홈 / delivery"가 1280에서 두 줄로 꺾임 |

---

### 신규 영역 첫 사용 (사장 눈으로)

#### (a) 로그인 / 로그아웃
- **로그아웃**: 데이터(사업자 2개·투찰 저장·자격 지역) **전부 유지**, 헤더에 "Google로 로그인" 노출. 게스트로 계속 쓸 수 있음 — 설계 좋음
- **로그인**: 버튼 한 번에 완료(구글 세션 재사용), 자료·자격 지역 그대로. `eatbid.mergedFor`에 계정 ID가 남아 병합 처리 확인
- **결함 U18**: 로그아웃 순간 발주 예정이 **전국 잡탕으로 되돌아감**(서울하늘숲유치원·서울항동초…) — 자격 지역은 김해시 그대로인데도
> "로그아웃했는데 내 자료가 그대로 보이네? 남이 내 컴퓨터 켜면 다 보이는 거 아이가" (U24)

#### (b) 내 사업자 > 자격 지역 설정
- 위치·설명 명확("참가 자격은 사무소 소재지 기준입니다"), **클릭 한 번에 설정**, 헤더가 "내 자격 지역 · 김해시"로 즉시 바뀌고 발주 예정도 김해로 채워짐 — 이번 라운드 **가장 잘 만든 화면**
- **결함 U19**: "참여 이력이 있는 지역" 8개 중 3개가 쓰레기 — `급식소(경상남도`, `급식소`, **`기해시`**(오타). 김해시는 5번째
> "내 지역이 김해인데 왜 '급식소'가 지역으로 나오노? 기해시는 또 뭐꼬?"

#### (c) 납품 — "이번 달 뭐 넣어야 하나"
- 히어로 "8월 납품 계약 3건 · 합계 20,053,449원", 학교·기간·계약액·급식일수·상태·확인서 인쇄까지 한 판에 — **답이 나옴**
- **결함 U21**: 납품 달력 칸이 숫자만. 클릭도 툴팁도 없어 그날 **어느 학교**인지 알 수 없음
> "16일에 세 군데 가야 되는 건 알겠는데, 어느 학교고?"
- **결함 U22**: 8월 31일 중 **22일이 경고색** — 경고가 일상이 되어 의미를 잃음
- 품목이 "축산" 한 단어뿐 — 진짜 "뭐"(품목 상세)는 없음. 데이터 한계일 수 있으나 화면 제목이 답을 약속한 것처럼 읽힘

### 새 결함

| ID | 심각도 | 결함 | 사장의 말 |
|---|---|---|---|
| U18 | **치명** | 로그아웃 시 발주 예정이 전국으로 되돌아감. `forecast` 무파라미터 호출과 `?sigungu=` 호출이 경쟁, 늦게 온 쪽이 화면을 덮음 | "아까는 우리 동네만 나오더니 왜 또 서울이 뜨노" |
| U19 | **상** | 자격 지역 후보에 쓰레기 3개(`급식소(경상남도`·`급식소`·`기해시`) | "기해시는 또 뭐꼬" |
| U20 | **상** | (U11 악화 재기록) 내 성적 LCP 2,009ms·CLS 0.44 | "눌러놓고 딴 짓 하다 와야겠네" |
| U21 | 중 | 납품 달력이 숫자만 — 그날 어느 학교인지 모름 | "16일에 세 군데라는데 어디고?" |
| U22 | 중 | 납품 달력 경고색이 22/31일 — 경고가 일상 | "온통 노란데 뭐가 위험한 기고" |
| U23 | 하 | 브레드크럼 "홈 / delivery" 영어 잔존 + 1280에서 두 줄 꺾임 | "델리버리가 뭐꼬" |
| U24 | 하 | 로그아웃해도 자료가 그대로 보임 — 공용 PC 프라이버시 불안 | "로그아웃했는데 왜 그대로고" |

---

### 💰 돈 내고 계속 쓸 것인가 (3주째 매일 열 이유가 있는가)

**매일 바뀌는 것**: 오늘 첫 문장(전국 개찰 수) · 진행 중 공고 · 발주 예정 목록
**주 1~2회 바뀌는 것**: 내 성적(개찰 후) · 동가 이력
**월 1회면 되는 것**: 납품 정산 표

**냉정한 판정**: 매일 열 이유는 사실상 **"오늘 마감 공고"** 하나다. 오늘 그게 **1건**이었다. 김해 축산 사장에게 하루 0~1건이면, 공고 없는 날은 열 이유가 없다.

**지루해지는 지점**: 공고 0건인 날 오늘 화면은 사실상 빈 화면이 된다. "발주 예정"이 그 공백을 메우려 하지만 *"예상 도래 · 지난 회 낙찰 90.17"*만 나열될 뿐 **할 일이 없다** — 읽고 끝난다.

**돈을 계속 내게 하려면 필요한 것**(우선순위):
1. **공고 없는 날의 할 일** — 발주 예정에 "지금 미리 값 정해두기"(예약 투찰값)를 붙이면 빈 날에도 손이 감
2. **알림** — 매일 열지 않아도 놓치지 않는다는 안심. 역설적이지만 "매일 안 열어도 되게" 만드는 게 해지를 막음
3. **납품 달력의 실용화**(U21) — 어느 학교 가는지가 뜨면 납품 화면이 월 1회에서 **매일 아침 확인용**으로 바뀜. 지금 구조에서 가장 싼 승격

### 딱 하나만 남긴다면
- **오늘**: 오늘·내일 마감 카드
- **공고 상세**: 투찰 계산기 (2라운드째 손댈 것 없음)
- **내 성적**: 3단 스택 표 — KPI "무효 578회"는 여전히 겁준다
- **내 사업자**: 자격 지역 고르기 — 쓰레기 3개만 빼면 완성
- **납품**: 정산 표 (달력은 아직 장식)

### 총평
치명 3건(U1·U2·U3)이 라운드 1에서 잡힌 뒤로 **결정 흐름은 안정**됐다. 이번 라운드의 성과는 U12·U13·U14·U17 해소로 **군더더기 마찰이 사라진 것**이고, 자격 지역 설정은 이 제품에서 가장 잘 만든 화면이다.
그러나 새 치명 1건(U18 로그아웃 시 지역 필터 붕괴)과 **성능 악화(U20)**가 생겼다. 특히 성능은 라운드 1에서 지적했는데 더 나빠졌다 — 규칙대로 회귀는 한 단계 위로 본다.
가장 큰 물음은 결함이 아니라 **"내일도 열 이유"**다. 오늘 마감 1건짜리 화면으로 3주를 버티게 하려면, 공고 없는 날에 할 일을 주거나(예약 투찰값) 납품 달력을 실용화해야 한다.
