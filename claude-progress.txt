## 2026-08-29 (1) — 기초공사로 좁힌 날: 재파싱까지 가는 길 [master]

사용자가 두 번 물었다. **"리포가 2개라고? 뭘 일을 어떻게 한 거임?"** 그리고
**"계속 문제 생기는데 이거 기초공사 아직 안 함?"** 둘 다 맞는 지적이었다.

답은 **안 했다**였다. 전수감사·규칙·설계문서는 다 나왔는데 **시공이 0이었다.**
`region_src='code'` 가 0.00% 였다 — 파싱은 코드를 뽑는데 아무도 안 썼다.

그래서 **좌석을 늘리지 않고 줄였다.** 여섯이 각자 값진 걸 찾았지만
**찾는 속도가 고치는 속도를 넘었다.** 셋만 남기고 셋을 세웠다 —
`data`(재파싱 단독) · `critic`(독립 검증) · `front`(화면). 나머지는 대기.

### 이번에 세운 방법 — 게이트

오늘 **"됐다"가 나중에 안 된 걸로 드러난 게 여섯 번**이다(아래 참조).
전부 **"만들었다"와 "돈다" 사이**에서 났다. 그래서 재파싱은 이렇게 건다:

1. **판정 기준을 숫자 보기 전에 못박는다** (`docs/GATE-A-CRITERIA.md`)
   — 나중에 숫자를 보고 기준을 만들면 그건 기준이 아니라 변명이다
2. **실행자와 검증자를 가른다** — `critic` 이 같은 것을 **자기 명령으로** 다시 잰다.
   같은 쿼리를 쓰면 같은 실수를 한다. **두 숫자가 다르면 그게 곧 발견이다**
3. **화면을 따로 본다** (`docs/CHECK-AFTER-REPARSE.md`)
   — 게이트 A 는 레이크·DB 기준이다. **DB 초록은 통과가 아니다**

### 고친 것

- **[x] 마감이 마감이 아니었다** — `BID_END_DT`(마감)와 `OPNG_DT`(개찰)가 표본 1,200건에서
  **같은 적이 0번**이다. 우리 `deadline` 은 개찰에서 왔다. **화면의 `마감 1시간 전`은 이미
  마감된 공고였고, 사장이 투찰을 놓칠 수 있는 종류다.** 라이브 111건 실측: 60분 99 · 10분 8 ·
  30분 3 · **2910분(48.5시간) 1**. **`deadline - 1h` 로 보정했다면 12건이 틀리고 그중 하나는
  이틀 가까이 틀렸다** — 고정 오프셋 금지의 근거가 숫자로 남았다.
- **[x] 급감 가드가 스스로 꺼져 111건이 1건이 됐다** — `prev_n` 을 못 읽으면
  `and prev_n and` 가 falsy 라 **가드가 실패한 게 아니라 꺼졌다.** 2026-08-27 22:00 UTC
  무인. **로더의 0건 방어는 0 에만 걸려 1 을 막지 못했다 — 두 겹이 같은 원인 하나로 뚫렸다.**
  열린 공고 화면이 **6시간 47분** 동안 1건이었고, 발견도 알림이 아니라 사람이 로그를 뒤져서였다.
  **`비교할 게 없다`는 `정상`이 아니다.**
- **[x] 차단 정지 장치가 한 번도 발동한 적이 없었다** — `internal.py` 가 정지 신호를 던지고
  `_post` 가 일부러 다시 던지는데, **한 층 위 `except Exception` 이 그걸 "실패한 공고 하나"로
  처리했다**(`RuntimeError` 는 `Exception` 이다). 증거: `crawl-guard.json` 트립 4건이 전부
  `stopped=false`. **그리고 트립 사유의 절반이 p90 이었다 — 우리가 실패한 게 아니라 소스
  응답이 0.98s → 2.23s 로 느려진 것이다. 소스가 이미 반응하는데 우리는 지연 배수만 키우며
  계속 두드렸다.** `docker-compose` 의 `restart: unless-stopped` 도 지웠다 — 자동 재시작이
  후퇴 배수를 1.0 으로 되돌려 이 수정을 무력화한다.
- **[x] `update_lake`·`backfill` 이 매일 데이터를 버리고 있었다** — `EATBID_DATA_ROOT` 를
  무시하고 `/app/data` 에 썼다. **`fetch_open` 은 이 함정을 고치고 경고 주석까지 달아놨는데
  같은 파일 둘만 안 고쳤다.** `e19f880` 이 시도 코드에서 같은 파일 하나를 빠뜨린 것과
  **같은 파일, 같은 형태**다. 그리고 `state.sqlite` 도 거기라 **클러스터에서 재개가 작동한
  적이 없다 — 16개 시도 × 35일을 매일 처음부터 받았다.**
- **[x] 학교 링크가 이중 인코딩으로 죽어 있었다** — Next 16 은 라우트 세그먼트를 디코드하지
  않고 주는데 한 라우트가 "이미 디코드됐다"고 가정했다. **낙찰 속보와 오늘 발주 예정의 학교
  링크가 전부 `학교를 찾지 못했습니다`.** 두 라우트의 주석이 서로 반대로 적혀 있었다 —
  **주석이 둘이면 하나는 틀린다.** 창구를 `lib/school-id.ts` 하나로 모았다.
- **[x] `.catch(() => {})` 24 → 5곳** — 실패의 등급이 셋으로 갈렸다:
  **조용함**(사장이 모른다) < **거짓 단언**(`없습니다` — 잘못 안다) < **영구 로딩**
  (`오는 중입니다` — **시간을 버린다**). 온보딩 첫 화면이 실패하면 영원히 로딩이었다.
- **[x] 축산 사장이 김치 섞인 구간을 보고 있었다** — 서버가 `byCat` 을 응답의 19.9%로 보내는데
  웹이 받아서 버렸다. 첫 카드가 전 품목으로는 `90.19~90.29 · 5회`인데 **축산만 보면
  `90.72~90.83 · 1회`** 다. 구간도 표본도 완전히 다르다.
- **[x] 지역 목록이 신설 지역을 잘라내고 있었다** — 하드코딩이 아니라 **우리가 붙인 `n >= 20`
  문턱**이었다. 142 → 152 지역. **회차가 적은 것은 사실이고, 사실을 숨기는 대신 `n` 을 같이
  내려보낸다.**
- **[x] raw 백업이 처음 생겼다** — 231,994 파일 / 2.18GB 를 H:(F: 와 **물리적으로 다른
  디스크**)로. `state*.sqlite` 는 파일 복사 대신 `conn.backup()` — **쓰는 중에 복사하면
  찢어진 사본이 나오고 그건 조용하다.** 그리고 스냅샷 README 가 **두 시각이 어긋난 방향**을
  따졌다: `state` 가 `data` 보다 오래됐다 → **state 는 실제보다 적게 아는 쪽으로만 틀린다.**
  반대였다면 안 받은 걸 받았다고 착각해 영구 누락이다.
- **[x] CI 가 0 개였다 → 고리가 실제로 돌았다** — `git push → 테스트 → 빌드 → GHCR →
  매니페스트 태그 커밋 → ArgoCD 적용` 까지 초록. **다만 푸시 트리거가 안 걸려 수동 실행이
  필요했다** — 그건 아직 "사람이 손으로 하는 단계"로 남아 있다.

### 이번에 실측으로만 잡힌 것

- **`region_src='code'` 전체 4.79% 인데 최근 3개월이 통째로 0%** 였다. 평균 하나만 봤으면
  "조금은 되네" 로 넘어갔다. **비율은 전체 평균이라 특정 구간이 0 이어도 가려진다** —
  연·월로 쪼개는 보조 검사가 첫 라운드에서 값을 했다.
- **`firm_bids` 축이 검증 목록에서 빠져 있었다.** 로더가 주소 유래 `a.sigungu` 를 그대로
  COPY 한다. 오염이 `schools` 59건인데 **`firm_bids` 는 64,537행**(형식 불통과는 411,696행).
  **`schools` 만 고치면 게이트는 초록인데 사장의 성적 화면은 안 바뀐다.**
- **학교의 40.4%가 화면에 없다.** 등식으로 확정: `그룹 9,390 = built 6,708 + skip 2,682`,
  `built 6,708 = loaded 5,596 + conflict 1,112`. **둘 다 성립 → 넷째 원인은 없다.**
  그리고 **손실의 절반(1,919)이 우리가 만든 문턱**(`낙찰회차 5미만`)이다.
- **레이크는 노드 안에 없다.** `/lake` = `F:\Project\eat-bid\data`. `k3d cluster delete` 를
  해도 raw 도 parquet 도 안 없어진다. **반대로 노드와 함께 죽는 건 Postgres 다** —
  위험이 정확히 반대로 있었다.
- **PVC 가 `2Gi` 인데 안에 5,478MB 가 들어 있다.** `local-path` 는 용량을 강제하지 않아
  **그 숫자가 아무것도 막지 않았고 아무 소리도 안 냈다.**
- **재수집은 선형이 아니다.** `crawl-guard.json` 이 하루 **네 번 트립**을 기록했고 지연이
  **4배**까지 올라갔다. 소스가 부하에 반응해 우리를 늦춘다. **워커를 늘려도 가드가 상쇄한다.**
- **압축은 풀어야 준다.** 개별 `.gz` 를 그대로 tar 하면 절감 1%, **풀어서 tar+xz -3 이면 52%**
  (1초). `-9e` 는 67% 지만 샤드당 52초라 256샤드에 3.7시간이다.
- **`schoolId` 문자열 안에 파싱 쓰레기가 박혀 있다** — `급식실|혜성여자고등학교`. 열린 공고
  5건이 그 id 를 쓴다. 컬럼만 보면 안 보인다.

### 내가 만든 사고

- **실측표에 전제를 빠뜨렸다.** "풀어서 tar+xz" 를 재고 표엔 방식만 적었다. 좌석이 그대로
  구현해 **절감 1%** 를 받았고, dry-run 왕복 시험이 없었으면 그걸 올리고 성공이라 보고했다.
- **`724행` 을 축 없이 여러 번 말했다.** 재보니 `schools` 68 · `school_auctions` 1,310 ·
  `firm_bids` 411,696 — **어느 축과도 안 맞았다.** 폐기했다. → **모든 숫자에 축과 측정 시각.**
- **"백필 40시간" 을 세 번 사실처럼 인용했다.** 근거가 없었다. 분모와 속도로는 14시간인데
  가드 트립으로 더 길다. **`TOT_CNT` 를 파싱하기 전까지 아무도 모른다.**
- **`region_src=0` 을 버그로 오해했다.** 내가 잰 재적재가 새 코드보다 먼저 시작된 것이었다.
  좌석이 **`region_src` 가 NULL 이라는 것**으로 갈랐다 — 코드가 돌았다면 못 찾아도 `'address'`
  가 들어간다. **값의 부재와 코드의 부재는 다르다.**
- **스테이징 잔여물이 무관한 커밋에 딸려 들어갔다.** `.githooks` 커밋에 파싱 마이그레이션이
  섞였다. `git add -A` 가 아니라 **실패한 커밋이 남긴 스테이징**이었다 — 새 변종이다.
- **모든 실패가 `LoadError` 를 지난다고 단언했다.** 실측하니 `LoadError` 5파일 vs
  `.catch(() => {})` 24곳. **삼켜진 실패는 거기 도달조차 안 한다.**
- **두 번째 PC 를 백업 대상으로 셌다.** 사용자가 짚었다 — 배포 컴은 **같은 코드가 도는
  상관된 기계**라 백업이 아니다. 오늘 사고 대부분이 디스크가 아니라 우리 코드였다.

### "있는데 안 도는" 여섯

오늘 같은 형태가 여섯 번 났다. **만들었다와 돈다 사이가 이 프로젝트의 주된 사고 지점이다.**

| 장치 | 상태 |
|---|---|
| pre-commit 훅 | 리포에 없어 클론하면 안 돎 |
| 배포 스탬프 | 서버는 `BUILD_SHA=unknown` |
| 크롤 정지 장치 | 4번 트립, `stopped:false` — 한 번도 발동 안 함 |
| 급감 가드 | 꺼진 채로 111→1 통과 |
| 지도 좌표 | 커밋했는데 배포가 안 됨 |
| `docker stop -t 30` | `sh -c` 가 신호를 안 넘겨 SIGKILL(137) |

### 다음

- **재파싱 진행 중** (`--out-dir data/parquet --force`, 약 76분). 끝나면 **적재 전에 멈추고**
  레이크 `sigungu_code` 채움률부터 잰다. **5.76% 에서 안 오르면 적재하지 않는다** —
  그 상태로 적재하면 아무것도 안 바뀐 채 27분을 쓴다.
- **게이트 A** — 세 사람이 각자 잰다. 통과하면 단계 2(`schools.id` 동결 · 자격 판정 ·
  `canon_sgg` 제거), 아니면 멈춘다.
- **`daily-refresh` 크론이 정지돼 있다**(`suspend: true`). **재파싱 끝나면 되돌려라.**
- 문턱 1,919 · 로깅 · 코드북 · R2 토큰 · 터널은 게이트 A 뒤다.

---

## 2026-08-28 (3) — 키 재감사 · 24시간 폴링 · 코드북 원칙 [master]

사용자가 방향을 두 번 더 좁혔다: **"문자열이 키인 거 전부 다시. 그 축으로"** →
**"우리가 하는 건 문자열 파싱이 아님. 정부가 이미 잘 내려주는 하나의 테이블이고
우리는 리버싱해서 예쁘게 만들면 된다"** → **"모든 값은 중앙에서 오프셋처럼 관리해야 함.
DB도 마찬가지. 이미 정부의 베스트 프랙티스임."**

이 셋이 오늘 흩어져 있던 처방을 하나로 묶었다. `CLAUDE.md` 맨 앞에 원칙으로 박았다.

### 고친 것

- **[x] 로더가 100% 죽고 있었다** — `a[15]` 인데 회차 원장은 15컬럼(0~14)이다. `group by` 의
  15 가 1-based 라 착각이 났다. **어제 17:34 이후 성공한 적재가 없었고**, `purr_cd` 0/4,700 과
  `rsd` 잔존이 둘 다 이걸로 설명된다. 검증 Job 을 돌려 **학교 5,641곳 · 투찰 913만 행까지
  통과**를 확인했다. 원장을 위치 인덱스로 읽는 자리가 41회라 `COL` 이름표를 세웠다.
- **[x] `fetch_open` 이 수집 결과를 통째로 버리고 있었다** — `EATBID_DATA_ROOT` 를 무시하고
  `/app/data` 에 쓰는데 로더는 `/lake` 를 읽는다. **매일 아침 전국을 돌아 받아온 걸 아무도
  안 읽는 곳에 쓰고 exit 0 으로 끝났다.** 지금까지 열린 공고가 갱신된 건 사람이 손으로
  돌렸기 때문이다. X13 이 "적게 읽음"을 막았다면 이건 "제대로 읽고 엉뚱한 데 씀"이다.
- **[x] 지역 좌표를 생성물로** — 좌표표가 `시도|시군구` **이름**으로 키를 만들어 표기 변이를
  따라 만들어져 있었다(`서울` 3개·`전북특별자치도` 1개). **`전라남도` 는 아예 없어 93행이
  지도에서 조용히 사라졌다.** 표준 중심점에서 생성해 136키 → 736키, 커버리지 96.4%.
  인천 5개 구는 **2017 스냅샷에 없어 빈칸으로 둔다** — 영종구는 영종도라 모구 좌표를 찍으면
  바다 건너에 점이 찍히고, **지도는 위치를 단언하는 매체**라 사용자가 그걸 사실로 읽는다.
- **[x] 배포 스탬프** — `CLAUDE.md` 배포 절이 줄바꿈이라 **앞이 실패해도 뒤가 돌았다**(CronJob
  `;` 사고와 같은 것을 배포 문서가 갖고 있었다). 그리고 `&&` 로도 절반만 막힌다 — 빌드가
  **성공했는데 내용이 낡은** 경우엔 태그에 옛 이미지가 남아 import·rollout 이 정상 동작한다.
  `BUILD_SHA` 를 구워 배포 끝에 대조한다. 도커 다이제스트는 로컬(config)과 파드(manifest)가
  계산 방식이 달라 **항상 불일치**하므로 대조에 못 쓴다(실측).
- **[x] 24시간 폴링** — 공고 등록의 **94.2%가 09~18시**, 새벽 0~6시는 3,295건 중 3건(0.1%),
  주말 0.8%. 그래서 **평일 주간 30분 · 야간·주말 3시간**으로 24시간 돌되 헛돌지 않는다.
  증분화로 아는 공고는 상세를 건너뛰고(신규 없으면 0회), `--open-only` 로 수 초에 끝난다.
  요청률 +0.2%. **급감 판정은 폴링에서 뺐다** — 개찰이 09~11시에 몰려 아침 폴링에서 40%
  급감이 정상적으로 발생한다. 그대로 켰으면 매일 아침 늑대소년이었다.
- **[x] 원본 변화 감지** — 100% 필드는 18개월 진폭 **0.0%p** 라 1건만 빠져도 신호로 잡아도
  오탐이 없고, 부분 채움 필드는 진폭 **62.8%p** 라 감시에서 아예 뺐다. 🔴는 적재를 멈추고
  🟡은 기록만 한다. **멈추는 경로를 수집이 아니라 적재에 뒀다** — 수집을 멈추면 raw 를 잃는다.
  배포 전에 **오탐 네 종류**를 잡았다(시군구 파싱 실패·0행 블록·기준선 캐시·표본 민감성).
- **[x] 재료 없는 공고가 침묵했다** — 열린 공고의 35%가 과거 이력이 비었는데 조건부 렌더링이라
  통째로 빈칸이었다. `이 학교는 지난 개찰 기록이 아직 없습니다` 로 사실만 말하고, **이유는
  지어내지 않았다**(수집이 덜 됐는지 첫 발주인지 우리는 모른다). "없음"과 "로딩 중"을 갈랐다.
- **[x] `summary=1` 이 100% 500** — `any(${bizNos})` 가 배열을 문자열로 바인딩. 웹이 `.catch`
  로 삼켜 KPI 가 조용히 비어 있었다.

### 키 재감사 — 세 번째 전수 감사

**`AUDIT-KEYS.md`** 를 새 축("이 키에 코드가 있는가")으로 만들었다. 앞선 두 감사에 없던 게
둘 나왔다 — **좌표표**와 **품목 코드 부재**.

| 축 | 판정 |
|---|---|
| 학교 `PURR_CD` | 🔴 100% · 이름충돌 0 — 문자열 키 사용 중 |
| 시군구 `SIGUNGU_CD` | 🔴 188종 · **여러 시도에 걸친 것 0** — 추출조차 안 함 |
| 시도 `SIDO_CD` | 🔴 16종 |
| 자격지역 `PDLC_CD` | 🔴 169종 · **정규화 후 충돌 0. 코드가 표기 변이를 흡수한다** |
| 업체 `SHIPPER_CD` | ⚪ **키 금지** — 더 잘게 쪼갠다(한 사업자번호 → 두 코드 23건) |
| 품목 | 🟢 **코드가 없다.** 이름이 유일한 식별자 |
| `MLFD_CLASS_CD` · `areas[].sgg_cd` | 🟡 **함정** — 형식은 코드인데 함수가 아니다 |

### 이번에 실측으로만 잡힌 것

- **품목에는 코드가 없다.** `ds_mainItemlist` 컬럼이 `ELCTRN_BID_ID`·`MAIN_ITEM_NM` 둘뿐이다.
  `MLFD_CLASS_CD` 는 `_CD` 접미사·두 자리 숫자·높은 채움으로 **형식 검사를 전부 통과하는데**
  `00` 이 `농산물`·`공산품`·`수산물`을 동시에 가리킨다. **함수인지 세어보지 않으면 안 보인다.**
- **우리는 공고의 92.9%만 받는다.** `P_PRGRS_STAT_CD=007`(낙찰) 고정. 서울 2026-07 실측
  169 vs 157 — 유찰 3·공고취소 8·저장중 1 을 안 받았다. **유찰은 재입찰 사슬의 앞쪽 절반**이다.
- **투찰의 9.5%가 철회**됐고 낙찰자 철회 공고가 8.0%다. 다만 **7a 반례는 설명 못 한다** —
  투찰 단위로 대조군을 두고 재니 10.2% vs 9.7% 로 차이가 없었다.
- **좌표 소스도 시간축을 탄다.** 2017 스냅샷이라 2026-07 인천 개편으로 생긴 구가 없다.
- **워커가 이미지가 아니라 마운트된 `src/` 를 직접 읽는다.** 배포 통제가 생각과 다르다.

### 내가 만든 사고 (이번 회차)

- **채움률만 보고 `sgg_cd` 로 자격을 배선하라고 지시했다.** 실측하니 고유 쌍이 33개뿐이라
  시군구를 식별 못 하는 `MLFD_CLASS_CD` 급 함정이었다. **우리가 규칙으로 박아둔
  `코드→이름 함수` 검사를 내가 건너뛰었다** — 규칙을 만든 사람이 그 규칙을 안 지켰다.
- **`CLAUDE.md` 에 재현 안 되는 숫자를 적었다.** `MLFD_CLASS_CD` "60개 순번·45개"가
  어느 계산으로도 안 나온다. 판정은 맞지만 근거를 못 대는 숫자를 문서에 두면 다음 사람이
  인용한다 — `5광주 13전남` 주석으로 6만 건을 놓친 것과 같은 구조다. 숫자를 뺐다.
- **`CLAUDE.md` 배포 절을 줄바꿈으로 썼다.** 아침에 CronJob `;` 를 `&&` 로 고쳐놓고
  저녁에 같은 함정이 든 문서를 내가 새로 만들었다.

### 다음 — 코드북 중앙화

사용자 원칙: **"모든 값은 중앙에서 오프셋처럼 관리. DB도 마찬가지. 공유해야 한다."**
지금은 같은 개념이 다섯 곳에서 재정의된다(시군구만 4곳). 그래서 한 곳을 고치면 다른 곳을
빠뜨린다 — 오늘 시도코드가 그랬다.

코드북을 DB 테이블로 두고 사실 테이블은 코드만 저장하며 이름은 조인으로 표시할 때만 얻는다.
그러면 **이름이 바뀌어도 코드북 한 행만 고치면 전 화면이 따라온다.** `SPEC-CODEBOOK.md` 작성 중.

**미해결**: 지도 194행 중 127개만 그려진다(좌표는 187행에 붙는데 60행이 안 그려짐, 원인 미상) ·
학교 4곳 분리 · 3키 병합 · 유찰·취소 미수집 7.1% · JSON 배열 원자화 · 자격이 아직 이름 비교.

## 2026-08-28 (2) — 전수검사 → 기초공사 전환 [master]

사용자가 대전제를 정했다: **"전수검사 후 기초공사 기간 돌입."** 그리고 방식의 결함을 짚었다 —
*"학교를 문자열로 봐? id 다 있지 않냐"* / *"문자열인 부분 전부 잘못될 확률이 높음. 고치고 있는데
또 고치게 만들면 어캄"* / *"우리가 모르는 필드가 있나, 필드가 있는데 제대로 사용 안 하는가
두 축으로"*. 감사 좌석 셋을 소환해 파이프라인 단계로 갈랐다.

산출: `docs/AUDIT-SOURCE.md` · `AUDIT-PIPELINE.md` · `AUDIT-SERVE.md` · `AUDIT-STRINGS.md`
· `PLAN-REFACTOR.md`(기초공사 계획) · `.claude/rules/`(4파일) · `CLAUDE.md` · 이 파일

### 감사가 판을 바꾼 것

- **재파싱이 필요한 게 훨씬 적다.** 레이크에 **이미 있는데 서빙이 안 읽는 것**이 많다 —
  `institution_code` 100% · `sido_code` 100% · **`areas[].sgg_cd` 97.8%** · **`bids.valid` 100%** ·
  `rank`·`bid_at`·`is_qualification_review` 100%. **A-8·A-9·자격 코드 대조·판정 8곳 통합·시장지도
  PK가 전부 재파싱 없이 닫힌다.** 8/30까지 기다릴 이유가 사라졌다.
- **우리는 공고의 92.9%만 받는다.** `P_PRGRS_STAT_CD=007`(낙찰)로 고정해 왔는데 **빈 값을 주면
  전체가 온다.** 서울 2026-07 실측: 전체 169 vs 우리 157 — 유찰 3·공고취소 8·저장중 1을 안 받았다.
  **유찰은 재입찰 사슬의 앞쪽 절반**인데 레이크에 없다. 재파싱으로 못 채운다 — **재수집**이다.
- **투찰의 9.5%가 철회됐는데 모른다.** `WITHDRAWAL_YN` 100% 채움, **낙찰자 철회 공고 14,681건(8.0%)**.
  7a 반례(최저가 88.001이 떨어지고 96.614가 낙찰)가 **철회로 설명될 수 있다** — 그러면
  "무효는 원본에 없는 판정"이라는 판정이 뒤집힌다. 확인 중.
- **자격의 열쇠는 `PDLC_CD`다**(ds_areaList 100%, 통제 189종). 우리는 `PDLC_NM`만 뽑고 코드를 버린다.

### 고친 것

- **[x] 시장 지도가 도시를 뒤섞고 있었다** — `market_regions` PK가 `(sigungu, category)`라 시도가
  빠졌다. 집계는 `(sido,sgg,cat)`로 하면서 충돌은 시도를 빼고 봐서, **여러 시도에 같은 이름을 가진
  시군구 51개**가 품목당 1행으로 뭉개졌다. 라이브 실측: `남구` 6행이 공산=대구·축산=울산·
  농산=인천·기타=부산. **어느 도시가 살아남는지는 insert 순서가 정했다.**
- **[x] 증분 크롤만 옛 지역 목록이 남아 있었다** — `fetch_open`·`backfill`은 고쳐놓고 `update_lake`를
  빠뜨렸다. 열린 공고엔 전남·광주가 들어오는데 **개찰 이력은 매일 안 들어왔다** — 백필로 채워도
  다음 날 증분이 되돌리는 항구적 손실이었다. 이제 하드코딩된 지역 목록이 리포에 없다.
- **[x] `/api/firms/bids?summary=1`이 100% 500** — `any(${bizNos})`가 JS 배열을 문자열로 바인딩해
  `malformed array literal`로 죽었다. **웹이 `.catch`로 삼켜 KPI가 조용히 비어 있었다.**
- **[x] 시장 지도에 `'김해시'`가 박혀 있었다** — 모든 사용자가 김해시를 자기 자격 지역으로 봤다.
- **[x] 분석판이 못 찾으면 남의 학교를 그렸다** — `?? schools[0]`. URL은 A인데 화면은 B였다.
- **[x] `_SIDO_MAP`의 전북 갈림** — `전북특별자치도`만 자기 자신으로 매핑돼 두 값을 냈다.
- **[x] 테스트 배선** — TS 24개·파이썬 27개. 기준은 **"있었으면 우리가 실제 겪은 사고를 막았나"**
  하나. 픽스처는 전부 실측. `.githooks/pre-push`·`pre-commit`으로 배선하고 **일부러 깨뜨려
  실패하는 것까지 확인**했다.
- **[x] `CLAUDE.md`·`claude-progress.txt`·`.claude/rules/`** — mw-auction 릴레이 방식. **`CLAUDE.md`가
  아예 없어서 모든 세션이 맨바닥에서 시작했다.**

### 이번에 실측으로만 잡힌 것

- **`전남광주통합특별시`는 실재한다.** 2026-07-01 출범한 대한민국 최초의 광역 행정통합이고
  전라남도와 광주광역시가 **폐지**됐다. **내가 두 번이나 "eaT 자체 명칭"이라고 단정했다.**
  지식 시점 이후의 사실이라 몰랐던 게 아니라, **확인 없이 단정한 게 문제다.**
  그리고 **인천 자치구 개편도 같은 달**이다 — 행정구역 변경이 가설이 아니라 두 달 전에 두 건 났다.
- **`SHIPPER_CD`를 키로 쓰면 안 된다.** "사업자번호보다 안정적"이라 보고 바꾸려 했는데
  **방향이 반대였다** — 한 사업자번호 → 두 코드가 23/5,343이라 **더 잘게 쪼갠다.** 바꿨으면
  그 23곳 이력이 쪼개져 A-8과 같은 죄를 새로 지었다. 덤으로 **재발급 위험은 실재하지 않는다**(0/5,366).
- **`MLFD_CLASS_CD`는 코드가 아니다** — `_CD` 접미사·53% 채움·두 자리 숫자로 **형식 검사 셋을
  통과하고** 60코드 중 45개가 여러 이름을 가졌다. 품목 목록 안의 순번이었다.
- **eaT 코드와 표준 행정코드는 값이 완전히 다르다**(서울 eaT 1 / 표준 11, 시군구 뒷자리 일치 0).
  **다만 시도 집합은 16↔16 대응**이라 코드북 매핑은 성립한다. 시군구는 표준 목록에
  화성·용인·고양·전주가 없어 원인 확인 전까지 보류다.
- **`verify_live.py`는 한 번도 배선된 적이 없다.** 크론·스크립트·문서 어디에도 실행 참조가 없다.
  **"이 리포에 검증이 있다"고 믿으면 안 된다.**
- **`app/입찰판 새로고침.bat`은 아버지가 쓰는 진짜 경로다.** "코드에서 안 읽는다"와 "사람이 안
  쓴다"는 다른 얘기다 — 죽은 코드로 지울 뻔했다.

### 내가 만든 사고 (이번 회차)

- **`전남광주통합특별시`를 없는 이름이라 두 번 단정했다.** 근거는 "실제 행정구역에 없다"였는데
  확인을 안 했다. **오늘 세 번째 "확인 없이 단정"이다**(차단 이력 · 분석판 · 이것).
- **시도코드를 두 곳만 고치고 `update_lake.py`를 빠뜨렸다.** 그래서 손실이 항구적이었다.
  **한 곳을 고치면 같은 값을 쓰는 곳 전부를 찾아야 한다** — 규칙으로 박았다.
- **`activeDeadlineSeconds`를 CronJob spec 바로 아래 뒀다.** JobSpec 필드라 조용히 무시된다.
  커밋도 sync도 성공했는데 값만 안 들어갔다. **"조용한 실패를 막는 장치"가 그 자체로 조용히
  실패할 뻔했다** — 배포 후 라이브에서 읽어봐서 잡혔다.

### 다음 (순서 제약을 지켜라)

```
S-2(닫힘) ──► 재파싱
자격 코드 적재(sgg_cd) ──► ?region= 배선   ※ 역순이면 지역제한 공고가 0건이 된다
purr_cd 적재 + 카운터  ──► 문턱 제거        ※ 역순이면 문턱 제거가 A-8을 숨긴다
front 표기(작은 n)     ──► 로더 문턱 제거
7a 정의 확정          ──► 판정 8곳 통합
X20(purr_cd 키)       ──► 원자화
R10(zod 현행화)       ──► 웹 로컬타입 제거 ──► react-query
```

**미해결**: 의정부·강남 사장이 낼 수 있는 공고 65건을 "0건"으로 본다(웹이 `region`을 한 번도
안 보내 서버 판정이 실행되지 않는다) · 공고 상세가 조건 없이 `자격 충족`을 단다 · 학교 4곳 분리 ·
3키 병합 · 유찰·취소 미수집 7.1% · JSON 배열 원자화.

## 2026-08-28 — 리팩터링 주간 1일차 — 문자열 키·조용한 실패·수집 누락 [master]

UI/UX 고도화로 시작했다가 뿌리가 모래성이라는 게 드러나 기초공사로 전환했다.
사용자 지적 셋이 방향을 정했다: ①"학교를 문자열로 봐? id 다 있지 않냐" ②"문자열인 부분
전부 잘못될 확률이 높음. 고치고 있는데 또 고치게 만들면 어캄" ③"왜 이렇게 됐는지, 재발
방지하려면 어떻게 해야 하는지". 좌석 여섯(front·data·visual·architect·refactor-critic·sim)을
붙였고, 오늘 나온 라이브 결함 대부분이 서로 공격하는 과정에서 나왔다.

산출 문서: `docs/SPEC-SSOT.md`(1,533줄) · `docs/DEBT.md`(1,806줄) · `docs/PLAN-REFACTOR.md`
· `docs/SOURCE-FIELDS.md` · `docs/SPEC-SCHOOL-ID.md` · `docs/SPEC-REPARSE-2026-08-30.md`

### 막은 것 — 무인으로 도는 파괴 경로

- **[x] 열린 공고 전량 삭제 경로** — 로더가 목록 JSON 읽기 실패를 `except: oj=[]`로 삼킨 뒤
  **무조건 `delete from open_auctions`**를 실행하고 0건을 넣고 **정상 종료·커밋**했다. 매일 03시
  무인 크론이라 파일이 한 번 깨지면 그날 공고가 통째로 사라지고 아무도 모른다. 읽기 실패면
  기존 목록을 두고 건너뛴다. 0건일 때도 지우지 않는다.
- **[x] 낡은 레이크로 재구축** — CronJob이 `update_lake.py;fetch_open.py && load_postgres.py`.
  `;`라서 레이크 갱신이 실패해도 로더가 **낡은 레이크로 7개 테이블을 재구축**했다. `set -e`+`&&`.
- **[x] 실패를 덮는 뚜껑** — initContainer의 `psql ... || true`. 예전에 이 파일로 테이블을 전량
  날렸는데 그 파일에 실패를 못 보게 하는 뚜껑이 덮여 있었다. `ON_ERROR_STOP=1`.
- **[x] 스키마가 클러스터에 한 번도 안 닿고 있었다 (A31)** — `schema.sql`은 스스로 "라이브
  마이그레이션 병기"라며 ALTER를 쌓아왔다(`account.issuer`·`user_mark` PK 교체·`categories`
  전부). 그런데 적용 Job에 ArgoCD 훅이 없어 한 번 돌고 끝이었고, **`spec.template`이 immutable이라
  명령을 고치면 sync가 `field is immutable`로 실패**했다. 즉 새 컬럼을 추가해도 이미 도는
  클러스터엔 영원히 안 들어가고, 로더가 그 컬럼에 insert하면 7개 테이블이 통째로 빈다.
  지금까지 안 터진 건 누가 손으로 psql을 돌렸기 때문이고 **그 기록이 리포에 없다**.
  → `db-migrate` Job을 PreSync 훅으로 분리(4초). 적재는 넣지 않았다 — 그 스크립트는 delete로 시작한다.
- **[x] `initial-load` Job이 모든 배포를 영구히 막고 있었다** — 위와 같은 immutable 문제.
  역할이 `db-migrate`(스키마)와 `daily-refresh`(적재)로 나뉘었으므로 리포에서 제거.
- **[x] 크론 시간 상한 없음** — 주기 24시간인데 상한이 없으면 매달린 Job이 `Forbid` 정책 때문에
  다음 회차를 조용히 스킵시킨다. **스킵을 보이게 만드는 게 아니라 못 일어나게 만드는 게 옳다** —
  데드라인(3시간)이 주기보다 짧으면 스킵 조건 자체가 사라진다. `failedJobsHistoryLimit` 1→5,
  `startingDeadlineSeconds` 3600(미설정이면 스케줄 100회 놓쳤을 때 CronJob이 아예 멈춘다).
- **[x] 산출물 truncate-then-write** — `open(OUT,"w")`가 한 바이트도 쓰기 전에 기존 파일을 0으로
  자른다. 데드라인 킬이 실재하게 됐으므로 tmp+fsync+replace로. 정답이 같은 리포 `archive.py`에 있었다.
- **[x] X13 부분 실패 감지** — 16지역 중 15개가 죽어도 "유효한 JSON"이 나오고 크론의 `set -e`도
  못 잡는다(성공으로 끝나기 때문). 실제로 `open_auctions: 1`이 들어간 날이 있었고 아무도 아무 말을
  안 했다. 지역 실패 카운터 + 직전 대비 50% 급감 시 비영 종료. 임계 근거는 공고 수명 분포 실측
  (수명 1일 이하 0.8% → 자연 감소로 -50%는 불가능).

### 고친 것 — 화면이 하던 거짓말

- **[x] 시도코드 18 누락 — 58,459건을 통째로 안 받고 있었다** ★ 최대 소득
  `fetch_open.py`가 `range(1,18)`로 코드를 만들어 **존재하지 않는 5·13을 호출**하고 0건을 받았고,
  정작 **18(전남광주통합특별시, 58,459건 — 서울 다음, 경기보다 많다)**을 빼먹었다.
  원인은 `backfill.py` docstring의 `5광주 13전남`. **코드는 config를 쓰는데 주석이 없는 코드를
  가르쳤고**, 그 주석을 믿고 "광주·전남은 소스에 없다"고 확정 보고까지 했다.
  → `config.REGION_CODES`가 유일한 진실. 전 코드(0~20) 탐침으로 16개가 전부임을 확정.
- **[x] 시장 지도에 `'김해시'`가 코드에 박혀 있었다** — 모든 사용자가 김해시를 자기 자격 지역으로
  보고 `★ 내 자격 지역`까지 붙었다. 아버지 지역이라 맞아 보였고 그래서 살아남았다.
- **[x] 분석판이 못 찾으면 남의 학교를 그렸다** — `schools.find(...) ?? schools[0]`. URL은 A인데
  화면은 B였고 오류도 안 났다. 학교 키가 갈리거나 오염된 경우가 전부 이 폴백으로 흘러들었다.
- **[x] 자격 판정이 부분문자열** — `서구`가 `강서구`·`달서구`에, `북구`가 `강북구`·`성북구`에 걸려
  인천 서구 업체가 대구 달서구 공고를 "자격 충족"으로 봤다. 완전일치로 좁혔으나 **이것도
  임시방편이다** — 자격은 이름이 아니라 `SIGUNGU_CD`로 비교해야 한다.
- **[x] "무효"는 우리가 지어낸 판정이었다** — `valid`는 원본 필드가 아니라 우리 계산값이고,
  원본은 `낙찰`/`낙찰실패` 2값만 준다. 반례: 회차 5509883은 "88% 이상 최저가" 방식인데
  **최저가 88.001이 떨어지고 96.614가 낙찰**됐다. 우리 두 정의 중 어느 것도 설명 못 한다(2,145회차).
  → 숫자는 그대로 두고 부르는 말만 바꿨다. `무효`(판정) → `하한 아래`(위치). 세 화면에 출처 고지.
- **[x] 발주 예보 알고리즘 2벌** — 서버와 웹이 각각 구현했고 매직 넘버 네 개까지 똑같이 베껴졌는데
  **기준 시각 하나만 달라**(로컬 자정 vs 현재 시각) 같은 학교의 D-day가 하루 어긋났다.
- **[x] "오늘"이 4종** — UTC 2 + 로컬 1 + 웹 1. 매일 09시 이전에 하루 어긋났다. `todayKST` 신설.
- **[x] 목록 실패가 "공고 없음"으로 보였다** — 서버가 죽은 것과 공고가 없는 것은 다르다.
- **[x] 저장 실패 은폐** — `put()`이 `res.ok`를 안 봤다. 인증이 만료돼도 화면은 저장된 것처럼
  보였고 다른 기기에서 열면 없었다. 실패를 말하고 `pendingSync`로 자동 재전송한다.
- **[x] 알림 뱃지가 가짜를 세고 있었다** — 스토어가 `mockNotifications`로만 초기화되고
  `addNotification` 호출처가 **0곳**. `unreadCount`는 배선돼 있었지만 세는 대상이 가짜였다.

### 재료를 정확하게 — S-1(품목 오염) 계열

- **[x] 품목 3층 분류기** — 1층 구조화 필드(`MAIN_ITEMS` 통제어휘) > 2층 이름 규칙 > 3층 무분류.
  **2층의 "첫 매치 우선"을 폐기하고 모든 매치를 수집**한다. 그래야 키워드를 늘려도 다른 품목을
  훔치지 않는다 — 첫 매치 방식으로 키워드만 늘렸을 때 **200건을 훔쳤고, 다중 매치로 바꾸니 퇴행 0**.
  기타 6.1%→1.2%. 아버지 업체의 "기타" 34건이 전부 축산으로 제자리를 찾았다(육가금류였다).
- **[x] 다중 품목** — 회차 14,704건(10%)이 다중인데 대표 하나만 남기고 버렸다. `categories` 병기,
  필터는 포함 기준(공산 필터 결과 +14,253건 복원), 집계는 대표 기준. **둘 다 정직하고, 틀린 건
  기준을 안 밝히는 것**이라 `countBasis`를 응답에 실었다.
- **[x] S-1 배선** — `by_cat_floor`·`school_roster_cat`(1,094,514행)을 로더가 만드는데 **서버·웹
  읽기 0회**였다. 계산이 아니라 배선이 남아 있었다. 임호초 로스터 1위가 `더클래식 32회`(혼합)
  → **`호야축산 24회`**. 인덕과기고 축산 표본 45회 → **7회**, 평균 90.412 → 90.233.
- **[x] `rsd` 제거** — 아무도 안 읽는데 매 적재마다 계산·저장되고 문턱까지 달려 있었다.
- **[x] 표본 표기** — `이 학교 축산 15회` · `하한 90 45회 중 71%`. 표본이 줄어든 자리를 화면이
  말하지 않으면 정확해진 대신 신뢰도를 오해하게 된다.

### 사용자 데이터

- **[x] 사업자 축 — 입력의 46%가 저장조차 안 됐다** — `user_mark` PK가 `(userId, bidNo)`라
  회차당 값 하나만 담겼는데, 사장은 **2개 사업자로 605회차를 중복 투찰**한다(같은 값 쓴 적
  0/605, 차이 중앙값 0.220, 상관 0.913 — 학교마다 중심 하나 잡고 독립 지터). PK를 확장하고
  오늘 카드에 2칸을 나란히. **복사·오프셋 자동화는 무용**이라 배치만 돕는다.
- **[x] 손실 경로 3건** — 저장소 파싱 실패 시 조용히 `{}`로 초기화 / 로그인 병합이 엔트리를 통째
  덮어써 둘째 사업자 값을 지움 / 업체 화면 경쟁사 목록이 `catch`로 삼키고 원본을 덮어씀.
- **[x] `usePersistedState`** — 화면 12곳이 각자 localStorage를 읽고 쓰며 하이드레이션·손상 방어를
  제각각 갖고 있었다. 훅이 규칙을 강제한다(초기 state는 넘겨받은 값만 — React #418 재발 차단).

### 정리

- **[x] 스타터 잔재 132파일 −9,793줄 + 의존성 13개.** 빌드 24초 → **14.9초**.
  순서 함정 하나: **readiness 프로브가 `/dashboard/overview`(스타터 데모)를 가리켜서**, 먼저 안
  고치고 지우면 파드가 영영 Ready가 안 되고 **원인이 커밋 diff에 안 보인다**.
- **[x] 헤더 최소화·계정 허브·전역 설정 모달** — 1280에서 헤더가 넘쳐 **브레드크럼이 한 글자씩
  세로로 쌓였다**. 원인은 좌측 flex에 `min-w-0`가 없어 min-content로 눌린 것. 고치다 **넓은 표가
  body 가로 스크롤을 만들던 구조 결함**도 함께 잡혔다.
- **[x] 차트에 축을 세웠다** — 159px의 정체는 viewBox 900×230이 622px 컨테이너에서 축소된 것.
  디자인이 아니라 스케일 사고였다. y축·그리드·축선 추가, 높이 280px 고정, 표본 12 미만은 점 스트립.
- **[x] 웰컴에 증거** — 카피만 있고 제품이 뭘 주는지 증거가 0이었다. 그리고 **세션 boot가
  대시보드에서만 돌아 웰컴에선 Google 로그인 버튼이 영영 안 보였을 구조**였다.
- **[x] 품목 SSOT** — 정의가 세 곳에서 달랐고 zod enum에 **김치가 빠져** 있었다(회차 10,024건).

### 이번에 실측으로만 잡힌 것 (코드를 읽어서는 못 본 것)

- **`SIGUNGU_CD`를 추출조차 안 하고 있었다.** `normalize.py`가 `SIDO_CD`는 뽑는데 바로 옆의
  `SIGUNGU_CD`는 안 뽑았다. 코드를 읽으면 "지역을 다룬다"로 보이지, 뭘 안 뽑는지는 안 보인다.
- **`MLFD_CLASS_CD`는 코드처럼 생겼지만 코드가 아니다.** `_CD`로 끝나고 53% 채움에 두 자리
  숫자다 — 형식 검사 셋을 다 통과한다. 실측하니 **고유 코드 60개 중 45개가 두 개 이상의 이름**을
  갖는다. 품목 목록 안의 순번이었다. **`코드 → 이름`이 함수인지 세어보는 검사**가 이걸 막았다.
- **`PDLC_NM`은 자격 지역이지 소재지가 아니다.** 테스트 픽스처의 `"PDLC_NM":"한우"`를 보고
  "자격 제한이 (지역 × 품목) 쌍"이라는 가설이 섰는데, 전수 288,559행에 **`한우` 같은 값이 0건**이고
  전부 `경남/창원시` 형식이었다. **픽스처가 지어낸 값이었다.** 결정적 반증은 `전남광주/전체` 8건의
  실제 소재지가 **전북 군산·경기**였다는 것 — 자격 지역과 소재지는 다른 축이다.
- **`RBID_YN`은 "재입찰이다"가 아니라 "재입찰 가능"이다.** 'Y'가 91.8%인데 실제 재입찰
  (`CHG_TP_NM`)은 1.5%다. 이름만 보고 재입찰 판정에 썼으면 크게 오독했다.
- **`activeDeadlineSeconds`를 CronJob spec 바로 아래 두면 조용히 무시된다.** JobSpec 필드다.
  커밋도 됐고 sync도 성공했는데 값이 안 들어갔다. **배포 후 실제로 읽어보지 않았으면 "조용한
  실패를 막는 장치"가 그 자체로 조용히 실패한 채 남았을 것이다.**
- **대조군을 둬도 못 거른 오류가 있다.** "광주·전남 0건"을 제주(1,022건)를 대조군으로 두고
  확정했는데, **대조군은 "코드가 맞다"는 가정까지는 검증하지 못한다.**
- **`ON_ERROR_STOP`을 켜기 전에 그 아래가 이미 실패 중일 수 있다.** 뚜껑을 걷는 변경은
  걷기 전에 아래를 확인해야 한다. 이번엔 무오류였지만 그건 운이었다.

### 내가 만든 사고 (같은 실수를 막기 위해 남긴다)

- **없는 결함을 고치다 분석판을 통째로 죽였다.** 이중 `decodeURIComponent`를 제거하며 "Next가
  이미 디코드해 준다"를 전제했는데 이 라우트에선 틀렸다. **모든 분석판 링크가 "학교를 찾지
  못했습니다"로 떴고 배포된 채 있었다.** 게다가 검토 좌석이 "그 버그는 이론상이었다(`%` 든
  학교명 0건)"고 정정해줬는데도 **이미 배포된 걸 되짚지 않았다.**
- **`git add -A`로 다른 좌석의 진행 중 작업을 쓸어 담았다.** 인프라 수정 커밋 하나에 132파일
  삭제가 섞였다. 트리를 보존한 채 셋으로 갈라 다시 올렸다.
- **근거 없는 전제를 팀의 최우선 제약으로 박았다.** "예전에 크롤러가 차단된 적이 있다"고
  반복했는데 **이 프로젝트엔 그런 기록이 없다**(다른 프로젝트 일이었다). 그 전제로 폴링 주기를
  보수적으로 잡았고 병렬화도 막았다. 사용자가 "차단된 적 있음?"이라고 묻자 잡혔고,
  **그 정정 과정에서 시도코드 18 누락(6만 건)이 발견됐다.**

### 다음 (기초공사)

전수검사 3좌석 진행 중 — 원본(모르는 필드) · 파이프라인(있는데 오용) · 서빙(있는데 오용).
그 결과로 **재파싱 한 번**(12~15분) → 키를 이름에서 코드로(학교 `PURR_CD` · 업체 `SHIPPER_CD` ·
지역 `SIGUNGU_CD`) → 계약을 실제 응답에 맞춤 → 판정을 한 곳으로. 백필 완료 예정 8/30.

**아직 안 고친 것**: 자격 판정이 여전히 이름 비교(임시방편) · `school_auctions`에서 10,770건 누락
(공동구매 5,966 + 문턱 4,419) · 인천 학교 4곳이 두 행으로 분리 · 남산초 등 3키가 두 학교 병합 ·
사업자번호→`SHIPPER_CD` 2개인 업체 19건 · 커버리지 8%인 시도 다수(백필 중).
